--- a/target/mips/tcg/translate.c
+++ b/target/mips/tcg/translate.c
@@ -4543,4 +4543,8 @@ static void gen_compute_branch(DisasContext *ctx, uint32_t opc,
         case OPC_BGEZALL:
             tcg_gen_setcondi_tl(TCG_COND_GE, bcond, t0, 0);
-            blink = 31;
+            /* SOC_REF_FIX_BLIKELY_LINK: link only on the taken path. */
+            tcg_gen_movcond_tl(TCG_COND_NE, cpu_gpr[31], bcond,
+                               tcg_constant_tl(0),
+                               tcg_constant_tl(ctx->base.pc_next + 8),
+                               cpu_gpr[31]);
             goto likely;
@@ -4579,4 +4579,8 @@ static void gen_compute_branch(DisasContext *ctx, uint32_t opc,
         case OPC_BLTZALL:
             tcg_gen_setcondi_tl(TCG_COND_LT, bcond, t0, 0);
-            blink = 31;
+            /* SOC_REF_FIX_BLIKELY_LINK: link only on the taken path. */
+            tcg_gen_movcond_tl(TCG_COND_NE, cpu_gpr[31], bcond,
+                               tcg_constant_tl(0),
+                               tcg_constant_tl(ctx->base.pc_next + 8),
+                               cpu_gpr[31]);
             likely:
