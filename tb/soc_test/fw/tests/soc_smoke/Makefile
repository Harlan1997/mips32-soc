# soc_smoke — full peripheral + GPIO + Timer + DMA + PIC + UART + exception smoke test
# Backward-compatible successor of the pre-split monolithic firmware.
TEST_NAME    := soc_smoke
TEST_SOURCES := main.c
FW_TOP_DIR   := ../..
include $(FW_TOP_DIR)/common/Makefile.rules
