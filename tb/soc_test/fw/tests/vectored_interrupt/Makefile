# Minimal product-mode proof for the Cause.IV / IntCtl.VS CPU vector contract.
TEST_NAME       := vectored_interrupt
TEST_SOURCES    := product_vectored_interrupt.s
FW_TOP_DIR      := ../..
CUSTOM_BOOT     := 1
LINKER_SCRIPT   := $(CURDIR)/link.ld
include $(FW_TOP_DIR)/common/Makefile.rules
