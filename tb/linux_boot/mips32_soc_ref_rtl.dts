// SPDX-License-Identifier: GPL-2.0-or-later
/dts-v1/;

/ {
	#address-cells = <1>;
	#size-cells = <1>;
	compatible = "harlan,mips32-soc-ref";

	aliases { serial0 = &uart0; };
	chosen {
		bootargs = "console=ttyS0,115200 earlycon=uart8250,mmio32,0x40000000 rdinit=/init";
		stdout-path = "serial0:115200n8";
	};
	/*
	 * The RTL image stores the kernel at the physical DDR window
	 * 0x08000000.  Keep the Linux physical RAM resource at that same base;
	 * declaring RAM at zero would make ordinary kseg0 data accesses alias the
	 * beginning of the kernel image through the Linux-only crossbar mapping.
	 */
	memory@8000000 { device_type = "memory"; reg = <0x08000000 0x01000000>; };

	cpus {
		#address-cells = <1>;
		#size-cells = <0>;
		cpu@0 {
			compatible = "mips,mips24Kc";
			device_type = "cpu";
			reg = <0>;
			clocks = <&cpu_clk>;
		};
	};
	cpu_clk: cpu-clock {
		compatible = "fixed-clock";
		#clock-cells = <0>;
		clock-frequency = <50000000>;
	};
	cpuintc: interrupt-controller {
		#address-cells = <0>;
		#interrupt-cells = <1>;
		interrupt-controller;
		compatible = "mti,cpu-interrupt-controller";
	};
	uart0: serial@40000000 {
		compatible = "ns16550a";
		interrupt-parent = <&cpuintc>;
		interrupts = <4>;
		reg = <0x40000000 0x1000>;
		reg-shift = <2>;
		reg-io-width = <4>;
		clock-frequency = <50000000>;
		no-loopback-test;
		status = "okay";
	};
};
