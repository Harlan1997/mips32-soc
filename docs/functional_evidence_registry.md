# RTL Functional Evidence Registry

| COP1 CVT.S.D destination-format exception classification | `SYSTEM_DIFFERENTIAL`, opt-in | `source /etc/profile.d/modules.sh && module load vcs && VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M RUN_DIR=/tmp/fpu-flags-cvt-sd-20260903b tb/unit/cpu_test/run_mips_fpu_flags.sh`; `source /etc/profile.d/modules.sh && module load vcs && VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/fpu-cvt-sd-cpu-20260903 make fpu-single-gate`; `source /etc/profile.d/modules.sh && module load vcs && VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M SKIP_COVERAGE=1 QEMU_TIMEOUT=60 BUILD_DIR=/tmp/fpu-cvt-sd-diff-20260903 make qemu-system-fpu-single-differential-gate` | `/tmp/fpu-flags-cvt-sd-20260903b/sim.log`, `/tmp/fpu-cvt-sd-cpu-20260903/soc_test/fpu_single/sim.log`, `/tmp/fpu-cvt-sd-diff-20260903/isa_ref/qemu_system_fpu_single_differential/{completion_report.md,qemu/trace_compare.log}` | `mips_fpu` now classifies `CVT.S.D` using the rounded single result and original double operand: exact conversions remain clear, finite values overflowing to single infinity set Overflow|Inexact, and nonzero values rounded into/to the single subnormal range set Underflow|Inexact. The same host-float boundary fallback preserves exact min-normal*0.5 Underflow behavior when VCS flushes the subnormal result. Unit, real CPU firmware and RTL/QEMU retire differential pass. Complete IEEE-754 range/tininess policy, all COP1 operations, FPE delivery and Linux FPU ABI remain open. |

| QEMU custom-machine current-contract recheck | `SYSTEM_INTEGRATED`, bounded opt-in | `QEMU_BUILD_JOBS=1 QEMU_TIMEOUT=120 BUILD_DIR=/tmp/qemu-current-recheck-20260903 make qemu-system-mips32-soc-ref qemu-system-current-contract-gate` | `/tmp/qemu-current-recheck-20260903/isa_ref/qemu_system_current_contract/` | The custom `mips32-soc-ref` build is reproducible and the current-contract aggregate passes its peripheral, DMA v2, QSPI, DDR and retire-capture children. This confirms the bounded QEMU peripheral model remains closed; physical devices/timing, RTL Linux userspace, unrestricted VM and full ISA/FPU signoff remain open. |

| RTL Linux IRQ/CP0 wakeup probe | `DIAGNOSTIC_EVIDENCE`, bounded opt-in | `source /etc/profile.d/modules.sh && module load vcs && SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M HOST_TIMEOUT=420s RTL_CYCLE_LIMIT=19000000 LINUX_PROGRESS_TRACE=1 LINUX_WAIT_TRACE=1 LINUX_WAIT_TRACE_LIMIT=80 LINUX_VECTOR_TRACE=1 LINUX_VECTOR_TRACE_LIMIT=80 LINUX_CP0_TRACE_LIMIT=80 LINUX_MODE_TRACE=1 LINUX_MODE_TRACE_LIMIT=40 SKIP_LINUX_BUILD=1 KERNEL=/tmp/linux-vic-20260903/linux_boot/real/kernel/vmlinux RUN_DIR=/tmp/rtl-linux-irq-probe-20260903 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl-linux-irq-probe-20260903/{completion_report.md,sim/sim.log}` | The 19M-cycle RTL run completed with simulator status 0 and continuous CPU progress. CP0 traces show repeated Compare programming and Cause.TI/Cause.IP7 assertions; mode traces show repeated interrupt entry at `EBase+0x200` and `eret` returns. No panic/error or missed timer wakeup was observed. This rules out a simple CP0 Compare/WAIT wakeup failure for this artifact, but does not close the later Linux init-task/userspace handoff or full RTL/QEMU Linux differential. |

| Linux GPIO userspace runtime probe | `SYSTEM_INTEGRATED`, bounded opt-in | `LINUX_GPIO_ONLY=1 SKIP_LINUX_BUILD=1 KERNEL=/tmp/linux-gpio-runtime-20260903/kernel/vmlinux DTB=/tmp/linux-gpio-runtime-20260903/mips32_soc_ref.dtb RUN_DIR=/tmp/linux-gpio-userspace-gate3-20260903 make linux-gpio-userspace-gate` | `/tmp/linux-gpio-userspace-gate3-20260903/{qemu_stdout.log,qemu_stderr.log,completion_report.md}` | Dedicated gate passes Linux sysfs mount, GPIO512 export, direction-to-output, value write and readback, with `MIPS32_SOC_LINUX_GPIO_SUCCESS`. This closes the bounded QEMU Linux GPIO userspace contract; physical pads/pinmux/interrupts, RTL Linux userspace and full product signoff remain open. |
| Linux differential ELF-entry handoff auto-discovery | `IMPLEMENTED`, bounded infrastructure | `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M RTL_CYCLE_LIMIT=2000000 MAX_TRACE_RECORDS=200000 QEMU_TIMEOUT=2s HOST_TIMEOUT=240s BUILD_DIR=/tmp/qemu-linux-diff-entryfix-20260903 KERNEL=/tmp/rtl-linux-minimal-recheck-yy9j2t/kernel/vmlinux DTB=/tmp/rtl-linux-minimal-recheck-yy9j2t/mips32_soc_ref.dtb make qemu-system-linux-differential-gate` | `/tmp/qemu-linux-diff-entryfix-20260903/isa_ref/qemu_linux_differential/{completion_report.md,qemu/trace_compare.log}` | The Linux differential runner now derives the QEMU handoff anchor from the supplied kernel ELF entry, while retaining `ALIGN_FIRST_PC` as an explicit override. A fresh 2M-cycle run binds `0x8844e88c` and passes `TRACE_COMPARE_PASS records=29424 mode=stream`; the prior stale `0x88a55c78` default failed before comparison. This fixes artifact portability only; RTL Linux userspace, unrestricted system-mode differential, complete ISA/privileged/MMU/FPU/OS VM and product signoff remain open |

| P1 bounded RTL/simulation aggregate recheck | `CONTRACT_CLOSED`, bounded | `source /etc/profile.d/modules.sh`; `module load vcs`; `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M SKIP_COVERAGE=1 BUILD_DIR=/tmp/p1-current-recheck-20260903 make p1-current-complete` | `/tmp/p1-current-recheck-20260903/cpu_mmu_complete/cpu_mmu_completion_report.md`, `/tmp/p1-current-recheck-20260903/p1_complete/p1_completion_report.md`, child `sim.log`/`compile.log` artifacts | Fresh low-resource recheck passes RTL frontend `8/8`, coherency/stress, four page sizes, hardware walker A/D/reset/error, dual-core MMU shootdown, CPU/MMU, ISA R2, vectored interrupt and DDR4 closure. No OOM or simulator failure observed. This closes the bounded P1 RTL/simulation bundle only; RTL Linux userspace/full RTL-QEMU Linux differential, unrestricted Linux VM, complete ISA/privileged/FPU ABI, full coherency, formal/CDC/RDC/lint and physical signoff remain open |

| QEMU system architecture aggregate after userspace polling fix | `SYSTEM_INTEGRATED`, bounded opt-in | `SKIP_COVERAGE=1 QEMU_TIMEOUT=120 QEMU_BUILD_JOBS=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/qemu-architecture-poll-20260903 LINUX_KERNEL=/tmp/linux-mprotect-poll-20260903/kernel/vmlinux LINUX_DTB=/tmp/linux-mprotect-poll-20260903/mips32_soc_ref.dtb LINUX_CONFIG=/tmp/linux-mprotect-poll-20260903/kernel/.config make qemu-system-architecture-closure-gate` | `/tmp/qemu-architecture-poll-20260903/isa_ref/qemu_system_architecture_closure/completion_report.md`, `source_commit.txt`, `linux_artifact_sha256.txt`, child logs | The full serial aggregate passes current contract, selected ISA/MDU/FPU/privileged/peripheral and MMU/LLSC differentials, precise FPE boundaries, and the repaired QEMU Linux userspace marker gate. The report binds the exact source revision and Linux artifact hashes. This closes the bounded QEMU architecture integration scope only; full ISA/privileged/IEEE-754/FPU ABI, RTL Linux userspace/full RTL-QEMU differential, unrestricted Linux VM/shootdown, physical DDR/QSPI and formal/CDC/RDC/lint signoff remain open. |

| QEMU Linux userspace mprotect/wait4 polling closure | `SYSTEM_INTEGRATED`, opt-in | `QEMU_TIMEOUT=120 JOBS=1 BUILD_DIR=/tmp/linux-mprotect-poll-20260903 RUN_DIR=/tmp/linux-mprotect-poll-20260903 make linux-boot-build-gate`; two artifact rechecks with `SKIP_LINUX_BUILD=1 KERNEL=/tmp/linux-mprotect-poll-20260903/kernel/vmlinux DTB=/tmp/linux-mprotect-poll-20260903/mips32_soc_ref.dtb` | `/tmp/linux-mprotect-poll-20260903/completion_report.md`, `/tmp/linux-mprotect-poll-repeat-20260903/completion_report.md`, `/tmp/linux-mprotect-poll-repeat2-20260903/completion_report.md`, `tb/linux_boot/init.S` | The protected-page child wait now uses exact-PID `WNOHANG` polling plus a bounded 1 ms nanosleep, avoiding the custom single-vCPU SIGSEGV zombie wakeup boundary while retaining exact PID and SIGSEGV status checks. Three runs pass all boot, mmap, mprotect, protected-fault, brk, sleep, yield, fork/exec and wait/status markers. This closes the bounded QEMU Linux userspace contract only; RTL Linux userspace, full RTL/QEMU system differential, unrestricted Linux VM and complete ISA/privileged/FPU signoff remain open. |

| Architecture aggregate Linux artifact audit | `IMPLEMENTED`, infrastructure | `bash -n tb/isa_ref/run_qemu_system_architecture_closure_gate.sh`; valid artifact audit using `/tmp/linux-vic-20260903/linux_boot/real/{kernel/vmlinux,mips32_soc_ref.dtb}`; missing-kernel negative audit | `tb/isa_ref/run_qemu_system_architecture_closure_gate.sh`, `/tmp/arch-audit-negative-20260903.log` | The aggregate now accepts explicit `LINUX_KERNEL` and `LINUX_DTB` inputs and rejects missing or stale artifacts before expensive sub-gates. It requires `CONFIG_MIPS32_SOC_VIC=y` and the SoC VIC DTB compatible string, preventing an old kernel/report mismatch. This improves evidence integrity only; Linux wait4 reproducibility, RTL Linux and full system differential remain open. |

| Linux guest LL/SC success/failure policy refinement | `BLOCK_REDUCED`, opt-in | `QEMU_BUILD_JOBS=1 scripts/qemu/build_mips32_soc_ref.sh`; `SKIP_COVERAGE=1 QEMU_BUILD_JOBS=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/llsc-policy-regression-20260903 make qemu-system-llsc-differential-gate`; three repeated Linux gate runs under `/tmp/linux-sc-policy-{1,2,3}-20260903` | `scripts/qemu/mips32_soc_ref.c`, `scripts/qemu/patches/qemu-9.2-mips32-sc-consume-reservation.patch`, `scripts/qemu/build_mips32_soc_ref.sh`, `/tmp/llsc-policy-regression-20260903/isa_ref/qemu_system_llsc_differential/completion_report.md`, `/tmp/linux-sc-policy-{1,2,3}-20260903/gate.log` | Linux guest mode now retains failed-SC reservations while successful SC consumes the reservation; strict bare-metal RTL/QEMU LL/SC differential remains passing. Linux userspace completes 2/3 runs; the residual failure is the child `wait4` boundary, so this is not a Linux userspace signoff. |

| Linux gate explicit RUN_DIR propagation | `IMPLEMENTED`, infrastructure | `SKIP_LINUX_BUILD=1 KERNEL=/tmp/linux-vic-20260903/linux_boot/real/kernel/vmlinux DTB=/tmp/linux-vic-20260903/linux_boot/real/mips32_soc_ref.dtb QEMU_TIMEOUT=120 RUN_DIR=/tmp/linux-vic-gate-verify-20260903 tb/linux_boot/run_linux_boot_gate.sh` | `Makefile`, `/tmp/linux-vic-gate-verify-20260903/completion_report.md`, `/tmp/linux-vic-gate-verify-20260903/qemu_stdout.log` | The make recipe now preserves an explicitly supplied run directory instead of nesting it under `linux_boot/real`. Artifact audit identified the prior aggregate failure as stale kernel/DTB selection; a VIC-enabled artifact passes the direct gate in one run, while repeated QEMU userspace runs remain nondeterministic after the protected-page SIGSEGV and stay open. |

| Linux SoC VIC cascade and UART child IRQ | `SYSTEM_INTEGRATED`, opt-in | `QEMU_BUILD_JOBS=1 QEMU_TIMEOUT=120 BUILD_DIR=/tmp/linux-vic-20260903 make linux-soc-vic-gate` | `/tmp/linux-vic-20260903/linux_boot/real/kernel/.config`, `build.log`, `qemu_stdout.log`, `completion_report.md` | The tracked Linux irqchip overlay builds into vmlinux as `CONFIG_MIPS32_SOC_VIC=y`; the boot log proves `mips32-soc-vic` initialized on CPU IRQ 2 and `ttyS0` received a child-domain IRQ. The gate also passes boot, mmap/mprotect, protected-write fault, brk, sleep/yield, fork/exec/wait markers. RTL userspace boot, full RTL/QEMU Linux differential, unrestricted VM/shootdown and complete ISA/MMU/OS signoff remain open |
| RTL Linux with SoC VIC kernel | `INCOMPLETE`, opt-in | `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M SKIP_LINUX_BUILD=1 KERNEL=/tmp/linux-vic-20260903/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=20000000 LINUX_PC_TRACE=1 LINUX_PC_TRACE_RETIRE_ONLY=1 LINUX_PC_TRACE_SYMBOL=kernel_init RUN_DIR=/tmp/rtl-linux-vic-kinit-20260903 make rtl-linux-progress-gate` | `/tmp/rtl-linux-vic-kinit-20260903/{completion_report.md,sim.log,sim/sim_runtime.log}` | The real RTL image using the kernel with `CONFIG_MIPS32_SOC_VIC=y` elaborates and runs 20M cycles with continuous progress and no OOM/panic. `kernel_init` records are present, but `run_init_process` and userspace markers remain absent; the final window is `r4k_wait`. RTL Linux userspace and full RTL/QEMU Linux differential remain open |
| RTL Linux TLB refill exception detail | `DIAGNOSTIC_EVIDENCE`, opt-in | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=/tmp/rtl-linux-minimal-recheck-yy9j2t/kernel/vmlinux LINUX_REQUIRE_PROGRESS=0 LINUX_PROGRESS_TRACE=0 LINUX_EXCEPTION_TRACE=1 LINUX_EXCEPTION_TRACE_LIMIT=4 LINUX_TLB_TRACE=1 LINUX_TLB_TRACE_LIMIT=12 LINUX_TLB_TRACE_CYCLE_START=5690000 LINUX_TLB_TRACE_CYCLE_END=5710000 RUN_DIR=/tmp/rtl-linux-exception-detail-20260903 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl-linux-exception-detail-20260903/sim/sim_runtime.log` | `LINUX_EXCEPTION_DETAIL` captures the first architectural `TLBS` at `0x885c54c0: sw v1,16(v0)` with `VA=0xc0000010`, stable data-fault state, no data-bus error, and the subsequent invalid then valid `TLBWR` handler sequence. This narrows the remaining issue to Linux refill/VM behavior; it does not close RTL Linux userspace, full RTL/QEMU differential, or unrestricted OS VM semantics |

| COP1 MTHC1/MFHC1 high-word transfers | `SYSTEM_DIFFERENTIAL`, opt-in | `source /etc/profile.d/modules.sh && module load vcs && VCS_JOBS=1 QEMU_BUILD_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M SKIP_COVERAGE=1 BUILD_DIR=/tmp/fpu-highword-qemu-20260903 make mips-control-fpu-cond-gate rtl-frontend-compile fpu-single-gate qemu-system-fpu-single-differential-gate` | `/tmp/fpu-highword-20260903/soc_test/fpu_single/sim.log`, `/tmp/fpu-highword-qemu-20260903/isa_ref/qemu_system_fpu_single_differential/completion_report.md`, `qemu/trace_compare.log` | Added legal COP1 transfer decode with zeroed reserved fields, FPR high-word commit/readback through the even-low/odd-high double-pair representation, and GPR hazard handling. The real CPU gate passes and RTL/QEMU system retire differential passes. Full IEEE-754 policy, FPE delivery, lazy-FPU/signal-frame ABI and full COP1 compliance remain open |

| COP1 MOVF/MOVT single/double conditional move | `IMPLEMENTED`, opt-in | `source /etc/profile.d/modules.sh && module load vcs && VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M SKIP_COVERAGE=1 BUILD_DIR=/tmp/fpu-movfc-diff-20260903 make mips-control-fpu-cond-gate rtl-frontend-compile fpu-single-gate qemu-system-fpu-single-differential-gate` | `/tmp/fpu-movfc-20260903/soc_test/fpu_single/sim.log`, `/tmp/fpu-movfc-diff-20260903/isa_ref/qemu_system_fpu_single_differential/completion_report.md`, `qemu/trace_compare.log` | Added COP1 funct `0x11` decode for MOVF/MOVT in single and even-register-pair double formats, reserved-field rejection, FCC/tf selection and FPR writeback. RTL CPU gate passes; RTL/QEMU system retire comparison passes (`TRACE_COMPARE_PASS records=1392`). Full IEEE-754 policy, FPE delivery, lazy-FPU/signal-frame ABI and full COP1 compliance remain open |
| COP1 MOVF/MOVT double retire differential extension | `IMPLEMENTED`, opt-in | `source /etc/profile.d/modules.sh && module load vcs && VCS_JOBS=1 QEMU_BUILD_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M SKIP_COVERAGE=1 BUILD_DIR=/tmp/fpu-movfc-double-diff-20260903 make qemu-system-fpu-double-differential-gate` | `/tmp/fpu-movfc-double-20260903/soc_test/fpu_double/sim.log`, `/tmp/fpu-movfc-double-diff-20260903/isa_ref/qemu_system_fpu_double_differential/completion_report.md`, `qemu/trace_compare.log` | The double-pair firmware now covers FCC0 false/true `MOVF.D/MOVT.D`; RTL gate and QEMU system retire differential pass (`TRACE_COMPARE_PASS records=374`). Full IEEE-754 policy, FPE delivery, lazy-FPU/signal-frame ABI and full COP1 compliance remain open |

| Linux static TLB handler retire diagnosis | `DIAGNOSTIC_EVIDENCE`, opt-in | `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M RTL_CYCLE_LIMIT=48000000 LINUX_PC_TRACE=1 LINUX_PC_TRACE_RETIRE_ONLY=1 LINUX_PC_TRACE_START=0x8801a140 LINUX_PC_TRACE_END=0x8801a1c0 LINUX_PC_TRACE_CYCLE_START=46700000 LINUX_PC_TRACE_CYCLE_END=46800000 LINUX_TLB_TRACE=1 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl-linux-static-handler-recheck-20260903/sim.log` | The static generated `handle_tlbl` code retires continuously from `0x8801a148` through the `TLBP` and conditional branch at `0x8801a198`, then proceeds outside the narrow trace window. This confirms the I-cache maintenance contract remains translated-PA for both physically tagged caches and rules out the earlier VA-address hypothesis. The Linux userspace marker remains absent; this is diagnosis only, not Linux boot or VM signoff |

| RTL Linux bootrom absolute-address relocation fix | `IMPLEMENTED`, opt-in | `KERNEL=/tmp/mips32-linux-rtl-minimal-20260903/kernel/vmlinux RUN_DIR=/tmp/rtl-linux-minimal-fix-20260903/image tb/linux_boot/build_rtl_linux_image.sh`; `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M HOST_TIMEOUT=600s RTL_CYCLE_LIMIT=60000000 LINUX_REQUIRE_USERSPACE=1 LINUX_PROFILE=rtl-minimal SKIP_LINUX_BUILD=1 KERNEL=/tmp/mips32-linux-rtl-minimal-20260903/kernel/vmlinux RUN_DIR=/tmp/rtl-linux-minimal-fixed-60m-20260903 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl-linux-minimal-fix-20260903/image/bootrom.elf`, `/tmp/rtl-linux-minimal-fixed-60m-20260903/{completion_report.md,sim/sim.log}` | `%hi` + `ori` previously reconstructed `0x8844e6ec` as `0x8845e6ec`, causing a TLBL and execution through the empty `BFC0_0200` aperture. `%hi` + `addiu` now reconstructs the target exactly. The repaired 60M-cycle run remains memory-bounded, executes legal kernel `__bzero`/IRQ/TLB paths and has no OOM/panic, but observes zero userspace markers; RTL Linux userspace and full RTL/QEMU Linux differential remain open |

| RTL Linux minimal-profile propagation and bounded recheck | `INCOMPLETE`, opt-in | `BUILD_DIR=/tmp/mips32-linux-rtl-minimal-20260903 LINUX_PROFILE=rtl-minimal JOBS=1 tb/linux_boot/build_linux_boot.sh`; `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M HOST_TIMEOUT=300s RTL_CYCLE_LIMIT=30000000 LINUX_REQUIRE_USERSPACE=1 LINUX_PROFILE=rtl-minimal SKIP_LINUX_BUILD=1 KERNEL=/tmp/mips32-linux-rtl-minimal-20260903/kernel/vmlinux RUN_DIR=/tmp/rtl-linux-minimal-userspace-20260903 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/mips32-linux-rtl-minimal-20260903/{rebuild.log,image-rebuild.log,image_manifest.txt}` and `/tmp/rtl-linux-minimal-userspace-20260903/{completion_report.md,sim/sim.log}` | Profile-aware kernel/image construction and runner propagation pass. The 30M-cycle RTL run exits normally with about 1.1 MB VCS data structure, continued post-reset progress and no OOM/panic, but observes zero Linux userspace markers. This narrows the open issue to later init-task/scheduler handoff; it does not close RTL Linux userspace or full RTL/QEMU Linux differential |

| Verification foundation module-aware tool inventory | `FOUNDATION_READY_WITH_EXPLICIT_TOOL_STATUS` | `bash -n scripts/run_verification_foundation_gate.sh && RUN_ROOT=/tmp/verification-foundation-recheck-20260903 scripts/run_verification_foundation_gate.sh` | `/tmp/verification-foundation-recheck-20260903/{verification_foundation_report.md,tool_inventory.tsv,formal_static_audit.log,formal_bind_compile.log}` | The foundation gate now initializes the project EDA modules before probing tools and records module/VCS load status. A fresh run reports VCS available, static formal audit `files=5 modules=5 assertions=12`, 5/5 DUT bind elaborations and two waiver files checked. Formal proof, CDC/RDC and lint engines remain unavailable and explicitly deferred; this is not signoff. |

| RTL Linux `kernel_init_freeable` tail retire probe | `BLOCK_VERIFIED`, diagnostic only | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=14000000 HOST_TIMEOUT=300s RUN_DIR=/tmp/rtl-linux-initcall-tail-20260902 LINUX_PROGRESS_TRACE=1 LINUX_PC_TRACE=1 LINUX_PC_TRACE_RETIRE_ONLY=1 LINUX_PC_TRACE_START=88ced000 LINUX_PC_TRACE_END=88ced190 LINUX_PC_TRACE_CYCLE_START=10000000 LINUX_PC_TRACE_CYCLE_END=14000000 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl-linux-initcall-tail-20260902/completion_report.md`, `sim/sim.log` | The real RTL reaches and returns from the observed `driver_init` call at cycle 11289759, then enters the normal `r4k_wait` idle path around 11.8M cycles. Timer IRQ wakeups continue through the 14M-cycle bound; no `run_init_process`, kernel-to-user ERET or userspace marker appears. This narrows the blocker to later kernel-init scheduling/handoff or an unobserved initcall, without proving an ISA/CP0/WAIT mismatch; RTL Linux userspace and full RTL/QEMU Linux differential remain open |

| QEMU system MMU root/context lease differential | `SYSTEM_DIFFERENTIAL`, bounded opt-in | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/mmu-context-qemu-lease2-20260902 QEMU_TIMEOUT=60 tb/isa_ref/run_qemu_system_mmu_contract_gate.sh` | `/tmp/mmu-context-qemu-lease2-20260902/{completion_report.md,rtl_gate.log,rtl/rtl_retire.jsonl,qemu/qemu_retire.jsonl,qemu/trace_compare.log}` | The real MMU-enabled RTL and `mips32-soc-ref` compare `TRACE_COMPARE_PASS records=349`. The corpus covers root lease stale/valid release and generation reuse plus four-slot atomic root/ASID context allocation, stale release rejection and generation-1 reuse. This closes the bounded QEMU model/RTL ownership-token slice; Linux page-table allocation, unrestricted demand paging, multicore shootdown, full privileged/MMU compliance and product signoff remain open |

| QEMU selected differential aggregate MMU isolation recheck | SYSTEM_DIFFERENTIAL, bounded opt-in | source /etc/profile.d/modules.sh && module load vcs && SKIP_COVERAGE=1 QEMU_TIMEOUT=180 HOST_TIMEOUT=300 QEMU_BUILD_JOBS=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/qemu-selected-mmu-isolated-20260902 make qemu-system-selected-differential-gate | /tmp/qemu-selected-mmu-isolated-20260902/isa_ref/qemu_system_selected_differential/completion_report.md and child logs/reports | After propagating isolated BUILD_DIR-derived paths through the MMU contract and PageMask Makefile recipes as well as process-pressure, the complete selected aggregate passes again. This confirms the bounded MMU differential corpus is reproducible without repository-default firmware/trace reuse; complete ISA/privileged compliance, unrestricted Linux VM, full coherency and product signoff remain open |
| QEMU selected differential aggregate including SRS corpus | SYSTEM_DIFFERENTIAL, bounded opt-in | `SKIP_COVERAGE=1 QEMU_TIMEOUT=180 QEMU_BUILD_JOBS=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/qemu-selected-srs-20260902 make qemu-system-selected-differential-gate` | `/tmp/qemu-selected-srs-20260902/isa_ref/qemu_system_selected_differential/completion_report.md` and `srs_{exception,nested,map}.log`/child reports | The selected serial aggregate now includes the existing opt-in SRS exception-entry/ERET, EXL-held nested-fault and Cause.IP2/SRSMap system differential gates. This closes aggregate coverage of the bounded SRS corpus; external VEIC/EICSS, Linux SRS ABI, scheduler ownership and complete privileged-ISA compliance remain open |

| QEMU selected differential aggregate including MMU context contract | SYSTEM_DIFFERENTIAL, bounded opt-in | source /etc/profile.d/modules.sh && module load vcs && SKIP_COVERAGE=1 QEMU_TIMEOUT=180 HOST_TIMEOUT=300 QEMU_BUILD_JOBS=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/qemu-selected-mmu-contract-20260902 make qemu-system-selected-differential-gate | /tmp/qemu-selected-mmu-contract-20260902/isa_ref/qemu_system_selected_differential/completion_report.md and child logs/reports | The selected serial aggregate now includes the ASID/context/shootdown MMU contract in addition to IPI, 4-KiB refill, PageMask and eight-ASID process-pressure differentials, and passes. This remains bounded single-core software-managed MMU evidence; unrestricted Linux VM, full privileged compliance, multicore shootdown and product signoff remain open |

| QEMU selected differential aggregate including OS page-table pressure | SYSTEM_DIFFERENTIAL, bounded opt-in | source /etc/profile.d/modules.sh && module load vcs && SKIP_COVERAGE=1 QEMU_TIMEOUT=180 HOST_TIMEOUT=300 QEMU_BUILD_JOBS=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/qemu-selected-mmu-os-20260902 make qemu-system-selected-differential-gate | /tmp/qemu-selected-mmu-os-20260902/isa_ref/qemu_system_selected_differential/completion_report.md and child logs/reports | The selected aggregate now includes the separate SOC_MMU_OS_PRESSURE four-task root/L2 page-table workload, in addition to MMU context, IPI, refill, PageMask and eight-ASID process pressure, and passes. This remains bounded single-core evidence; unrestricted Linux demand paging/VM, full privileged compliance, multicore ownership and product signoff remain open |

| QEMU selected aggregate with FPU branch/FPE and LL/SC coverage | SYSTEM_DIFFERENTIAL, bounded opt-in | source /etc/profile.d/modules.sh && module load vcs && SKIP_COVERAGE=1 QEMU_TIMEOUT=180 HOST_TIMEOUT=420 QEMU_BUILD_JOBS=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/qemu-selected-full-20260902 make qemu-system-selected-differential-gate | /tmp/qemu-selected-full-20260902/isa_ref/qemu_system_selected_differential/completion_report.md and child logs/reports | The selected serial aggregate passes ISA/MDU/control-flow/exception/peripheral/VIC, FPU single/double/rounding/CU1/branch and seven FPE boundary differentials, DMA, MMU context/IPI/refill/PageMask/process-pressure/OS-pressure, L1/L2 nonblocking, and LL/SC reservation differential. FPU branch uses QEMU 24Kf explicitly. This remains bounded evidence and does not claim complete ISA, IEEE-754, Linux VM, coherency or product signoff |

| QEMU selected differential aggregate with MMU refill/PageMask/process pressure | SYSTEM_DIFFERENTIAL, bounded opt-in | source /etc/profile.d/modules.sh && module load vcs && SKIP_COVERAGE=1 QEMU_TIMEOUT=180 HOST_TIMEOUT=300 QEMU_BUILD_JOBS=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/qemu-selected-mmu-fixed-20260902 make qemu-system-selected-differential-gate | /tmp/qemu-selected-mmu-fixed-20260902/isa_ref/qemu_system_selected_differential/completion_report.md and child logs/reports | The selected serial aggregate passes all existing ISA, MDU, exception, privileged, unaligned, peripheral/VIC, FPU, DMA, and L1/CPU-ROB/DDR/L2 nonblocking gates, plus MMU IPI, 4-KiB demand refill, PageMask and eight-ASID process-pressure retire differentials. The process-pressure Makefile recipe now propagates BUILD_DIR, preventing stale repository build reuse. Scope remains bounded; full ISA/privileged compliance, unrestricted demand paging/Linux VM, full coherency and product signoff remain open |

| L2 nonblocking parallel refill reset-in-flight | `UNIT_VERIFIED`, opt-in | `source /etc/profile.d/modules.sh && module load vcs && VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M RUN_DIR=/tmp/l2nb-reset-final tb/unit/cache/run_l2_nb_downstream_gate.sh` | `/tmp/l2nb-reset-final/{compile.log,sim.log}` | The two-slot L2 test asserts reset while both clean refill slots are active, clears abandoned expectations, releases reset, and verifies a new refill completes. The same run retains eight propagated error beats, per-RID isolation, dirty writeback/refill overlap, and cross-ID interleave evidence. Arbitrary reset/error timing combinations, snoop-WB reset, full coherency/directory behavior and commercial signoff remain open |

| L2 nonblocking parallel refill error/retry | `UNIT_VERIFIED`, opt-in | `source /etc/profile.d/modules.sh && module load vcs && VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M RUN_DIR=/tmp/l2nb-error-final3 tb/unit/cache/run_l2_nb_downstream_gate.sh` | `/tmp/l2nb-error-final3/{compile.log,sim.log}` | The two-slot L2 test injects `SLVERR` on one RID for a complete eight-beat burst while a second RID completes normally, checks eight propagated error beats, disables injection, and successfully refills the failed line. The run also retains `peak_downstream=3`, cross-ID interleave, and dirty writeback/refill overlap evidence. Arbitrary AXI ordering, reset/error combinations, full coherency/directory behavior and CPU/default-path signoff remain open |

| FPU double FPE inexact/underflow RTL/QEMU differential | `SYSTEM_DIFFERENTIAL`, opt-in | `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/fpu-boundary-diff-20260902 make qemu-system-fpu-fpe-double-inexact-differential-gate`; `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/fpu-underflow-diff2-20260902 make qemu-system-fpu-fpe-double-underflow-differential-gate` | `/tmp/fpu-boundary-diff-20260902/isa_ref/qemu_system_fpu_fpe_double_inexact_differential/completion_report.md`, `/tmp/fpu-underflow-diff2-20260902/isa_ref/qemu_system_fpu_fpe_double_underflow_differential/completion_report.md`, child `qemu/trace_compare.log` files | Real CPU/SoC and `mips32-soc-ref` compare enabled double `CVT.W.D 1.5` Inexact and minimum-subnormal-times-half Underflow behavior one retire at a time; both differential gates pass. This closes only these selected double flag boundaries; complete IEEE-754 tininess/range policy, full double FPE corpus, Linux FPU context/ABI and full COP1 remain open |

| MMU OS pressure 8x8 RTL/QEMU differential | `SYSTEM_DIFFERENTIAL`, opt-in | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/mmu-process-pressure-diff-8x8-tlbwi-20260902 make qemu-system-mmu-process-pressure-gate` | `/tmp/mmu-process-pressure-diff-8x8-tlbwi-20260902/{completion_report.md,rtl_gate.log,qemu_gate.log,qemu/trace_compare.log}` | The real CPU and `mips32-soc-ref` compare the deterministic software-owned refill workload one retire at a time. Eight ASIDs each touch eight 4-KiB pages, using 32 dynamic TLB pairs, reverse context reuse, wired retention, full dynamic TLBWI shootdown, and post-shootdown demand refills; the RTL reports `refills=73` and the strict comparator passes. TLBWR hardware-random timing is intentionally outside this deterministic differential corpus; Linux VM/page-table ownership, multicore shootdown and full privileged/MMU compliance remain open |

| CPU/MMU aggregate recheck including Machine Check runtime | `CONTRACT_CLOSED`, current RTL scope | `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/cpu-mmu-complete-mcheck-20260902 RUN_ROOT=/tmp/cpu-mmu-complete-mcheck-20260902/cpu_mmu_complete make cpu-mmu-complete` | `/tmp/cpu-mmu-complete-mcheck-20260902/cpu_mmu_complete/{cpu_mmu_completion_report.md,cpu_mmu_completion.log}` and child logs | The complete CPU/MMU aggregate passes with the new real CPU/SoC duplicate-match Machine Check runtime gate included. This revalidates the current single-core RTL/simulation bundle only; complete ISA/FPU, Linux/OS VM semantics, full coherency and product signoff remain open |

| CPU/SoC MMU duplicate-match Machine Check runtime | `SOC_INTEGRATED`, opt-in | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/mmu-machine-check-20260902 make product-mmu-machine-check-gate` | `/tmp/mmu-machine-check-20260902/soc_test/product_mmu_machine_check/{compile.log,sim.log}`, firmware map/objdump | Real `SOC_MMU_ENABLE=1` + `SOC_MICRO_TLB_ENABLE=1` execution fills the D micro-TLB from main entry 1, installs overlapping main entry 2, injects stale fast-path state, and reaches the CPU exception path. The gate observes `Cause.ExcCode=0x18`, general vector `EBase+0x180`, matching EPC and the success mailbox. This closes the runtime Machine Check slice; complete privileged/MMU compliance, OS page-table ownership, unrestricted demand paging and Linux VM remain open |

| MMU micro-TLB duplicate-match Machine Check | `UNIT_VERIFIED`, opt-in | `source /etc/profile.d/modules.sh && module load vcs && VCS_JOBS=1 make tlb-invalidate-gate`; `source /etc/profile.d/modules.sh && module load vcs && VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M SKIP_COVERAGE=1 BUILD_DIR=/tmp/tlb-multihit-frontend-20260902 make rtl-frontend-compile` | `/home/admin/mips32-soc/build/unit_tb/tlb_invalidate/{compile.log,sim.log}`, `/tmp/tlb-multihit-frontend-20260902/unit_tb/rtl_frontend_compile/*/compile.log` | I/D micro-TLB hits no longer mask a duplicate match in the authoritative main TLB. The directed test fills both micro ports, installs an overlapping second entry, and observes `lookup0_multi_hit=1` and `lookup1_multi_hit=1`; the full frontend matrix passes `8/8`. This closes the current duplicate-detection slice only; complete privileged/MMU compliance, OS page-table ownership, unrestricted demand paging and Linux VM remain OPEN |

| Bounded Linux RTL/QEMU retire differential, streaming 2M recheck | `SYSTEM_DIFFERENTIAL`, bounded opt-in | `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M RTL_CYCLE_LIMIT=2000000 QEMU_TIMEOUT=10s HOST_TIMEOUT=240s KERNEL=build/linux_boot/real/kernel/vmlinux DTB=build/linux_boot/real/mips32_soc_ref.dtb BUILD_DIR=/tmp/qemu-linux-diff-2m-fixed-20260902 make qemu-system-linux-differential-gate` | `/tmp/qemu-linux-diff-2m-fixed-20260902/isa_ref/qemu_linux_differential/completion_report.md`, `rtl_gate.log`, `qemu_gate.log`, `qemu/trace_compare.log` | The streaming comparator consumes RTL/QEMU JSONL incrementally and passes `TRACE_COMPARE_PASS records=143248 mode=stream`. The previous list-based comparator reached about 3 GB Python RSS and was OOM-killed on the 606 MB RTL plus roughly 200 MB QEMU traces; the independent large-trace check measured about 10 MB RSS. This closes comparator memory safety for the bounded prefix only; full RTL Linux userspace boot, unrestricted system-mode differential, complete ISA/privileged/MMU/FPU/OS VM and product signoff remain OPEN |

| Bounded Linux RTL/QEMU retire differential recheck | `SYSTEM_DIFFERENTIAL`, bounded opt-in | `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M RTL_CYCLE_LIMIT=100000 QEMU_TIMEOUT=2s HOST_TIMEOUT=180s KERNEL=build/linux_boot/real/kernel/vmlinux DTB=build/linux_boot/real/mips32_soc_ref.dtb BUILD_DIR=/tmp/qemu-linux-diff-20260902b make qemu-system-linux-differential-gate` | `/tmp/qemu-linux-diff-20260902b/isa_ref/qemu_linux_differential/completion_report.md`, `rtl_gate.log`, `qemu_gate.log`, `qemu/trace_compare.log` | The real RTL Linux trace and `mips32-soc-ref` compare one retire at a time after the explicit Boot ROM-to-kernel handoff and pass the bounded 100K-cycle prefix. This refreshes the lockstep baseline only; full RTL Linux userspace boot, unrestricted system-mode Linux differential, complete ISA/privileged/MMU/FPU/OS VM and product signoff remain OPEN |

| Scheduler busy-time reschedule retention | `UNIT_VERIFIED`, opt-in | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M RUN_DIR=/tmp/scheduler-pending-20260902d tb/unit/cpu_test/run_scheduler_timer_ipi.sh`; `RUN_DIR=/tmp/cpu-scheduler-20260902d tb/unit/cpu_test/run_cpu_scheduler.sh`; `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/scheduler-frontend-20260902 make rtl-frontend-compile` | `/tmp/scheduler-pending-20260902d/sim.log`, `/tmp/cpu-scheduler-20260902d/sim.log`, `/tmp/scheduler-frontend-20260902/unit_tb/rtl_frontend_compile/*` | `cpu_scheduler` now retains a timer/IPI/yield trigger received during context save/restore and launches one deferred switch after restore; the request is consumed at launch so a level held by `active_mask` cannot create an endless loop. Busy-time IPI integration, legacy scheduler unit test and all 8 frontend configurations pass. This closes the bounded hardware handshake only; Linux scheduler policy, OS ownership, SMP scheduling and long-term shootdown remain OPEN |

| COP1 reciprocal special-value boundary | `SYSTEM_DIFFERENTIAL`, opt-in | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M SKIP_COVERAGE=1 QEMU_TIMEOUT=60 BUILD_DIR=/tmp/fpu-zero-diff4-20260902 make qemu-system-fpu-single-differential-gate qemu-system-fpu-double-differential-gate`; `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M make mips-fpu-recip-gate` | `/tmp/fpu-zero-diff4-20260902/isa_ref/qemu_system_fpu_{single,double}_differential/qemu/trace_compare.log`, child `rtl/vcs_uvm.log`, `/tmp/fpu-special-unit-20260902/unit_tb/mips_fpu_recip/{compile.log,sim.log}` | `RECIP.{S,D}` and `RSQRT.{S,D}` return signed infinity for signed zero and set Divide-by-zero; reciprocal of signed infinity and RSQRT of positive infinity return signed zero, negative-zero RSQRT preserves sign, and negative RSQRT raises Invalid with the default NaN. Primitive boundary flags and real CPU firmware pass; both QEMU system-mode retire differentials pass. Complete IEEE-754 edge cases, FPE delivery policy and OS FPU ABI remain OPEN |

| BPU formal invariant asset | `FOUNDATION_READY_WITH_EXPLICIT_TOOL_STATUS` | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/verification-foundation-bpu-20260902 make verification-foundation-gate` | `/tmp/verification-foundation-bpu-20260902/verification_foundation/verification_foundation_report.md`, `formal_static_audit.log`, `formal_bind_compile/formal_bind_compile_report.md` | Added real-DUT BPU bindings for BTB taken-training and BHT flush-direction training. Static audit reports `files=5 modules=5 assertions=12`; VCS bind elaboration passes `5/5`. No solver, CDC/RDC or lint engine is available, so proof/signoff remains OPEN |

| BPU mispredict resolution training | `SOC_INTEGRATED`, opt-in | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/bpu-mispredict-fix-submit-20260902 make bpu-redirect-gate`; `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/bpu-mispredict-soc-smoke-20260902 make soc-smoke` | `/tmp/bpu-mispredict-fix-submit-20260902/unit_tb/bpu_redirect_unit/sim.log`, frontend compile logs, `/tmp/bpu-mispredict-soc-smoke-20260902/soc_test/smoke/sim.log` | BPU now trains on every valid architectural ID resolve, including a mispredict with `flush_if` asserted; CPU suppresses resolve only for precise exception/context restore and keeps IF recovery separate. Unit test observes BHT learning actual not-taken and BTB retention under flush. BPU gate and default SoC smoke pass. Fetch queue, multiple in-flight branches, performance targets and formal remain open |

| RTL Linux direct-sim diagnostic defaults | `IMPLEMENTED`, resource safety | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=100000 LINUX_PROGRESS_TRACE=1 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl-linux-diagnostic-defaults-20260902/sim/sim.log`, `completion_report.md` | Cache-op and CP0 diagnostic limits now default to zero in `tb_mips_soc.v`, while explicit limits remain supported. The smoke produces zero actual cache-op/CP0 trace records, about 4.3 KiB of log and about 1.1 MB VCS data structure. This is resource containment only; RTL Linux userspace and full differential remain OPEN |

| RTL Linux trace hexadecimal-argument normalization | `IMPLEMENTED`, diagnostic infrastructure | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=100000 LINUX_PC_TRACE=1 LINUX_PC_TRACE_RETIRE_ONLY=1 LINUX_PC_TRACE_START=0x88d10844 LINUX_PC_TRACE_END=0x88d11070 LINUX_STALL_TRACE_PC=0x88a55b9c LINUX_CACHEOP_TRACE_LINE=0x1234 LINUX_TARGET_TRACE_LINE=0x40133b LINUX_DELAY_TRACE_START=0x892434e0 LINUX_DELAY_TRACE_END=0x892434e4 LINUX_FAULT_TRACE_START=0xc0000000 LINUX_FAULT_TRACE_END=0xc0010000 LINUX_VECTOR_LINE=0x470410 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl-linux-all-hex-smoke-20260902/completion_report.md`, `/tmp/rtl-linux-all-hex-smoke-20260902/sim/sim.log` | The runner strips an optional `0x/0X` prefix for all `%h` trace addresses before forwarding them to VCS. The all-hex-prefix smoke passes, the command contains canonical digits, and no `Too many digits` warning occurs. This fixes diagnostic argument handling only; it does not close RTL Linux userspace or full differential |

| RTL Linux `driver_init` post-return focused trace | `BLOCK_VERIFIED`, diagnostic only | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=12200000 LINUX_PROGRESS_TRACE=1 LINUX_PC_TRACE=1 LINUX_PC_TRACE_RETIRE_ONLY=1 LINUX_PC_TRACE_LIMIT=300 LINUX_PC_TRACE_START=88d10844 LINUX_PC_TRACE_END=88d11070 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl-linux-ofcore-trace-20260902b/completion_report.md`, `/tmp/rtl-linux-ofcore-trace-20260902b/sim/sim.log` | Corrected no-prefix PC arguments produce valid retire records: `driver_init` invokes `of_core_init` and returns through `0x88d1084c` at cycle `11371701`; the bounded 12.2M-cycle run completes with VCS data structure about 1.1 MB and no userspace marker. This rules out a local `driver_init`/`of_core_init` infinite loop, but does not close subsequent init-task handoff, RTL Linux userspace, or full RTL/QEMU differential |

| Current ISA/QEMU DDR/tooling recheck | `IMPLEMENTED`, bounded | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M SKIP_COVERAGE=1 BUILD_DIR=/tmp/closure-recheck-20260902 make isa-implementation-audit qemu-system-ddr-gate qemu-system-state-converter-test` | `/tmp/closure-recheck-20260902/isa_audit/isa_implementation_audit.log`, `/tmp/closure-recheck-20260902/isa_ref/qemu_system_ddr/`, QEMU state-converter test output | ISA audit reports `ISA_IMPLEMENTATION_AUDIT_PASS rows=20`; the custom `mips32-soc-ref` DDR behavioral window and state-converter tests pass. This refreshes bounded implementation evidence only; full ISA/privileged ISA, full RTL/QEMU system differential, Linux RTL userspace, physical DDR timing and product signoff remain OPEN |

| Hardware walker PTE A/D update handshake | `UNIT_VERIFIED`, opt-in | `RUN_DIR=build/unit_tb/page_table_walker tb/unit/mmu/run_page_table_walker.sh` | `rtl/cpu/mips_page_table_walker.v`, `tb/unit/mmu/tb_page_table_walker.sv`, `build/unit_tb/page_table_walker/{compile.log,sim.log}` | The opt-in walker emits a stable leaf-PTE address and sets Accessed for reads or Accessed+Dirty for stores, waits for `pte_update_ready`, reports `pte_update_error` as `FAULT_BUS`, then returns the translation. The default SoC read-only integration is unchanged; OS PTE ownership and Linux VM remain OPEN |
| Hardware walker A/D SoC writeback integration | `SOC_INTEGRATED`, opt-in smoke | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/mmu-ad-backpressure-20260902 make mmu-hardware-walker-ad-soc-gate` | `/tmp/mmu-ad-backpressure-20260902/soc_test/mmu_hardware_walker_ad_soc/sim.log` and firmware artifacts | The complete SoC elaborates with `SOC_HARDWARE_WALKER_AD_ENABLE=1`, routes walker PTE writes through the shared D-side AXI AW/W/B path, and the firmware passes with `aw=3`, `w=3`, `delayed_w=3`. The test observes independent AW/W timing and verifies all three PTE payloads (`0x0000603b`, `0x0000703b`, `0x0000801d`). Injected `SLVERR`, arbitrary reset/error interleavings and OS page-table ownership remain separate contracts |
| Hardware walker A/D SoC reset-in-flight | `SOC_INTEGRATED`, opt-in | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/mmu-ad-reset-20260902 make mmu-hardware-walker-ad-reset-soc-gate` | `/tmp/mmu-ad-reset-20260902/soc_test/mmu_hardware_walker_ad_reset_soc/{vcs.log,sim.log}` | The test asserts reset after the walker accepts AW while W remains pending, then releases reset and requires the restarted firmware to complete all three PTE A/D writebacks. The run passes with `aw=4`, `w=3`, `delayed_w=3`, proving the shared AXI owner is cleared without deadlock. Arbitrary reset/error interleavings and OS page-table ownership remain OPEN |
| Hardware walker A/D SoC writeback `SLVERR` | `SOC_INTEGRATED`, opt-in negative gate | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/mmu-ad-write-error-20260902k make mmu-hardware-walker-ad-write-error-soc-gate` | `/tmp/mmu-ad-write-error-20260902k/soc_test/mmu_hardware_walker_ad_write_error_soc/{vcs.log,sim.log}` | The behavioral DDR model injects one write `SLVERR` at PTE address `0x00002080`; the default L2 write-through path now preserves and forwards `m_bresp`, the walker observes `pte_update_error`, and the failed PTE remains `0x0000600b`. The gate passes with `MMU_AD_AXI_WRITE_ERROR_PASS`; physical fault timing and arbitrary reset/error interleavings remain OPEN |
| Hardware walker A/D completion aggregate | `P1_AGGREGATED`, opt-in | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/mmu-ad-complete-20260902 make mmu-hardware-walker-ad-complete-gate` | `build/soc_test/mmu_hardware_walker_ad_complete/mmu_hardware_walker_ad_completion_report.md` plus the three child `sim.log` files | Aggregates successful A/D writeback, reset-in-flight recovery, and AXI `SLVERR` propagation. The aggregate requires the positive and negative markers and preserves the documented OS ownership/physical timing boundary |

| P1 RTL/simulation extension fresh recheck | `CONTRACT_CLOSED`, current RTL scope | `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/p1-current-recheck-20260902 make p1-current-complete` | `/tmp/p1-current-recheck-20260902/p1_completion_report.md` and child logs | Frontend, coherency stress, seven hardware-walker page-size configurations, CPU/MMU/ASID/shootdown, ISA R2, vectored interrupt and DDR4 closure gates all pass under the protected EDA budget. This revalidates the current P1 RTL/simulation bundle only; full MESI/directory, complete ISA/FPU, Linux/OS boot and product signoff remain OPEN |

| RTL Linux `r4k_wait` return/idle boundary | `BLOCK_VERIFIED`, diagnostic only | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=14000000 LINUX_PC_TRACE=1 LINUX_PC_TRACE_RETIRE_ONLY=1 LINUX_PC_TRACE_START=88a55b7c LINUX_PC_TRACE_END=88a55ba8 LINUX_PC_TRACE_CYCLE_START=11500000 LINUX_PC_TRACE_CYCLE_END=14000000` | `build/linux_boot/rtl_progress_gate/completion_report.md`, `sim/sim.log` | Retire-only records show `lw $ra,20($sp)` at `0x88a55b9c` and `jr $ra` at `0x88a55ba0` complete normally after `EI/EHB` and `__r4k_wait`; a later invocation re-enters `r4k_wait`, and sparse progress remains at `0x88a55b9c` through 14M cycles. The run finishes with VCS data structure size about 1.1 MB and no OOM. This identifies legal repeated idle/interrupt handling rather than a proven load or return-path defect; RTL Linux userspace and full RTL/QEMU differential remain OPEN |

| RTL Linux `kernel_init_freeable` tail boundary | `BLOCK_VERIFIED`, diagnostic only | `LINUX_PC_TRACE=1 LINUX_PC_TRACE_RETIRE_ONLY=1 LINUX_PC_TRACE_START=88ced100 LINUX_PC_TRACE_END=88ced188 RTL_CYCLE_LIMIT=40000000` using the QEMU-passing kernel image | `/tmp/rtl-linux-freeable-tail-20260902.log` | No retire record reaches the function tail during the 40M-cycle run, which completes normally. Alongside positive `wait_for_initramfs`, `driver_init`, and `devtmpfsd` traces, this narrows the active range to later `kernel_init_freeable` initcall/async work; it does not prove a CPU defect or close RTL Linux userspace/full differential |

| RTL Linux `devtmpfsd` execution | `BLOCK_VERIFIED`, diagnostic only | `LINUX_PC_TRACE=1 LINUX_PC_TRACE_RETIRE_ONLY=1 LINUX_PC_TRACE_START=88a56c40 LINUX_PC_TRACE_END=88a57040 RTL_CYCLE_LIMIT=20000000` using the QEMU-passing kernel image | `/tmp/rtl-linux-devtmpfs-20260902.log` | Trace cap reached 1024 records, with body retire records around cycles `8451674` and `9966955` from separate task contexts. This disproves the narrow no-devtmpfsd-scheduling hypothesis; `run_init_process` was not observed in a separate time-limited probe, so init-task/exec handoff and RTL Linux userspace remain OPEN |

| RTL Linux `driver_init` return boundary | `BLOCK_VERIFIED`, diagnostic only | `LINUX_PC_TRACE=1 LINUX_PC_TRACE_RETIRE_ONLY=1 LINUX_PC_TRACE_START=88d10810 LINUX_PC_TRACE_END=88d11010 RTL_CYCLE_LIMIT=20000000` using the QEMU-passing kernel image | `/tmp/rtl-linux-driver-20260902.log` | Retire-only trace observes `driver_init` entry at cycle `11289761`, normal internal initcall execution, and return through `0x88d1082c`; the 20M-cycle run completes without a userspace marker. This rules out a narrow driver-init-local stop, while init-task/scheduler handoff, RTL Linux userspace, and full RTL/QEMU differential remain OPEN |

| RTL Linux diagnostic trace defaults | `IMPLEMENTED`, resource safety | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=100000 HOST_TIMEOUT=180s RUN_DIR=/tmp/rtl-linux-default-trace-20260902 make rtl-linux-progress-gate`; explicit `LINUX_REFILL_TRACE=1`/`LINUX_EXCEPTION_TRACE=1` remains available | `/tmp/rtl-linux-default-trace-20260902/sim/sim.log` (4.9 KiB, 100K-cycle completion) and `/tmp/rtl-linux-async2-20260902.log` (1.6 KiB, 20M-cycle explicit-disable completion) | High-frequency refill and exception diagnostics now default off consistently in `Makefile`, `run_rtl_linux_progress_gate.sh`, and `tb_mips_soc.v`. The default smoke contains no actual refill/exception records; this prevents accidental direct-sim or runner use from producing unbounded per-cycle output. It is resource containment only and does not close RTL Linux userspace or full differential |

| QEMU Linux userspace gate fresh recheck | `SYSTEM_INTEGRATED`, opt-in | `QEMU_TIMEOUT=30 make linux-boot-build-gate` | `build/linux_boot/real/completion_report.md`, `build/linux_boot/real/qemu_stdout.log`, `build/linux_boot/real/qemu_stderr.log` | Current `mips32-soc-ref` with single-threaded TCG passes `/init`, boot, mmap/mprotect, protected-write SIGSEGV, brk, nanosleep, sched_yield, two exec children, two wait4 reaps and wait-status validation. This closes the fresh QEMU generic Linux userspace evidence; RTL Linux userspace, full RTL/QEMU system differential and complete ISA/MMU/FPU/OS VM remain OPEN |

| RTL Linux `of_core_init` focused follow-up | `BLOCK_VERIFIED`, diagnostic only | Existing compiled image replay with `LINUX_PC_TRACE` window `0x88a1b298..0x88a1bd00`, cycle window `11672880..12000000`, retire-only and bounded record count | `/tmp/mips32-linux-of-core-init-20260902/sim/focused_kobject.log` and focused attach trace | The final observed OF node `0x88db5174` has valid parent/name pointers and reaches kset insertion plus `sysfs_create_dir_ns()`/`kernfs_create_dir_ns()`; no traversal cycle, invalid pointer, or focused raw-spin-lock retry was observed. This narrows rather than fixes the Linux handoff blocker: sysfs/init-task scheduling completion, RTL userspace marker, and full RTL/QEMU differential remain OPEN |

| QEMU architecture closure aggregate fresh recheck | `SYSTEM_INTEGRATED`, opt-in | `SKIP_COVERAGE=1 QEMU_TIMEOUT=120 QEMU_BUILD_JOBS=2 BUILD_DIR=/tmp/qemu-architecture-reuse-20260902 make qemu-system-architecture-closure-gate` | `/tmp/qemu-architecture-reuse-20260902/isa_ref/qemu_system_architecture_closure/completion_report.md` and child reports | Current custom-machine aggregate passes current-contract, selected retire differential, MMU refill/PageMask/OS pressure, FPU exception/rounding boundaries, LL/SC and QEMU Linux userspace marker. The aggregate Linux child reuses the verified kernel/DTB to avoid an unrelated rebuild timeout. Full RTL Linux system-mode differential, complete ISA/privileged/MMU/IEEE-754/ABI, unrestricted Linux VM, physical timing and ASIC signoff remain open |
| QEMU selected differential full recheck | `SYSTEM_DIFFERENTIAL`, opt-in | `SKIP_COVERAGE=1 QEMU_TIMEOUT=180 QEMU_BUILD_JOBS=2 BUILD_DIR=/tmp/qemu-selected-full-20260902 make qemu-system-selected-differential-gate` | `/tmp/qemu-selected-full-20260902/isa_ref/qemu_system_selected_differential/completion_report.md` and child logs | All selected stages pass: ISA audit/R2, MDU, branch-likely, exceptions, break/traps, DI/EI/WAIT, BD exception, unaligned, peripheral/VIC, FPU single/double/rounding/CU1, DMA SG/fault/reset, VIC full sources and MMU IPI. This is selected bounded bare-metal differential evidence; complete ISA/MMU/Linux/OS and physical signoff remain open |

| QEMU current-contract aggregate fresh recheck | `SYSTEM_INTEGRATED`, opt-in | `QEMU_TIMEOUT=120 QEMU_BUILD_JOBS=2 make qemu-system-current-contract-gate` | `build/isa_ref/qemu_system_current_contract/` and child peripheral/DMA/QSPI/DDR/retire-capture reports | Current `mips32-soc-ref` custom machine passes the vendor-neutral peripheral contract, DMA v2 model, QSPI, DDR and bounded retire-capture aggregate. Physical PHY/JEDEC/device timing, Linux drivers, full RTL Linux boot and full-length RTL/QEMU differential remain open |

| MMU OS pressure aggregate fresh recheck | `SYSTEM_DIFFERENTIAL`, opt-in | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M QEMU_TIMEOUT=120 QEMU_BUILD_JOBS=2 SKIP_COVERAGE=1 make mmu-os-pressure-complete-gate` | `build/soc_test/product_mmu_process_pressure/sim.log`, `build/unit_tb/mmu_ipi_shootdown_pressure/`, `build/isa_ref/qemu_system_mmu_{refill_differential,pagemask_differential,contract,process_pressure}/`, `build/soc_test/mmu_os_pressure_complete/mmu_os_pressure_completion_report.md` | Aggregate history previously passed the 4x4 bounded corpus (`refills=16`); the current 8x8 deterministic TLBWI corpus is recorded in the dedicated evidence row above. This remains bounded single-core OS-style evidence; Linux page-table ownership, unrestricted demand paging, multicore Linux shootdown, and full MMU/privileged compliance remain open |

| RTL Linux PC trace argument propagation | `IMPLEMENTED`, diagnostic infrastructure | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=100000 LINUX_PC_TRACE=1 LINUX_PC_TRACE_SYMBOL=kernel_init LINUX_PC_TRACE_RETIRE_ONLY=1 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/mips32-linux-pc-trace-smoke-20260901/sim/sim_runtime.log`, `/tmp/mips32-linux-pc-trace-smoke-20260901/completion_report.md` | Removed the duplicate environment assignment that discarded the first `LINUX_EXTRA_SIM_ARGS` value. The smoke command line contains the requested trace flags and the ELF-resolved `0x88a55e10..0x88a56210` window; the bounded probe passes. This improves diagnostic reproducibility only and does not close Linux userspace boot or RTL/QEMU Linux differential |

| RTL Linux kthreadd retire-only trace | `BLOCK_VERIFIED`, diagnostic only | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=15000000 LINUX_PC_TRACE=1 LINUX_PC_TRACE_RETIRE_ONLY=1 LINUX_PC_TRACE_SYMBOL=kthreadd LINUX_PC_TRACE_LIMIT=512 make rtl-linux-progress-gate` | `/tmp/mips32-rtl-linux-kthreadd-retire/completion_report.md`, `sim/sim.log` | The default-off retire-only PC trace records only `wb_arch_valid` within its symbol window. A fresh probe enters `kthreadd` at cycle `8453329`, retires the `schedule()` call at `0x88057b70` around `10051007`, then returns to the manager loop. This disproves the narrow no-kthreadd/no-schedule hypothesis, but does not demonstrate kernel-init completion, `run_init_process`, userspace boot, or full RTL/QEMU Linux differential |
| RTL Linux wait-context diagnostic fields | `IMPLEMENTED`, opt-in | `LINUX_PC_TRACE=1 LINUX_PC_TRACE_RETIRE_ONLY=1 ... tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/mips32-rtl-linux-wait-need-resched/direct_wait3.stdout`; RTL compile and bounded run | Existing Linux PC retire records retain `sp/ra/a0/a1/v0` and now also expose `gp/t0`, allowing direct inspection of `gp+4` while executing `__r4k_wait` and separating `TIF_NEED_RESCHED` state from pipeline non-retirement. This is diagnostic infrastructure only; no scheduler or RTL semantic change is claimed |
| RTL Linux kthread indirect-entry retire fields | `IMPLEMENTED`, diagnostic only | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=16000000 LINUX_PC_TRACE=1 LINUX_PC_TRACE_RETIRE_ONLY=1 LINUX_PC_TRACE_START=0x880576f0 LINUX_PC_TRACE_END=0x88057810 LINUX_PC_TRACE_LIMIT=512 ... tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl-linux-kthread-20260902/sim/sim.log`, `/tmp/rtl-linux-kinit-entry-20260902.log`, `/tmp/rtl-linux-freeable-20260902.log` | The bounded retire record now exposes `s0/s3/s4`. A fresh 16M-cycle run shows `kernel_init` entering `kernel_init_freeable`, `kthread` invoking legal `worker_thread`/`rescuer_thread`/`kthread_worker_fn` targets, and no `devtmpfsd` entry before the idle boundary. A focused `kernel_init_freeable` run reaches `driver_init` (`0x88ced000`); no speculative CPU change is justified. RTL userspace and full RTL/QEMU Linux differential remain OPEN |
| RTL Linux PC retire cycle-window diagnostic | `IMPLEMENTED`, opt-in | `SKIP_COVERAGE=1 ... LINUX_PC_TRACE=1 LINUX_PC_TRACE_RETIRE_ONLY=1 LINUX_PC_TRACE_CYCLE_START=90000 LINUX_PC_TRACE_CYCLE_END=95000 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl-pc-cycle-filter-smoke3/completion_report.md`, `sim/sim.log`, `build/unit_tb/rtl_frontend_compile/` | PC-window retire tracing now accepts an independent cycle interval while retaining `sp/ra/a0`, CP0, memory and WB fields. A 100k-cycle smoke probe passed and recorded the configured `90000..95000` interval. This is bounded diagnostic infrastructure only; it does not change scheduler behavior or close RTL Linux userspace boot |

| Verification foundation current recheck | `FOUNDATION_READY_WITH_EXPLICIT_TOOL_STATUS` | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M make verification-foundation-gate` | `build/verification_foundation/verification_foundation_report.md`, `tool_inventory.tsv`, `formal_static_audit.log`, `formal_bind_compile.log` | Static formal audit reports 4 property modules and 10 assertions; VCS compiles binds for D-cache, TLB, VIC, and fabric. Waiver audit checked 2 files. Tool inventory reports only VCS available; formal proof and CDC/RDC/lint signoff remain explicitly unavailable. |

| QEMU timer current-value model alignment | `SYSTEM_DIFFERENTIAL`, opt-in | `make qemu-system-gpio-input-gate`; `SKIP_COVERAGE=1 make qemu-system-peripheral-differential-gate` | `build/isa_ref/qemu_system_gpio_input/qemu_stdout.log`, `build/isa_ref/qemu_system_peripheral_differential/completion_report.md`, `qemu/trace_compare.log` | `mips32-soc-ref` now models writable `TMR_VAL`, LOAD-to-current initialization, stopped-value retention, periodic reload, sticky source-2 interrupt and W1C. Firmware checks disabled-state write/readback; fresh QEMU-only and real RTL/QEMU retire differential gates pass. Linux timer driver and physical clock timing remain outside this slice. |

| L1 nonblocking SYNC gate interface repair | `SOC_INTEGRATED`, opt-in | `make l1-nonblocking-sync-gate` | `build/unit_tb/cache_concurrency/l1nb_sync/compile.log`, `sim.log` | Added explicit coherency sideband tie-offs/observation wires to the standalone unit test after current-DUT VCS compilation reported four undeclared wildcard-bound ports. Fresh rerun passes `REGRESSION_TEST_SUCCESS l1nb_sync`; no L1 or default blocking behavior changed. |

| PageMask-aware page-scope TLB invalidate | `BLOCK_VERIFIED`, opt-in | `make tlb-invalidate-gate` | `build/unit_tb/tlb_invalidate/compile.log`, `sim.log` | Page-scope invalidation now compares VPN2 with the stored entry PageMask, so an interior VPN2 invalidates a 16-KiB mapping; the directed test populates both I/D micro-TLBs and verifies stale translations are removed. Wired-floor, ASID/Global matching and default MMU-off behavior remain unchanged; full OS shootdown ownership remains open |

| ID delay-slot provenance guard | `BLOCK_VERIFIED` | `SKIP_COVERAGE=1 BUILD_DIR=/tmp/mips32-delay-pc-fix make cpu-irq-mem-pending-gate cpu-irq-delay-slot-gate cpu-cp0-gate rtl-frontend-compile`; 15M-cycle RTL Linux probe | `/tmp/mips32-linux-delay-pc-fix-15m/completion_report.md`, `/tmp/mips32-linux-delay-pc-fix-15m/sim/sim.log` | ID `Cause.BD/EPC` recovery now requires the delay-slot marker's paired branch PC to be adjacent to the current ID PC. The previous `0x88a3df5c` AdEL boundary is absent through 15M cycles, but userspace remains unbooted and full RTL/QEMU differential is open |

| EDA default memory budget | `IMPLEMENTED` | `VCS_JOBS=1 SKIP_COVERAGE=1 make perf-counters-gate perf-cpu-gate perf-workloads-gate` | `build/unit_tb/perf_counters/sim.log`, `build/soc_test/perf_cpu/sim.log`, `build/soc_test/perf_workloads/sim.log` | `scripts/run_eda_cgroup.sh` defaults to `MemoryMax=1500M` and `MemorySwapMax=512M`; all three current performance gates pass under the protected runner. `EDA_MEMORY_MAX`/`EDA_SWAP_MAX` remain explicit overrides. This is host-resource containment only, not architectural or coverage signoff. |

| QEMU architecture aggregate current recheck | `SYSTEM_INTEGRATED`, opt-in | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M QEMU_TIMEOUT=300s make qemu-system-architecture-closure-gate` | `build/isa_ref/qemu_system_architecture_closure/completion_report.md`, child logs, `build/linux_boot/real/completion_report.md` | Current-head aggregate passes current peripheral contract, selected ISA/MDU/FPU/privileged/peripheral retire differential, FPU precise-exception boundary, MMU refill/PageMask/OS-pressure, LL/SC and generic Linux kernel-to-userspace marker boot. The Linux guest reached both wait4 success markers after the intentional SIGSEGV child. Full RTL Linux differential, complete ISA/IEEE-754/OS VM, physical memory timing and formal/CDC/RDC/lint signoff remain open |
| RTL Linux long-budget userspace recheck | `INCOMPLETE`, opt-in | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=80000000 make rtl-linux-userspace-gate` | `/tmp/mips32-rtl-linux-userspace-80m/completion_report.md`, `sim/sim.log` | Same current QEMU-passing kernel makes no `run_init_process` entry or `/init` marker through 80M RTL cycles. It receives periodic CP0 timer IRQs from `r4k_wait`, so this disproves the prior short-cycle-budget explanation and narrows the open failure to kernel-init/kthread completion or scheduler handoff. No APB timer or speculative CPU semantic change is justified |
| Bounded RTL/QEMU Linux retire differential fresh recheck | `SYSTEM_DIFFERENTIAL`, opt-in | `SKIP_COVERAGE=1 KERNEL=build/linux_boot/real/kernel/vmlinux DTB=build/linux_boot/real/mips32_soc_ref.dtb RTL_CYCLE_LIMIT=100000 HOST_TIMEOUT=180s QEMU_TIMEOUT=10s BUILD_DIR=/tmp/linux-diff-strict-20260902 make qemu-system-linux-differential-gate` | `/tmp/linux-diff-strict-20260902/isa_ref/qemu_linux_differential/completion_report.md`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl`, `qemu/trace_compare.log` | Exact handoff at `0x88a55c78`; strict comparator passes the bounded aligned kernel retire prefix after removing the QEMU-only `linux-guest=on` SC compatibility override. This remains a bounded prefix and does not close RTL Linux userspace boot, full-length system differential, or full ISA/privileged/MMU signoff |

| Formal property asset content audit | `IMPLEMENTED` | `make formal-static-audit formal-bind-compile-gate verification-foundation-gate` | `build/verification_foundation/formal_static_audit.log`, `formal_bind_compile_report.md`, target `compile.log` files, `tool_inventory.tsv`, `verification_foundation_report.md` | Four formal `.sva` modules and ten assertions are structurally audited; target-selected real `dcache`, main-TLB, `apb_vic` and `soc_fabric` bindings compile under VCS with a reproducible `4/4` gate; broad selectors and permanently disabled assertions are rejected. The environment reports VCS available and formal/CDC/RDC/lint tools missing, so this is a solver-independent asset audit and not formal or commercial signoff |

| LWL/LWR/SWL/SWR cross-page precise fault and merge | `SOC_INTEGRATED`, opt-in | `BUILD_DIR=/tmp/mmu-cross-page-diff3 make qemu-system-mmu-refill-differential-gate` | `/tmp/mmu-cross-page-diff3/isa_ref/qemu_system_mmu_refill_differential/completion_report.md`, `qemu/trace_compare.log`, `rtl/soc/sim.log` | The MMU firmware keeps the odd 4-KiB page resident, invalidates the pair, then executes load and store pairs across `0x00020ffd`. RTL reports load `cross_value=0x88112233`, store `page0=0xB2C3D444`, store `page1=0xA5A500A1`, one precise fault for each pair, `demand_faults=10`, `page_allocs=4`, `unexpected_exc=0`, and `ok=1`; QEMU system retire differential passes. This closes precise cross-page merge sequencing only; unrestricted demand paging, Linux ABI and full ISA remain open |

| Context root change invalidates dynamic translations | `BLOCK_VERIFIED`, opt-in | `tb/unit/cp0/run.sh`; `make rtl-frontend-compile` | `build/unit/cp0/sim.log`, `build/unit_tb/rtl_frontend_compile/` | A `Context.PTEBase` write now flushes both micro-TLBs and all non-wired main-TLB entries while preserving entries below the programmed Wired floor. The CP0 test observes a dynamic mapping hit before the write and a miss after it. This closes root-switch invalidation only; OS PTE population, A/D management, demand paging, multicore shootdown, and Linux differential remain open |

| CP0 Context.PTEBase to hardware-walker root | `BLOCK_VERIFIED`, opt-in | `tb/unit/cp0/run.sh`; `make rtl-frontend-compile cpu-mmu-complete` | `build/unit/cp0/sim.log`, `build/unit_tb/rtl_frontend_compile/`, `build/cpu_mmu_complete/cpu_mmu_completion_report.md` | CP0 now exports the software-written `Context.PTEBase` as a 512 MiB-aligned walker root when the top-level walker parameter is zero; a non-zero integration parameter remains authoritative. Existing fixed-root walker, default MMU-off, and CPU/MMU regression paths pass. This closes root ownership plumbing only; Linux OS page-table population, A/D updates, demand paging, shootdown integration, and full RTL/QEMU Linux differential remain open |

| Scheduler context PTEBase isolation | `BLOCK_VERIFIED`, opt-in | `RUN_DIR=build/unit_tb/cpu_scheduler_integration_ptebase tb/unit/cpu_test/run_cpu_scheduler_integration.sh`; `make rtl-frontend-compile` | `build/unit_tb/cpu_scheduler_integration_ptebase/sim.log`, `build/unit_tb/rtl_frontend_compile/` | Each scheduler task now stores and restores its CP0 `Context.PTEBase` alongside ASID. The integration test restores task 1 with root `0x40000000`, verifies the valid handshake and CP0 field, and root changes use the existing dynamic TLB/micro-TLB invalidation path. OS page-table management, demand paging, multicore shootdown, Linux userspace boot, and full RTL/QEMU differential remain open |

| R-type trap code field and Linux BUG_ON decode | `SYSTEM_DIFFERENTIAL`, opt-in | `make qemu-system-trap-differential-gate`; `SKIP_COVERAGE=1 KERNEL=/tmp/mips32_linux_kernel_irqfix/kernel/vmlinux RTL_CYCLE_LIMIT=2000000 make rtl-linux-progress-gate` | `/tmp/mips32-trap-fix/isa_ref/qemu_system_trap_differential/`, `/tmp/mips32-linux-trap-fix/linux_boot/rtl_progress_gate/completion_report.md` | Fixed the decoder's incorrect `rd/sa == 0` requirement for R-type TGE/TGEU/TLT/TLTU/TEQ/TNE: `rd:sa` is the ten-bit trap code. The differential trap corpus now includes `TNE code=0xc`; the Linux probe no longer raises the former RI at `__BUG_ON` (`0x880e89a4`) and reaches normal kernel addresses before the bounded cycle limit. Userspace boot and full Linux differential remain open |

| RTL Linux interrupted-MEM state diagnosis | `BLOCK_VERIFIED`, diagnostic infrastructure | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=... RTL_CYCLE_LIMIT=13600000 LINUX_EXCEPTION_TRACE=1 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/mips32-linux-exception-trace/completion_report.md`, `/tmp/mips32-linux-exception-trace/sim.log` | The bounded trace records WB/MEM/EX validity and instruction identity at the first Linux failure boundary, proving the interrupted `lw` was still in MEM with no WB retirement. It narrows the open bug to interrupted-MEM recovery/state preservation; it does not close Linux userspace or full differential |

| RTL Linux asynchronous-interrupt EPC precision | `BLOCK_VERIFIED`, diagnostic follow-up | `SKIP_COVERAGE=1 BUILD_DIR=/tmp/mips32-epc-fix2 make rtl-frontend-compile cpu-cp0-gate cpu-irq-delay-slot-gate`; `SKIP_LINUX_BUILD=1 KERNEL=... RTL_CYCLE_LIMIT=16000000 LINUX_REQUIRE_PROGRESS=0 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/mips32-linux-epcfix/completion_report.md`, `/tmp/mips32-linux-epcfix/sim/sim.log` | Delay-slot recovery now requires control-transfer opcode evidence and an ordinary WB instruction retires before an asynchronous-interrupt EPC resumes. Frontend 8/8 and targeted CPU gates pass; the 16M RTL Linux probe still panics at the later `BadVAddr=0x2c` boundary, so this does not close Linux userspace or full system differential |

## 2026-08-30 fresh Linux/QEMU build and system peripheral recheck

`make linux-boot-build-gate` rebuilt the QEMU 9.2.0 `mipsel-softmmu`
`mips32-soc-ref` binary and the Linux v6.6 kernel successfully after removing
only failed kernel objects and source download caches. Fresh
`make qemu-system-peripheral-contract-gate qemu-system-qspi-gate
qemu-system-ddr-gate` and `make qemu-system-current-contract-gate` also pass.
The QEMU evidence covers the behavioral GPIO/timer/DMA/PIC/QSPI/DDR contract,
not physical device timing, RTL Linux boot, or full RTL/QEMU differential.

The generic Linux runner now gives the single-vCPU QEMU guest a 120-second
default host budget. The gate still requires `/init`, VM/protection, timer,
fork/exec, exact-PID wait/reap, and exit-status markers; this change only
contains host scheduling variance and does not relax a functional check. A
fresh rerun remains nondeterministic in this environment: it can stop after
the protected-page SIGSEGV or during the later fork/wait path before all
markers. Generic Linux userspace therefore remains an open reproducibility
item despite the earlier standalone marker-complete run.

The runner also terminates QEMU immediately after the final fork/wait marker;
this avoids waiting for the intentionally exiting `init` to cause a kernel
panic and does not accept a partial marker set.

After clearing the accumulated historical `/tmp` gate artifacts, a fresh
`JOBS=2 QEMU_TIMEOUT=120s make linux-boot-build-gate` passed with the complete
marker set and exited through the new bounded runner shutdown path. Earlier
resource-loaded A/B failures remain recorded as residual reproducibility risk.

## 2026-08-30 EDA host-OOM containment

Kernel logs attribute the prior high-water event to a VCS compiler process
reaching about 2 GiB in an `eda-*` cgroup; separate historical global OOM
events killed concurrent Python processes. The long RTL Linux probe itself
was measured at about 160 MiB. The aggregate SoC, UVM, directed-regression,
SVA, and current-contract entry points now use
`scripts/run_eda_cgroup.sh` for VCS, `simv`, and URG, with `VCS_JOBS=1` by
default and explicit `EDA_MEMORY_MAX`/`EDA_SWAP_MAX` overrides. Isolated
no-coverage `soc-smoke` and all three SVA scenarios passed. This is resource
containment evidence only; standalone unit scripts and the open Linux/ISA/
MMU closure claims are unchanged.

## 2026-08-30 Linux guest LL/SC A/B conclusion

The attempted `linux-guest=on` machine-property isolation was reverted after
an A/B run with the same fresh kernel. Physical reservation identity stalled
Linux after the intentional SIGSEGV child fault, while the existing virtual
`LLAddr` behavior completed all boot, VM, fork/exec, and wait4 markers. The
custom machine therefore keeps the existing virtual-`LLAddr` contract for
both bare-metal and Linux guests; Linux userspace closure and full RTL/QEMU
system differential remain open.

The fresh-image rerun also isolated a separate host execution issue: default
multi-threaded TCG can leave the single-vCPU guest in a `wait4` stall after a
SIGSEGV child exit, while `-accel tcg,thread=single` completes the same
markers. The Linux boot and bounded Linux differential runners now select
single-threaded TCG explicitly; this is a QEMU execution-mode containment for
the single-vCPU contract, not evidence of SMP or full Linux closure.

## 2026-08-30 Linux fresh-image reproducibility recheck

The rebuilt image under `/tmp/mips32-linux-deterministic-a.8XZ10T` still
reaches `/init` and the intentional read-only write fault, but the parent does
not advance to the next marker before the QEMU timeout. Adding a bounded
`wait4(WNOHANG)` poll did not change that boundary. The standalone image under
`/tmp/linux_gate_pid_repro` previously passed the generic userspace gate, so
the current result is retained as an open custom-machine SIGSEGV/child-exit
reproducibility issue rather than being treated as a Linux gate pass.

## 2026-08-30 generic Linux wait4 PID verification

`RUN_DIR=/tmp/linux_gate_pid_repro QEMU_TIMEOUT=60s JOBS=2
tb/linux_boot/run_linux_boot_gate.sh` completed the rebuilt Linux v6.6 kernel
and initramfs gate. QEMU reached `/init`, all VM, permission, heap, timer,
yield, fork and both exec markers, then reaped both children with the exact
PIDs returned by `fork` and verified normal exit status 0. This converts the
previous wait4 timeout diagnosis into fresh userspace boot evidence; it does
not close RTL Linux userspace boot or full RTL/QEMU Linux differential.

## 2026-08-29 stale delay-slot metadata recovery guard

`mips_cpu` now requires a valid paired delay-slot resume target when deriving
asynchronous interrupt `Cause.BD`/`EPC` from MEM, EX, or ID. This prevents stale
post-flush `id_bd` from rewinding an ordinary Linux instruction to an EPC that
skips its producer, while preserving the contiguous legal delay-slot path.
The targeted `cpu-irq-delay-slot-gate`, `cpu-cp0-gate`, and 8-configuration
`rtl-frontend-compile` run passed under
`/tmp/mips32-delay-metadata-fix`. This is a targeted RTL recovery fix; RTL
Linux userspace boot and full RTL/QEMU Linux differential remain open.

A fresh no-coverage 20M-cycle RTL Linux probe at
`/tmp/rtl_linux_after_delay_metadata_fix` progressed through the dynamic TLB
boundary and timed out near 17.4M cycles without a userspace marker. This is
not sufficient evidence for Linux boot closure.

## 2026-08-29 Linux progress Make entry maintenance

## 2026-08-29 current-contract build-root isolation

The current-contract Make entry now propagates an explicit external build root
through all Phase 2/3 wrappers and the IRQ delay-slot prerequisite. The default
path remains repository-local, while long runs can use an isolated scratch
directory without writing generated VCS output into `build/`. A fresh isolated
run passed all functional prerequisites and 10/10 stress seeds; it correctly
stopped at the existing 99% coverage policy (UVM 36.24%, Product 36.97%). This
is resource/reproducibility evidence and does not claim coverage closure.

The `rtl-linux-progress-gate` Make target now forwards the supplied kernel,
build-skip, cycle/timeout, and bounded trace controls to
`run_rtl_linux_progress_gate.sh`. A fresh 2M-cycle no-coverage run through the
Make entry passed with an existing kernel and zero userspace markers. This is
resource/reproducibility evidence only; RTL Linux userspace boot and full
RTL/QEMU Linux differential remain open.

The entry also forwards `SKIP_COVERAGE` and
`SKIP_URG_EXCLUSION_CHECK`; a fresh no-coverage run completed in 24.2 s with
1.1 MiB simulator data structure size and a normal bounded exit.
A follow-up 10M-cycle no-coverage run completed in 105.7 s with the same
bounded memory profile and continuous CPU progress, but still no userspace
marker.
The bounded delay diagnostic was also corrected to initialize its sample
counter and observe the active IF-PC/live registers. A 30M-cycle capture
shows the late loop is Linux `panic()`'s `__udelay` with monotonically
decreasing `v0`, not a Count or TLB deadlock; the panic-entry reason remains
open.

## 2026-08-27 maintenance

The SPECIAL3 decoder regression fixture now uses the architectural MIPS32 R2
`RDHWR` encoding (`rs=0`) and explicitly covers legal `WSBW` (`sa=6`). Fresh
`mips-control-special3-gate`, `cp0-rdhwr-gate`, and `isa-r2-gate` runs pass.
This confirms the selected implemented-subset evidence; it does not upgrade
the project claim to full ISA or privileged-ISA compliance.

Status levels used by this registry:

- `IMPLEMENTED`: RTL and interface contract are present.
- `BLOCK_VERIFIED`: block directed, negative, reset, and error tests pass.
- `SOC_INTEGRATED`: SoC wiring and firmware/SoC simulation pass.
- `CONTRACT_CLOSED`: the current RTL/simulation contract, including required
  backpressure and error evidence, is closed. This is not a product or silicon
  signoff.

## Current Registry

| Block | Status | Primary evidence | Log location | Residual risk / boundary |
|---|---|---|---|---|
| Bounded RTL/QEMU Linux retire differential | `SYSTEM_DIFFERENTIAL`, opt-in | `make qemu-system-linux-differential-gate` with `KERNEL` and `DTB` | `build/isa_ref/qemu_linux_differential/completion_report.md`, `rtl_gate.log`, `qemu/trace_compare.log` | Same relocated Linux kernel/DTB compares one retire at a time after an exact Boot ROM-to-kernel PC/instruction anchor; fresh isolated 2M-cycle RTL run passes the complete QEMU capture as a golden prefix (`/tmp/mips32-qemu-dtb-fix-2m`, 2M RTL limit, 10s QEMU capture). This is not Linux userspace boot, full-length Linux differential, complete ISA/privileged/MMU signoff, or physical product signoff |
| RTL Linux dynamic TLB fault boundary | `BLOCK_VERIFIED`, diagnostic only | `SKIP_COVERAGE=1 KERNEL=... RTL_CYCLE_LIMIT=20000000 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl_linux_20m_current/completion_report.md`, `sim/sim.log` | The probe reaches a real Linux `TLBS` at `0x8890cd90` for `0xc0000000`, then observes the generated handler write `VPN2=0x00060000` with a valid `EntryLo0` and continued execution. This does not close unrestricted demand paging, Linux VM ownership, userspace boot, or full RTL/QEMU differential |
| QSPI command/FIFO | `CONTRACT_CLOSED` | `make qspi-cmd-behavioral-gate` | `build/unit_tb/qspi_cmd_behavioral/sim.log` | Vendor-specific command timing and device modes are out of scope |
| QEMU system QSPI command model | `SOC_INTEGRATED` | `make qemu-system-qspi-gate` | `build/isa_ref/qemu_system_qspi/completion_report.md`, `qemu_stdout.log` | Image-backed transaction model covers APB command/FIFO, x1/quad reads, TX/RX, IRQ/DONE W1C, abort and timeout; no pin timing, PHY, JEDEC device, erase/program or RTL retire differential claim |
| QSPI canonical error taxonomy | `CONTRACT_CLOSED` | `make qspi-error-taxonomy-gate qspi-retry-policy-gate` | `build/unit_tb/qspi_error_taxonomy/sim.log`, `build/unit_tb/qspi_retry_policy/sim.log` | Secure-boot authentication and production policy are out of scope |
| QSPI APB/shared-pin SoC path | `SOC_INTEGRATED` | `make qspi-status-integration-gate qspi-soc-quad-gate` | `build/unit_tb/qspi_status_integration/sim.log`, `build/unit_tb/qspi_soc_quad/sim.log` | Device-specific command modes and production software policy are outside this RTL contract |
| QSPI x1/quad AXI XIP | `CONTRACT_CLOSED` | `make qspi-axi-xip-gate qspi-axi-xip-quad-gate` | `build/unit_tb/qspi_axi_xip/sim.log`, `build/unit_tb/qspi_axi_xip_quad/sim.log` | Device-specific command modes and erase/program policy are outside this RTL contract |
| QSPI development boot handoff | `SOC_INTEGRATED` | `make product-manifest-handoff-gate product-manifest-handoff-quad-gate` | `build/unit_tb/product_manifest_handoff*/sim_valid.log`, `sim_bad_crc.log`, `sim_xip_timeout.log` | Quad behavioral endpoint no longer overwrites a testbench-loaded image during time-0 initialization; this is still a development manifest/CRC flow, not a signed production boot chain |
| QSPI vendor-neutral closure aggregate | `SOC_INTEGRATED` | `make qspi-vendor-neutral-complete-gate qspi-vendor-neutral-boot-gate` | `build/unit_tb/qspi_*`, `build/unit_tb/product_manifest_handoff*/` | Aggregates x1/quad command, status, timeout/retry, AXI/XIP, shared-pin, SoC pad/memory and development manifest handoff evidence; physical flash timing, erase/program, secure boot and production boot remain open |
| DDR4 protocol controller | `CONTRACT_CLOSED` | `make ddr4-complete-gate ddr4-status-soc-gate` | `build/unit_tb/axi_ddr4_controller*/sim.log`, `build/soc_test/ddr4_status_gate/*/sim.log` | ECC policy and multi-rank behavior are outside this RTL contract |
| DDR4 contract entry audit | `IMPLEMENTED` | `make ddr-contract-entry-audit` | `build/unit_tb/ddr_contract_entry/ddr_contract_entry_report.md` | Audit intentionally reports `BLOCKED` until PHY/DFI, DRAM part, board timing, power-good and boot-budget inputs exist; it is not product DDR signoff |
| DDR4 ECC SECDED | `CONTRACT_CLOSED` | `make ecc-secded-gate` | `build/unit_tb/ecc_secded_32/sim.log` | Single-bit correction and double-bit detection are verified; silicon ECC policy, multi-rank behavior and physical memory signoff remain open |
| QEMU system DDR behavioral window | `SOC_INTEGRATED` | `make qemu-system-ddr-gate` | `build/isa_ref/qemu_system_ddr/completion_report.md`, `qemu_stdout.log` | QEMU covers status/version/error/W1C and cached/uncached access to the behavioral 128 MiB window; PHY training, JEDEC timing, refresh scheduling, ECC injection and board/device signoff remain open |
| QEMU system DDR fault/status model | `SOC_INTEGRATED`, opt-in | `make qemu-system-ddr-fault-gate` | `build/isa_ref/qemu_system_ddr_fault/completion_report.md`, `axi_error/qemu_stdout.log`, `geometry_error/qemu_stdout.log` | `ddr-fault-mode=1/2` exposes distinct AXI/geometry sticky error codes, status error bit and W1C recovery while the DDR window remains accessible. This is a reference status/fault model; PHY failure timing, real ECC injection, JEDEC behavior and board signoff remain open |
| QEMU system DMA AXI fault classification | `SOC_INTEGRATED` | `make qemu-system-dma-fault-gate` | `build/isa_ref/qemu_system_dma_fault/completion_report.md`, `read_error/qemu_stdout.log`, `write_error/qemu_stdout.log` | Opt-in `dma-fault-mode=1/2` forces read/write response failures and verifies distinct error codes, channel IRQ, PIC propagation and DONE/ERR W1C clearing. Physical DDR/AXI fault timing and production DMA signoff remain open |
| QEMU system DMA reset-in-flight | `SOC_INTEGRATED`, opt-in | `make qemu-system-dma-reset-inflight-gate` | `build/isa_ref/qemu_system_dma_reset_inflight/completion_report.md`, `qemu_stdout.log` | The reference machine requests one reset after the first legacy DMA start, before copying data, clears DMA busy/status/IRQ state in its reset callback, and the restarted guest completes the 256-byte transfer plus DONE/W1C. Physical DDR reset timing and arbitrary multi-channel reset interleavings remain open |
| QEMU/RTL DMA v2 SG data contract | `SOC_INTEGRATED`, opt-in | `make qemu-system-dma-sg-data-gate` | `build/isa_ref/qemu_system_dma_sg_data/completion_report.md`, `rtl_gate.log`, `qemu_stdout.log` | A bounded two-descriptor SG guest performs two real 16-byte moves and compares all eight destination words before the success mailbox on both RTL and `mips32-soc-ref`; full per-retire DMA differential, physical AXI fault/reset timing and Linux DMA ABI remain open |
| QEMU/RTL DMA v2 SG retire differential | `SYSTEM_DIFFERENTIAL`, opt-in | `make qemu-system-dma-sg-differential-gate` | `build/isa_ref/qemu_system_dma_sg_differential/qemu/trace_compare.log`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl` | `TRACE_COMPARE_PASS` compares the bounded two-descriptor guest one retire at a time after an explicit fixed delay that normalizes RTL bus completion versus the immediate reference-model copy; full DMA fault/reset timing and Linux DMA ABI remain open |
| QEMU system current contract aggregate | `CONTRACT_CLOSED` | `make qemu-system-current-contract-gate` | `build/isa_ref/qemu_system_current_contract/completion_report.md` | Aggregates passing QEMU peripheral, DMA v2, QSPI, DDR behavioral and retire-capture gates; UART/legacy-DMA/DMA-v2/PIC/QSPI/DDR APB windows use explicit high-priority MMIO endpoints backed by the same model state, the PIC window covers the complete `0x4000_4000..0x4000_423f` CSR span so flash XIP aliases cannot mask priority/VEC/ACK accesses, and v2 DONE+INT_EN completion raises deterministic PIC status; does not claim full RTL/QEMU differential, full ISA/MMU/Linux boot, or physical device signoff |
| QEMU selected system differential aggregate | `SYSTEM_DIFFERENTIAL` | `make qemu-system-selected-differential-gate` | `build/isa_ref/qemu_system_selected_differential/completion_report.md`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl`, `qemu/trace_compare.log` | Serial aggregate includes ISA R2, MDU, branch-likely, exceptions, break/traps, DI/EI/WAIT, BD/EPC, unaligned, peripheral/VIC including 32-source arbitration, opt-in FPU single/double/rounding/CU1, bounded DMA SG/fault/reset, VIC full sources, MMU IPI, and the opt-in L1/CPU-ROB/DDR/L2 nonblocking combined-path differential. This is selected bare-metal mailbox-boundary evidence, not full MIPS32/privileged/MMU/IEEE-754/Linux differential, full cache coherency or Linux cache ABI |
| MEM-stage load/store and merge reference | `BLOCK_VERIFIED` | `make mem-stage-gate` | `build/stage/mem/sim.log` | Standalone VCS/UVM scoreboard checks 2,009 randomized transactions, including byte/half/word, LL/SC, little-endian LWL/LWR/SWL/SWR and `translation_fault`/`mem_done` request suppression; `dmem_we` and `dmem_be` are checked independently and AdEL/AdES suppresses the request. Cross-page sequencing, Linux unaligned policy and full ISA remain open |
| QEMU system ISA R2 retire differential | `SYSTEM_DIFFERENTIAL`, opt-in | `make isa-r2-gate qemu-system-isa-r2-differential-gate` | `build/soc_test/isa_r2_sweep/sim.log`, `build/isa_ref/qemu_system_isa_r2_differential/completion_report.md`, `qemu/trace_compare.log` (`TRACE_COMPARE_PASS records=454`) | The real RTL CPU and patched QEMU 9.2 `mips32-soc-ref` compare the selected MIPS32 R2 corpus through mailbox retirement with FPU disabled, including SPECIAL3 `ALIGN` positions 0..3, `BITSWAP`, `WSBW`, `PREFX`, RDHWR, CP0 identity/cache geometry reads and control-flow barriers. The project patch maps legacy R2 encodings and treats no-FPU PREFX as an ordered integer no-op; other COP1X arithmetic and complete ISA/privileged/MMU differential remain open |
| RTL Linux long-probe resource safety | `BLOCK_VERIFIED` (diagnostic infrastructure) | `SKIP_COVERAGE=1 make soc-smoke SOC_TEST_RUN_DIR=build/soc_test/smoke_nocov`; `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/rtl_progress_gate/kernel/vmlinux RTL_CYCLE_LIMIT=15000000 RUN_DIR=build/linux_boot/rtl_progress_nocov_15m tb/linux_boot/run_rtl_linux_progress_gate.sh` | `build/soc_test/smoke_nocov/sim.log`, `build/linux_boot/rtl_progress_nocov_15m/completion_report.md` | Explicit no-coverage mode allows bounded Linux RTL probes without VCS/URG database growth while normal gates remain unchanged. The 15M-cycle probe passed post-reset progress but observed zero userspace success markers; RTL Linux userspace boot and RTL/QEMU Linux differential remain open |
| QEMU linux-user build | `CONTRACT_CLOSED` | `make qemu-linux-user` | `build/deps/src/qemu-9.2.0/build-mipsel-linux-user/qemu-mipsel`, build log | Official QEMU 9.2 `mipsel-linux-user` builds and reports version 9.2.0; the project patch keeps system-only SRS helpers out of linux-user. This closes the executable build prerequisite only, not Linux boot or ABI validation |
| Linux/U-Boot boot dependency audit | `IMPLEMENTED` | `make linux-boot-fetch-sources linux-boot-dependency-gate` | `tb/linux_boot/sources.lock`, `tb/linux_boot/fetch_sources.sh`, dependency-gate output | Official Linux v6.6 and U-Boot v2024.10 immutable commits are reproducibly fetched and marker-verified; cross compiler and both project-built QEMU binaries are present. Source provenance is closed, but Linux-specific platform support, DTB/U-Boot image flow and a real boot regression are still open; no Linux boot claim is made |
| QEMU system build reproducibility | `CONTRACT_CLOSED` | `scripts/qemu/build_mips32_soc_ref.sh` plus `make qemu-system-current-contract-gate` | `build/deps/src/qemu-9.2.0/build-mipsel-softmmu/qemu-system-mipsel`, `build/isa_ref/qemu_system_current_contract/qemu_build_identity.txt` | Project-input hash stamp prevents each aggregate child gate from reconfiguring/rebuilding the patched QEMU tree; source rebuild still occurs when the helper, custom machine, XML, or local QEMU patch changes |
| QEMU differential harness timeout contract | `IMPLEMENTED` | `tb/isa_ref/run_qemu_system_differential_gate.sh` | `build/isa_ref/qemu_system_selected_differential/` child logs | Default RTL gate timeout is 120 seconds and includes cold VCS compile/elaboration; this prevents a valid corpus from failing at 30 seconds before simulation, while callers can still override `RTL_TIMEOUT` |
| QEMU retire-capture timeout contract | `IMPLEMENTED` | `tb/isa_ref/run_qemu_system_retire_capture_gate.sh` | `build/isa_ref/qemu_system_*/qemu/` | Default QEMU process timeout is 30 seconds to include cold plugin/reference startup; mailbox completion remains the normal termination condition and `QEMU_TIMEOUT` is overridable |
| CPU/MMU current contract | `CONTRACT_CLOSED` | `make cpu-mmu-complete` | `build/cpu_mmu_complete/cpu_mmu_completion_report.md` | All 26 current CPU/MMU RTL gates pass, including scheduler integration and the 8x8 process-pressure extension; full Linux/OS boot, complete runtime page-table ownership, and production policy remain out of scope |
| ISA R2 SPECIAL3 ALIGN slice | `SYSTEM_DIFFERENTIAL` (bounded) | `make isa-r2-gate qemu-system-isa-r2-differential-gate` | `build/soc_test/isa_r2_sweep/sim.log`, `build/isa_ref/qemu_system_isa_r2_differential/qemu/trace_compare.log` | RTL and patched QEMU both execute all four `ALIGN` byte positions through the retire boundary. The QEMU capture now clears run-owned event/state artifacts before each invocation, preventing stale traces after timeout. Full ISA compliance remains open |
| CPU/MMU micro-TLB | `SOC_INTEGRATED` | `make micro-tlb-gate product-mmu-micro-tlb-gate tlb-invalidate-gate` plus `make rtl-frontend-compile` | `build/unit_tb/micro_tlb/compile.log`, `sim.log`, `build/unit_tb/tlb_invalidate_micro2/compile.log`, `sim.log`, `build/unit_tb/product_mmu_micro_tlb/sim.log`, `build/unit_tb/product_tlb_vectors_micro/sim.log`, `build/unit_tb/rtl_frontend_compile/micro_tlb/compile.log` | Opt-in `SOC_MICRO_TLB_ENABLE=1` is exercised through the real CPU/CP0/TLB/MMU path: product MMU boot observes D-side hits, product TLB vector firmware observes nested refill/general and BEV-to-EBase transitions plus I-side hits, and the invalidate unit proves I/D stale entries are flushed for page/ASID/all scopes and context restore. Blocking default remains unchanged; shootdown latency, performance, and long-running OS policy remain outside this slice. The SoC PTE-update/refill-handler cache interaction is still open. |
| Main-TLB duplicate-match Machine Check | `BLOCK_VERIFIED` | `make tlb-asid-policy-gate rtl-frontend-compile` | `build/unit/tlb_asid_policy/sim.log`, `build/unit_tb/rtl_frontend_compile/` | Main-TLB probe and both I/D lookup ports now flag every second matching valid entry, including byte-for-byte duplicate entries; the MMU reports fault type `110` and the TLBP sideband reports `probe_multi_hit`. This removes the previous priority-encoder ambiguity; recovery policy after MCheck remains CPU/OS-owned. |
| CPU/MMU SoC PageMask extension | `SOC_INTEGRATED` | `make product-mmu-pagemask-gate PRODUCT_MMU_PAGEMASK_DIR=build/soc_test/product_mmu_pagemask_current` | `build/soc_test/product_mmu_pagemask_current/sim.log` | Fresh real CPU/SoC firmware reaches `REGRESSION_TEST_SUCCESS product_mmu_pagemask refills=3` after 4KB/16KB/64KB/256KB demand-refill and data-access phases, covering distinct ASIDs, even/odd halves, non-zero offsets and PFN folding. Linux VM/page-table ownership and the hardware walker's explicit 4KB-only contract remain outside this slice |
| CPU/ISA integer trap instructions | `SYSTEM_DIFFERENTIAL` | `make qemu-system-trap-differential-gate`; `make qemu-system-trap-imm-differential-gate` | `build/isa_ref/qemu_system_trap_differential/`; `build/isa_ref/qemu_system_trap_imm_differential/` | Fresh RTL vs `mips32-soc-ref` retire-boundary comparison passes for all six register traps and six immediate traps, including signed/unsigned compare direction, precise ExcCode 13 and ERET recovery; this remains selected-corpus evidence, not full ISA compliance |
| CPU/MMU multi-segment runtime | `SOC_INTEGRATED` | `make product-kseg0-runtime-multi-gate` | `build/unit_tb/product_kseg0_runtime_multi/sim.log`, `sim_multi_wx.log` | No-preload MMU-enabled Boot ROM flow validates three RX/R/RW descriptors, rejects a valid-CRC W+X image before handoff, copies through uncached aliases, and executes copied text; hardware permission enforcement, PIC/GOT/TLS and production ELF loading remain outside this slice |
| CPU/MMU PIC/GOT-style runtime relocation | `SOC_INTEGRATED` | `make product-kseg0-runtime-multi-gate` | `build/unit_tb/product_kseg0_runtime_multi_picgot_two/sim.log`, `sim_multi_wx.log` | RW runtime data carries link-time pointers to both the R-only segment and RX function; stage-1 applies independent source/runtime deltas and invokes copied text through the relocated function pointer. This is a bounded relocation slice, not a complete ELF GOT/PLT or dynamic linker |
| CPU/MMU hardware walker permission integration | `SOC_INTEGRATED` | `make cpu-hardware-walker-gate` plus `make cpu-dside-hardware-walker-gate` plus `make page-table-walker-gate` plus `make page-table-tlb-refill-gate` plus `make mmu-refill-gate` | `build/unit_tb/cpu_dside_hardware_walker/sim.log`, `build/unit_tb/cpu_hardware_walker/sim.log`, `build/unit_tb/page_table_walker/sim.log`, `build/unit_tb/page_table_tlb_refill/sim.log`, `build/soc_test/mmu_refill_bootstrap/sim.log` | CPU opt-in walker covers I/D two-level refill, load/store retry, PA completion, leaf permission latch, TLBL/TLBS classification and request suppression; the bootstrap firmware adds bounded kseg0 software-page-table non-identity fault/refill/ERET evidence; full demand paging and OS page-table ownership remain outside this slice |
| CPU/MMU hardware walker SoC AXI integration | `SOC_INTEGRATED` | `make mmu-hardware-walker-soc-gate` | `build/soc_test/mmu_hardware_walker_soc/sim.log` | Real SoC core-0 D-side AXI bridge covers two-level root/L2 reads, both halves of one VPN2, hardware refill/retry, read-only permission fault and zero software demand faults; default walker remains opt-in and production page-table ownership/OS policy remain outside this slice |
| Hardware walker/TLB ready handshake | `RTL_UNIT` | `make page-table-tlb-refill-gate page-table-walker-gate cpu-hardware-walker-gate rtl-frontend-compile` | `build/unit_tb/page_table_tlb_refill/sim.log`, `build/unit_tb/page_table_walker/sim.log`, `build/unit_tb/cpu_hardware_walker_after_tlb_ready_fix/sim.log`, `build/unit_tb/rtl_frontend_compile/` | Successful walker responses are held as observable TLB writes even with a permanently-ready consumer; the default 4KB handshake behavior is preserved. Four page-size semantics are covered by the dedicated page-size gate. This is not full OS demand paging or Linux VM evidence. |
| Coverage manifest synchronization | `CONTRACT_CLOSED` | `python3 tb/coverage/audit_exclusions.py` | `tb/coverage/manifest.json`, `tb/coverage/exclusion_manifest.json`, `tb/coverage/{uvm,product}_exclusions.el` | Fresh VDB refinement emits the keyed audit manifest and detailed rule inventory; current audit passes with 432 synchronized entries. This does not close the 99% percentage threshold |
| Coverage URG metadata hygiene | `CONTRACT_CLOSED` | `make coverage-strict-clean-gate` | `build/coverage/strict_clean/{uvm,product}_urg.log`, `build/coverage/strict_clean/*_strict_exclusions/` | Fresh per-VDB `full_exclusions` are converted to strict-mode, per-metric `-elfilelist` inputs; UVM and product URG reports pass with zero metadata warnings. This closes exclusion metadata hygiene only; measured coverage remains below the 99% policy and is not claimed as percentage signoff. |
| CPU/CP0 TLS linker/runtime pointer | `SOC_INTEGRATED` | `make cp0-rdhwr-gate` plus Phase 3A `soc_base_test` with `cp0_sweep` | `build/soc_test/cp0_rdhwr/sim.log`, `build/uvm/cp0_fix/vcs_uvm.log`, `tb/soc_test/fw/tests/cp0_sweep/link_tls.ld` | Dedicated linker layout exports `.tdata/.tbss` symbols; UserLocal/RDHWR $29 is used as a real user-mode TLS base for initialized/zeroed slot read/write, with HWREna standard targets restored before the positive user-mode sweep; TLS relocation model and multi-thread scheduler ABI remain outside this slice |
| CPU-visible MDU | `CONTRACT_CLOSED` | `make mdu-cpu-gate` plus `make mdu-flush-gate` plus MDU subtest of `make dut-block-unit-gate` | `build/soc_test/mdu_cpu_gate/sim.log`, `build/unit_tb/mdu_flush/compile.log`, `build/unit_tb/mdu_flush/sim.log`, `build/unit_tb/dut_block_readiness/mdu/sim.log` | CPU and block evidence cover MULT/MULTU/DIV/DIVU, MTHI/MTLO/MFHI/MFLO, MADD/MADDU/MSUB/MSUBU, MUL, signed/unsigned boundaries, divide-by-zero, short-multiply early exit, and exception/context flush cancellation with HI/LO preservation; Booth/radix-4 latency, low-latency division and workload performance remain deferred |
| CPU performance counters/APB observability | `SOC_INTEGRATED` | `make perf-counters-gate perf-cpu-gate` plus `make rtl-frontend-compile` | `build/unit_tb/perf_counters/sim.log`, `build/soc_test/perf_cpu/sim.log`, `build/unit_tb/rtl_frontend_compile/` | Opt-in `SOC_PERF_COUNTERS=1` exposes cycle, retire, I/D miss, branch-mispredict, MDU-stall and version registers at `0x4000_C000`; this is an observation contract, not CPI accuracy, CoreMark/Dhrystone, or commercial performance signoff |
| CPU performance workload observation | `SOC_INTEGRATED` | `make perf-workloads-gate` | `build/soc_test/perf_workloads/sim.log` | Four repeatable sequential/strided/branch-mixed/MDU workloads report cycle, retire, I/D miss, branch-mispredict and MDU-stall deltas through the real CPU/APB counter path. This is a bounded microarchitectural baseline, not an official CoreMark, Dhrystone, STREAM result, CPI claim or commercial performance signoff |
| Opt-in FPU scheduler context | `SOC_INTEGRATED` | `make fpu-context-gate rtl-frontend-compile` | `build/unit_tb/fpu_context/sim.log`, `build/unit_tb/rtl_frontend_compile/` | Real scheduler/CPU integration saves task-0 FPR[1:2]/FCSR into the context bank and restores task-1 FPR[1:2]/FCSR under `SOC_FPU_ENABLE=1`; all 8 RTL frontend configurations pass. This closes the bounded hardware context-transfer slice, not a Linux signal frame, lazy-FPU policy, ABI or full OS context contract |
| FPU MOV/ABS/NEG IEEE bit-pattern boundary | `SYSTEM_DIFFERENTIAL`, opt-in | `make fpu-single-gate fpu-double-gate qemu-system-fpu-single-differential-gate qemu-system-fpu-double-differential-gate` | Isolated runs under `/tmp/mips32-fpu-system-bits2/soc_test/`, `/tmp/mips32-fpu-system-bits3/soc_test/`, and corresponding QEMU differential run directories | Real CPU/SoC and `mips32-soc-ref` verify negative zero, NaN payload preservation, ABS/NEG sign changes, and no Invalid sticky update for non-arithmetic NaN operations in both precisions. Complete IEEE-754 arithmetic flags/traps, FPU OS ABI and Linux context semantics remain open |
| FPU configurable rounding and fixed ROUND.W differential | `SYSTEM_DIFFERENTIAL`, opt-in | `make fpu-rounding-gate qemu-system-fpu-rounding-differential-gate` | `/tmp/fpu-rounding-double/rtl/rtl_retire.jsonl`, `/tmp/fpu-rounding-double/isa_ref/completion_report.md`, `qemu/trace_compare.log` | `CVT.W.S/CVT.W.D` are checked for FCSR.RM `00/01/10/11`; fixed `ROUND.W.S/ROUND.W.D` checks ties-away-from-zero (`+2.5 -> 3`, `-1.5 -> -2`) and RTL/QEMU compare passes through retire. The custom QEMU 9.2 build applies and validates the source marker; complete IEEE-754, FPE delivery and Linux FPU ABI remain open |
| VIC CPU nested interrupt path | `SOC_INTEGRATED` | `make vic-nested-gate` plus `make interrupt-priority-gate` | `build/soc_test/vic_nested/sim.log`, `build/unit_tb/vic_priority_checker/sim.log` | Real CPU/SoC exception entry proves source 9 -> 8 -> 7 -> 6 strict-priority preemption, per-frame EPC restoration and reverse ACK/SOFT_CLR unwind; the 32-source unit path additionally proves ACTIVE accumulation and deterministic priority behavior. Arbitrary nesting depth, full 32-source OS interrupt ABI and formal interrupt proof remain outside this bounded contract |
| VIC full 32-source priority/ACK contract | `SYSTEM_DIFFERENTIAL`, opt-in | `make vic-full-sources-gate qemu-system-vic-full-sources-differential-gate` | `build/soc_test/vic_full_sources/sim.log`, `build/isa_ref/qemu_system_vic_full_sources_differential/completion_report.md`, `qemu/trace_compare.log` (`TRACE_COMPARE_PASS records=1166`) | Real RTL and `mips32-soc-ref` execute all 32 software-pending sources with 4-bit priority, highest-priority selection, deterministic lower-ID tie-break, active/running-priority state, ACK/W1C drain and top-bit enable masking. Arbitrary nesting depth, external electrical source timing, multicore IRQ ownership and Linux interrupt ABI remain outside this bounded contract |
| BPU opt-in IF redirect | `SOC_INTEGRATED` | `make bpu-redirect-gate` plus `VCS_EXTRA_ARGS='+define+SOC_BPU_ENABLE=1' make soc-smoke` | `build/unit_tb/bpu_redirect_unit/sim.log`, `build/unit_tb/bpu_redirect_compile/bpu_opt_in/compile.log`, `build/soc_test/bpu_opt_in2/sim.log` | BTB/BHT/RAS predictions are routed through the IF delay-slot pending path; ID resolve remains architectural truth and exception/actual-target recovery is covered by the SoC smoke. CoreMark/Dhrystone hit-rate targets, fetch queue, multiple in-flight branches and formal remain outside this slice |
| CPU/CP0 MTC0 source forwarding and UserLocal context switching | `CONTRACT_CLOSED` | `make rtl-frontend-compile` plus `make cp0-rdhwr-gate` | `build/unit_tb/rtl_frontend_cp0_mtc0_forward`, `build/soc_test/cp0_rdhwr/sim.log` | COP0/MTC0 `rt` is treated as a GPR source and uses existing EX/MEM/WB forwarding; firmware verifies consecutive UserLocal A->B->A writes and RDHWR readback, while CP0/TLB context regression remains passing. Full scheduler ABI remains outside this entry |
| CPU load-return forwarding boundary | `SOC_INTEGRATED` | `make cpu-load-return-gate rtl-frontend-compile` | `build/soc_test/cpu_load_return/sim.log`, `build/unit_tb/rtl_frontend_compile/` | A real default blocking CPU/SoC guest performs an MMIO `LW` in a helper immediately followed by the caller's first useful consumer after the call delay slot; the pass mailbox proves the loaded value reached the returned-value path without an inserted software bubble. This is a bounded load-use/return contract, not arbitrary multi-outstanding load forwarding or full ISA compliance |
| CPU/MMU bounded page-table root allocator | `BLOCK_VERIFIED` | `make mmu-page-table-allocator-gate` | `build/unit_tb/mmu_page_table_allocator/compile.log`, `sim.log` | Four fixed 4KB-aligned root leases, exhaustion, stale-generation rejection, reuse and full-pool same-cycle release+alloc with an incremented token are verified; root memory ownership, PTE population, demand paging and a production OS allocator remain outside this slice |
| UART/WDT/APB baseline | `SOC_INTEGRATED` | `make phase3-complete` and focused UART/WDT gates | `build/uvm/phase3_complete/`, focused `build/unit_tb/` and `build/soc_test/` logs | Production software policy is outside this RTL contract |
| D-cache baseline | `BLOCK_VERIFIED` | Existing D-cache unit/CPU/SoC gates | Focused `build/unit_tb/` and `build/soc_test/` logs | Nonblocking D-cache WIP is retired; ECC/coherence are not claimed |
| D-cache parity/CacheErr propagation | `BLOCK_VERIFIED`, opt-in | `make dcache-parity-gate` | `build/unit_tb/dcache_parity/compile.log`, `sim.log` | Explicit simulation injection covers tag parity and 256-bit data-line parity on a real D-cache hit; a detected fault suppresses `cpu_data_ok`, raises `cpu_cache_error`, enters the error response path, and the line can be reread after injection is cleared. This is single-fault parity/error propagation only; SECDED correction, multi-bit ECC, L2 propagation, silicon reliability and OS cache-error policy remain open |
| Blocking D-cache CACHE writeback alias | `BLOCK_VERIFIED` | `make dcache-parity-gate cpu-cache-op-gate` | `build/unit_tb/dcache_parity/sim.log`, `build/unit_tb/cpu_cacheop/sim.log` | Blocking `dcache` accepts both `0x19` and the MIPS32 R2 `0x1d` alias as `Hit_Writeback_D`, retaining the valid line and clearing dirty only after a successful 8-beat writeback. `0x15` remains `Hit_Writeback_Invalidate_D`; the directed maintenance and parity regression passes. Full cache ordering and OS cache ABI remain open |
| Opt-in L1 nonblocking transaction block | `SOC_INTEGRATED` | `make l1-nonblocking-gate l1-nonblocking-cpu-multi-gate l1-nonblocking-cpu-stress-gate l1-nonblocking-cpu-error-gate l1-nonblocking-cpu-two-error-gate l1-nonblocking-cpu-two-error-reset-gate cache-concurrency-gate` | `build/unit_tb/cache_concurrency/l1nb/`, `build/unit_tb/cache_concurrency/`, `build/soc_test/l1_nonblocking_cpu_multi/`, `build/soc_test/l1_nonblocking_cpu_stress/`, `build/soc_test/l1_nonblocking_cpu_error/`, `build/soc_test/l1_nonblocking_cpu_two_error/`, `build/soc_test/l1_nonblocking_cpu_two_error_reset/` | Real CPU/D-cache opt-in path is wired through `mips_core` and covers 2-MSHR refill, secondary responses, four-entry writeback full/empty under downstream backpressure, dirty eviction, sub-word loads, reset-in-flight, one precise AXI refill error, simultaneous independent injected responses with precise younger-request flush/replay, reset with both MSHRs active followed by fresh post-reset errors, and the full-ROB same-cycle error/retire/tag-reuse boundary. The opt-in AXI bridge has two read slots with ID-based response routing; three-or-more errors, arbitrary reset/error timing, maintenance/coherence and default-path switching remain open |
| Opt-in L1 AXI bridge direct OOO contract | `UNIT_VERIFIED` | `make l1-nonblocking-axi-bridge-gate` | `build/unit_tb/cache_concurrency/l1nb_axi_bridge/` | Direct bridge test proves two AXI read IDs, ID 1 before ID 0 response routing, per-slot error association, and reset flush. It does not close full CPU simultaneous-fault or coherency behavior |
| Opt-in L1 maintenance compatibility | `SOC_INTEGRATED` | `make l1-nonblocking-maintenance-compat-gate l1-nonblocking-maintenance-cpu-gate` plus `make sva-gate` and `SVA_ENABLE=1 make l1-nonblocking-cpu-two-error-reset-gate` | `build/unit_tb/cache_concurrency/l1nb_maintenance_compat/`, `build/soc_test/l1_nonblocking_maintenance/`, `build/sva/`, `build/soc_test/l1_nonblocking_cpu_two_error_reset/` | CPU CACHE completion/stall, TagLo/TagHi and SYNC contracts pass; real opt-in CPU/D-cache firmware issues `Index_Invalidate_D`, `Hit_Invalidate_D`, `Index_Load_Tag_D`, `Index_Store_Tag_D`, `Index_Writeback_Invalidate_D`, `Hit_Writeback_Invalidate_D`, and `Hit_Writeback_D`. Dirty writeback commands hold completion until the L1 AXI line write drains; the unit and CPU gates check valid/dirty/tag round-trip and writeback-only line retention. Full cache ordering, coherency and OS cache ABI are not claimed |
| L1 I-cache parity and OOM closure | `BLOCK_VERIFIED` | `make cpu-icache-exec-gate cpu-icache-error-gate cpu-icache-product-error-gate cpu-icache-stress-gate cpu-icache-tag-gate` plus the parity unit test in `tb/unit/icache/tb_icache.v` | `build/unit_tb/icache_parity_closure/compile.log`, `sim.log`; `build/unit_tb/mips_core_icache_{exec,error,product_error,stress,tag}/compile.log`, `sim.log` | Explicit simulation parity injection replaces dynamic two-dimensional memory `force/release`, whose retained simulator objects caused the prior VCS OOM; production ports are tied off. Data/tag parity-to-CacheErr and focused CPU gates pass; silicon ECC, full OS cache ABI and cgroup-based resource policy remain outside this contract |
| L1/L2/DDR data path | `SOC_INTEGRATED` | `make l2-end-to-end-gate` and `make l2-end-to-end-gate L2_WRITEBACK=1` | `build/soc_test/l2_end_to_end/sim.log`, `build/soc_test/l2_end_to_end_wb2/sim.log` | Real CPU data traffic is observed at the L2 downstream AXI port for cold refill, L1 conflict eviction, L2 hit and WT/WB readback; L2 8-way capacity eviction and complete WB dirty-victim AW/W/B evidence remain block-level because the behavioral DDR window is 128KB |
| Opt-in L2 nonblocking dirty writeback buffer | `UNIT_VERIFIED`, opt-in | `RUN_DIR=build/unit_tb/cache_concurrency_wb_depth4_retry tb/unit/cache/run_concurrency_gate.sh` | `build/unit_tb/cache_concurrency_wb_depth4_retry/sim.log` | The real L2 nonblocking block test fixes `WB_DEPTH=4`, fills all four dirty-victim slots under miss-under-miss pressure, observes `peak_mshr=8 peak_wb=4`, and verifies 63 readbacks against the backing-memory scoreboard. Downstream AXI remains intentionally single-transaction; L2 coherency/snoop/directory, arbitrary writeback error/reset interleavings and CPU default-path selection remain open |
| Opt-in L2 nonblocking snoop invalidate/writeback | `UNIT_VERIFIED`, opt-in | `make cache-concurrency-gate` | `build/unit_tb/cache_concurrency/l2nb/sim.log` (`peak_wb=4`, `reads_checked=65`) | A clean resident line is invalidated and refilled, while a dirty snooped line is snapshotted into the WB buffer, written through downstream `AW/W/B`, invalidated, and refilled with preserved data; an outstanding matching MSHR is not invalidated, and a matching same-cycle CPU request is backpressured. Directory ownership and full multicore coherency remain open |
| Opt-in L2 nonblocking CPU/SoC path | `SOC_INTEGRATED`, opt-in | `make l2-nonblocking-end-to-end-gate` | `build/soc_test/l2_end_to_end_nonblocking/sim.log` (`L2_E2E_TEST_SUCCESS policy=nonblocking-write-back`) | The real CPU/L1/SoC smoke selects `SOC_L2_NONBLOCKING` and reaches the L2 end-to-end completion marker through the nonblocking L2 implementation. This workload is a bounded integration smoke; complete L2 coherency/snoop/directory, error/reset interleavings and default blocking-path switching remain open |
| Opt-in L2 nonblocking AXI error/reset recovery | `UNIT_VERIFIED`, opt-in | `make cache-concurrency-gate` | `build/unit_tb/cache_concurrency/l2nb/compile.log`, `sim.log` (`REGRESSION_TEST_SUCCESS l2nb (reads_checked=68)`) | The L2-NB unit contract injects refill `SLVERR` across a full burst, verifies primary/secondary waiter error propagation and clean retry, injects dirty-victim `B` error and verifies MSHR/WB recovery, then resets during an active refill and refills successfully after release. The implementation drains the complete downstream burst before resolving waiters; arbitrary error/reset timing, snoop-WB error reporting, full coherency/directory and commercial signoff remain open |
| L2 CPU firmware data-path gate | `SOC_INTEGRATED` | `make l2-cpu-gate` and `make l2-cpu-gate L2_WRITEBACK=1` | `build/soc_test/l2_cpu_gate/sim.log`, `build/soc_test/l2_cpu_gate_wb/sim.log` | Complete cached-memory, store-merge, conflict/eviction, and cacheable-physical/KSEG1 durable-alias firmware checks; the gate uses explicit behavioral-DDR test addresses, a 20 ms watchdog, and excludes only generic late JTAG/reset stimulus, which remains covered by reset-recovery gates |
| L1 nonblocking CPU AXI response error | `SOC_INTEGRATED` | `make l1-nonblocking-cpu-error-gate l1-nonblocking-cpu-error-reset-gate l1-nonblocking-cpu-two-error-gate l1-nonblocking-cpu-two-error-reset-gate` | `build/soc_test/l1_nonblocking_cpu_error/sim.log`, `build/soc_test/l1_nonblocking_cpu_error_reset/sim.log`, `build/soc_test/l1_nonblocking_cpu_two_error/sim.log`, `build/soc_test/l1_nonblocking_cpu_two_error_reset/sim.log`, corresponding `*_report.md` files | Opt-in DDR-model SLVERR crosses write-through L2/L1/ROB and retires precise CacheErr with ErrorEPC recovery. The two-line companion verifies independent in-flight faults and precise younger-request flush/replay; the reset companion asserts reset after both MSHRs are active and verifies both fresh post-reset injections. Three-or-more errors, arbitrary timing, physical DDR failure behavior and maintenance/coherence remain outside this slice |
| L1 nonblocking real DDR window | `SOC_INTEGRATED`, opt-in | `make l1-nonblocking-ddr-gate l1-nonblocking-cpu-multi-gate rtl-frontend-compile` | `build/soc_test/l1_ddr_nonblocking/sim.log`, `l1_ddr_nonblocking_report.md`, `build/soc_test/l1_nonblocking_cpu_multi/sim.log`, `build/rtl_frontend_compile/` | `SOC_L1_NONBLOCKING_DDR_ENABLE=1` admits `0x0800_0000..0x0fff_ffff` after physical translation; real CPU/L1/AXI/DDR4 controller firmware covers two stores, same-line merge/readback, a second line and final hit. Default blocking and SRAM-only opt-in behavior remain unchanged; full maintenance/coherence and physical PHY timing remain open |
| DMA CPU AXI response error | `SOC_INTEGRATED` | `make dma-axi-error-gate` | `build/soc_test/dma_axi_error/sim.log`, `dma_axi_error_report.md` | A behavioral DDR-model SLVERR on a real DMA source request reaches DMA `ERR_AXI_READ`, channel IRQ and PIC source 3; combined DONE/ERR W1C re-arm is checked. Reset-in-flight, physical DDR failure behavior and full DMA system policy remain open |
| DMA CPU reset-in-flight recovery | `SOC_INTEGRATED` | `make dma-reset-inflight-gate` | `build/soc_test/dma_reset_inflight/sim.log`, `dma_reset_inflight_report.md` | Testbench asserts SoC reset after channel 0 is genuinely busy; firmware restarts, repeats a 256-byte uncached direct transfer, checks data integrity and DONE/W1C. Physical DMA/DDR reset policy and arbitrary multi-channel reset interleavings remain open |
| Dual-core shared-memory coherency | `SOC_INTEGRATED`, opt-in | `make dcache-coherency-gate dual-core-soc-gate llsc-coherency-gate coherency-stress-gate` | Fresh current-head artifacts: `build/unit_tb/dcache_coherency/sim.log`, `build/soc_test/dual_core/sim.log`, `build/soc_test/coherency_stress/sim.log`, `build/unit_tb/llsc_coherency_gate/sim.log`; `coherency_stress` contains `COH_STRESS_SHARED_MEMORY_PASS` and `REGRESSION_TEST_SUCCESS` | Fixed a coherency-mode uncached-store path that updated resident D-cache data/tag without recomputing data/tag parity and caused a false CacheErr on the next access. Fresh focused unit, dual-core IPI, LL/SC coherency and 8-round shared-memory stress gates pass. Full MESI/directory ordering remains outside this contract |
| LL/SC reservation exception/coherency boundary | `SOC_INTEGRATED` | `make llsc-gate llsc-coherency-gate` | `build/soc_test/llsc/`, `build/soc_test/llsc_coherency_gate/sim.log` | CPU clears the reservation on reset, context restore, accepted exception/interrupt flush, ordinary completed store and peer line notification; firmware verifies `LL -> SYSCALL -> ERET -> SC=0` and dual-core peer notification `SC=0` with memory unchanged. This is a bounded reservation policy, not full MIPS memory ordering, atomicity under arbitrary interleavings or MESI/directory signoff |
| LL/SC interrupt boundary unit | `BLOCK_VERIFIED` | `make llsc-interrupt-boundary-gate` | `build/unit_tb/llsc_interrupt_boundary/sim.log` | Direct CPU test drives `IE/IM` and an external interrupt, then observes the common `interrupt_accept` flush clears a pre-existing reservation. This is a directed lifecycle check, not interrupt nesting or complete atomic memory-ordering verification |
| Asynchronous IRQ branch-delay BD/EPC | `SOC_INTEGRATED` | `make cpu-irq-delay-slot-gate` | `build/soc_test/cpu_irq_delay_slot/sim.log` (`REGRESSION_TEST_SUCCESS`, `CPU_CP0_SUMMARY intr=1`) | A real timer/PIC interrupt is accepted while a branch is in MEM and its delay slot is in EX; the handler requires `Cause.BD=1` and the success mailbox. This closes the targeted early-IRQ BD/EPC slice, not Linux interrupt ABI or complete privileged-ISA behavior |
| Dual-core MMU shootdown target-ACK handshake | `SOC_INTEGRATED`, opt-in | `make dual-core-soc-gate mmu-ipi-shootdown-pressure-gate rtl-frontend-compile` | `build/soc_test/dual_core/sim.log`, `build/unit_tb/mmu_ipi_shootdown_pressure/sim.log`, `build/rtl_frontend_compile/*/compile.log` | Dual-core APB aliases exercise target-1 and target-0 invalidation payloads; the SoC now returns ACK one cycle after the target-side invalidate event, preserving generation/target validation. Standalone pressure covers stale ACK, busy rejection and timeout. OS scheduler/page-table ownership, multicore data coherency protocol and Linux VM remain outside this contract |
| Dual-core MMU shootdown end-to-end firmware | `SOC_INTEGRATED`, opt-in | `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/mmu-dual-shootdown-20260903f FW_DIR=/tmp/mmu-dual-fw-20260903d RUN_DIR=/tmp/mmu-dual-shootdown-20260903f/soc_test tb/soc_test/run_dual_core_mmu_shootdown_gate.sh` | `/tmp/mmu-dual-shootdown-20260903f/soc_test/sim.log` (`dual-core MMU shootdown end-to-end gate: PASS`) | Real core0/core1 product-boot firmware establishes a core1 translated-page fast-path entry, sends a target-1 APB page invalidate, observes target ACK, refills and verifies the remapped value. This is a bounded opt-in multicore RTL slice; Linux VM/page-table ownership, SMP scheduler policy, full coherency and complete privileged/MMU compliance remain open |
| QEMU system dual-mailbox MMU IPI contract | `SYSTEM_DIFFERENTIAL`, opt-in bounded slice | `make qemu-system-mmu-ipi-contract-gate` | `build/isa_ref/qemu_system_mmu_ipi_contract/completion_report.md`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl`, `contract_compare.log` | The RTL dual-core APB mailbox path and the single-vCPU `mips32-soc-ref` model compare the same 24 control writes and all four sticky outcomes: done, timeout, rejected-while-busy and stale-ACK. Explicit core-0/core-1 QEMU windows are present. This is a bounded mailbox/event differential, not full SMP execution, Linux scheduler/page-table ownership, multicore TLB shootdown or coherency signoff |
| QSPI vendor-neutral functional bundle | `SOC_INTEGRATED` | `make qspi-vendor-neutral-complete-gate` | `build/unit_tb/qspi_*`, `build/unit_tb/spi_flash_unit/`, `build/unit_tb/xip_read_timeout/` | Aggregate executes SPI flash, timeout guard, status/taxonomy/retry, command/flash model, pad/shared-pin, x1/quad AXI-XIP, SoC quad memory and SoC status gates. Physical pin/device timing, erase/program endurance, secure boot, Linux MTD and production signoff remain outside |
| QEMU system DMA v2 event contract | `CONTRACT_CLOSED` | `make qemu-system-dma-v2-model-gate qemu-system-dma-v2-event-contract-gate` | `build/isa_ref/qemu_system_dma_v2_event_contract_fresh_20260814/` (`DMA_EVENT_CONTRACT_PASS events=23`) | Ordered START/DONE/W1C/IRQ semantic events compare equal for legacy alias, v2 direct, zero-length, two-descriptor SG, W1C/re-arm and IRQ corpus; status-poll latency is excluded by contract; physical AXI error/reset-in-flight remain open |
| DDR4 ECC SECDED path | `CONTRACT_CLOSED` | `make ecc-secded-gate ddr4-controller-gate ddr4-controller-stress-gate ddr4-status-gate ddr4-pic-integration-gate` | `build/unit_tb/ecc_secded_32/`, `build/unit_tb/axi_ddr4_controller*/`, `build/unit_tb/apb_ddr4_status/`, `build/unit_tb/ddr4_pic_integration/` | SECDED correction/detection, controller storage/injection, APB status classification, and PIC source 5 fatal/uncorrectable IRQ escalation are verified; multi-rank policy and silicon escalation are outside this RTL contract |
| AXI bounded multi-outstanding response routing | `BLOCK_VERIFIED` | `make fabric-unit-gate` | `build/unit_tb/fabric_ooo_closure4/xbar_ooo/sim.log`, `build/unit_tb/fabric_ooo_closure4/xbar_multi_ot/sim.log` | Crossbar boundary supports four entries per slave, different-ID R/B response reordering, same-ID ordering and cross-slave concurrency; L1/L2/APB/Flash end-to-end multi-outstanding, commercial VIP and formal remain outside this contract |
| EIC/VEIC vector path | `SOC_INTEGRATED` | `make product-vectored-interrupt-gate` | `build/unit_tb/product_vectored_interrupt/` | VEIC is opt-in and uses the VIC source ID contract; nested interrupt policy and full MIPS compliance remain separate |
| VIC priority arbitration | `BLOCK_VERIFIED` | `make interrupt-priority-gate` | `build/unit_tb/vic_priority_checker/compile.log`, `sim.log` | Checker verifies pending selection, maximum priority and lowest-ID tie-break over the existing 32-source/nesting TB; formal proof and multi-core interrupt policy remain deferred |
| ISA R2 implemented subset | `SYSTEM_DIFFERENTIAL` | `make isa-r2-gate qemu-system-isa-r2-differential-gate bitswap-gate` | `build/soc_test/isa_r2_sweep/`, `build/soc_test/bitswap/`, `build/isa_ref/qemu_system_isa_r2_differential/completion_report.md`, `qemu/trace_compare.log` (`TRACE_COMPARE_PASS`) | Directed firmware includes SPECIAL3 `EXT`/`INS`/`BITSWAP`/`RDHWR`, `PREF`/`PREFX`/`SYNCI`, and the JR.HB/JALR.HB/EHB control-flow barrier encodings; RDHWR uses explicit architectural encodings for SYNCI_Step, CPUNum, and CCRes with HWREna setup. The project QEMU 9.2 build applies explicit R2 overrides for legacy BITSWAP and no-FPU PREFX and validates source markers before cache reuse. The differential uses explicit `24Kc` for the default no-FPU RTL contract; FPU gates use `24Kf`. Full compliance suite and broader privileged/MMU differential remain open |
| MIPS32 R2 branch-likely subset | `SOC_INTEGRATED` | `make branch-likely-gate` | `build/soc_test/branch_likely/` | BEQL/BNEL/BLEZL/BGTZL and BLTZL/BGEZL taken/not-taken paths verify one delay-slot execution or annulment; BLTZALL/BGEZALL verify link-on-taken and no-link-on-annul. Full compliance edge matrix remains open |
| MIPS32 R2 branch-likely RTL/QEMU differential | `SYSTEM_DIFFERENTIAL` | `make qemu-system-branch-likely-differential-gate` | `build/isa_ref/qemu_system_branch_likely_differential/completion_report.md` | 49 effective retire records compare through the retire/mailbox boundary. QEMU plugin annulled-slot events are filtered using the preceding likely condition, and BLTZALL/BGEZALL link writes are conditionally decoded; full ISA compliance remains open |
| MIPS32 SPECIAL3 reserved-field negative coverage | `SOC_INTEGRATED` | `make mips-control-special3-gate bitswap-gate` | `build/unit_tb/mips_control_special3/compile.log`, `sim.log`, `build/soc_test/bitswap/sim.log` | EXT/INS/RDHWR, BITSWAP and the supported BSHFL operations are checked; reserved `sa` encodings and non-zero fixed-`rs` encodings raise RI with no side effects. Full ISA compliance remains deferred |
| COP1 single precision, COP1X fused arithmetic and conditional moves | `SYSTEM_DIFFERENTIAL`, opt-in | `make fpu-single-gate mips-fpu-compare-gate qemu-system-fpu-single-differential-gate mips-control-fpu-cond-gate` | `build/soc_test/fpu_single/sim.log`, `build/unit_tb/mips_fpu_compare/sim.log`, `build/unit_tb/mips_control_fpu_cond/sim.log`, `build/isa_ref/qemu_system_fpu_single_differential/completion_report.md`, `qemu/trace_compare.log` (`TRACE_COMPARE_PASS records=1320`) | Real CPU/D-cache/DDR and QEMU retire differential cover single arithmetic, COP1X `MADD.S/MSUB.S/NMADD.S/NMSUB.S`, `RECIP.S/RSQRT.S`, conversions, FPR transfers, conditional moves, all FCC selectors, FCSR Flags/Cause state and immediate CFC1 integer consumption. The project QEMU 9.2 build applies `qemu-9.2-mips32-fpu-int32-indefinite.patch`, aligning invalid/overflow W conversions with the RTL architectural indefinite result `0x80000000`; configurable rounding modes, precise FPE traps, OS context and ABI remain open |
| COP1 memory path | `SOC_INTEGRATED`, opt-in | `make fpu-single-gate qemu-system-fpu-single-differential-gate` | `build/soc_test/fpu_single/sim.log`, `build/isa_ref/qemu_system_fpu_single_differential/` | Real CPU/D-cache/DDR LWC1/SWC1 plus ordered two-word LDC1/SDC1 transfers reach FPR state; full FPU memory ABI and OS context remain open |
| COP1 compare/conditional branch slice | `SOC_INTEGRATED`, opt-in | `make mips-fpu-compare-gate fpu-branch-gate qemu-system-fpu-branch-differential-gate qemu-system-fpu-single-differential-gate` | `build/unit_tb/mips_fpu_compare/sim.log`, `build/soc_test/fpu_branch/sim.log`, `build/isa_ref/qemu_system_fpu_single_differential/completion_report.md` | All 16 C.* predicate results are checked for single- and double-precision ordered, equal, and legacy-MIPS SNaN/QNaN vectors; the real CPU FCSR path, including behavioral Flags[6:2] sticky and Cause[16:12] latest-operation state, and BC1F/BC1T/BC1FL/BC1TL ordinary delay-slot and likely-annul behavior compare through the selected system differential corpus. Complete IEEE-754 NaN policy, rounding, FPE delivery and full COP1 compliance remain open |
| COP1 CFC1 integer-consumer pipeline contract | `SYSTEM_DIFFERENTIAL`, opt-in | `make fpu-single-gate qemu-system-fpu-single-differential-gate` | `build/soc_test/fpu_single/sim.log`, `build/isa_ref/qemu_system_fpu_single_differential/completion_report.md`, `qemu/trace_compare.log` | Real CPU executes CFC1 followed immediately by SRL, ANDI and conditional branch without hand-inserted bubbles. RTL and QEMU retire traces compare equal after FCSR Cause[16:12] was modeled as latest-operation state while Flags[6:2] remain sticky. Arbitrary future COP1/GPR forwarding combinations remain outside this bounded contract |
| COP1 double-precision pair and COP1X fused behavior | `SYSTEM_DIFFERENTIAL`, opt-in | `make fpu-double-gate qemu-system-fpu-double-differential-gate` | `build/soc_test/fpu_double/sim.log`, `build/isa_ref/qemu_system_fpu_double_differential/completion_report.md`, `qemu/trace_compare.log` (`TRACE_COMPARE_PASS records=297`) | `SOC_FPU_ENABLE=1` real CPU path uses even-register 64-bit pairs and covers ADD.D/SUB.D/MUL.D/DIV.D/ABS.D/MOV.D/NEG.D, `SQRT.D`, COP1X `MADD.D/MSUB.D/NMADD.D/NMSUB.D`, `RECIP.D/RSQRT.D`, `MOVZ.D/MOVN.D` and selected sticky FCSR flags; behavioral development evidence only, not complete IEEE-754, FPE, OS-context or Linux ABI signoff |
| COP1 precise FPE enable slice | `SOC_INTEGRATED`, opt-in | `make fpu-fpe-exception-gate` | `build/soc_test/fpu_fpe_exception/sim.log`, `build/soc_test/fpu_fpe_exception/textReportFinal/` | CU1-enabled `DIV.S 1.0/0.0` with FCSR Enable[div0] verifies precise ExcCode 15, FCSR Cause/Flags update (`0x00008420`) and no FPR destination commit through the real CPU/SoC path; invalid/overflow/underflow/inexact combinations, rounding modes, OS FPU context and full IEEE-754/COP1 compliance remain open |
| COP1 precise FPE invalid slice | `SOC_INTEGRATED`, opt-in | `make fpu-fpe-invalid-gate` | `build/soc_test/fpu_fpe_invalid/sim.log` | CU1-enabled `DIV.S 0.0/0.0` with FCSR Enable[invalid] verifies precise ExcCode 15, Invalid Cause/Flags and no FPR destination commit; remaining overflow/underflow/inexact classes, range policy and full IEEE-754/COP1 compliance remain open |
| COP1 precise FPE overflow slice | `SOC_INTEGRATED`, opt-in | `make fpu-fpe-overflow-gate` | `build/soc_test/fpu_fpe_overflow/sim.log` | Real CPU `MUL.S` of largest finite single by 2 with FCSR Enable[overflow] verifies precise ExcCode 15, `Overflow|Inexact` Cause/Flags mask (`0x5`) and no FPR destination commit; underflow classes, range policy and full IEEE-754/COP1 compliance remain open |
| COP1 precise double FPE three-class slice | `SYSTEM_DIFFERENTIAL`, opt-in | `make fpu-fpe-double-gate qemu-system-fpu-fpe-double-differential-gate` | `build/soc_test/fpu_fpe_double/sim.log`, `build/isa_ref/qemu_system_fpu_fpe_double_differential/completion_report.md`, `textReportFinal/` | Real CPU and `mips32-soc-ref` execute `DIV.D` divide-by-zero, `DIV.D 0/0` invalid, and max-finite-double `MUL.D 2` overflow with enabled traps; each reaches ExcCode 15, Cause/Flags masks are checked (overflow is `Overflow|Inexact=0x5`), both FPR words remain uncommitted, and handler ERET recovery reaches the next vector. The system differential compares the bounded retire stream and passes. Full IEEE-754, inexact/underflow policy, OS FPU context/ABI and full COP1 compliance remain open |
| COP1 precise double FPE Inexact slice | `SYSTEM_DIFFERENTIAL`, opt-in | `make fpu-fpe-double-inexact-gate` plus independent QEMU capture/compare | `build/soc_test/fpu_fpe_double_inexact/sim.log`, `build/firmware/fpu_fpe_double_inexact/firmware.objdump`, `build/isa_ref/qemu_system_fpu_fpe_double_inexact_differential/rtl/rtl_retire.jsonl` | Real CPU executes `CVT.W.D 1.5` with only FCSR Inexact enabled, reaches ExcCode 15, verifies Cause/Flags index 0 (`FCSR=0x00001084`) and suppresses both words of the destination pair. Independent QEMU system capture converts 50 events to 50 retire records and strict comparison passes `TRACE_COMPARE_PASS records=49`; the standard wrapper's QEMU process-exit timeout remains an infrastructure residual, not a comparator waiver. Complete IEEE-754 double range/tininess policy, OS FPU context/ABI and full COP1 compliance remain open |
| COP1 precise double FPE Underflow boundary | `SYSTEM_DIFFERENTIAL`, opt-in | `make fpu-fpe-double-underflow-gate qemu-system-fpu-fpe-double-underflow-differential-gate` | `build/soc_test/fpu_fpe_double_underflow/sim.log`, `build/firmware/fpu_fpe_double_underflow/firmware.objdump`, `build/isa_ref/qemu_system_fpu_fpe_double_underflow_differential/completion_report.md`, `qemu/trace_compare.log`, `qemu/qemu_command.txt` | Real CPU executes minimum positive double subnormal times `0.5`, reaches ExcCode 15 with enabled Underflow|Inexact Cause/Flags and suppresses both destination-pair words. The QEMU custom-machine reference compares the same mailbox-bounded retire stream and passes. Capture guards reject pathological event/state growth before conversion, preventing the previous OOM. Exact tininess policy, complete IEEE-754, OS FPU ABI and full COP1 compliance remain open |
| COP1 precise FPE underflow boundary slice | `SOC_INTEGRATED`, opt-in | `make fpu-fpe-underflow-gate` | `build/soc_test/fpu_fpe_underflow/sim.log`, `build/soc_test/fpu_fpe_underflow/textReportFinal/` | Real CPU `MUL.S` involving the minimum positive single subnormal verifies the selected underflow/inexact Flags/Cause behavior and precise enabled-trap path through the SoC mailbox; this is one finite-single boundary vector, not complete tininess/rounding/inexact policy or full IEEE-754 compliance |
| COP1 precise FPE inexact enabled slice | `SYSTEM_DIFFERENTIAL`, opt-in | `make fpu-fpe-inexact-gate qemu-system-fpu-fpe-inexact-differential-gate` | `build/soc_test/fpu_fpe_inexact/sim.log`, `build/isa_ref/qemu_system_fpu_fpe_inexact_differential_v2/completion_report.md`, `qemu/trace_compare.log`, `qemu/qemu_command.txt` | Real CPU executes enabled `CVT.W.S 1.5`, reaches ExcCode 15, verifies Cause/Flags Inexact (`FCSR=0x00001084`) and confirms the destination FPR is not committed. RTL/QEMU system-mode retire traces compare through the mailbox boundary with `TRACE_COMPARE_PASS records=43`; the QEMU reference patch retains accrued Flags on enabled FPE. Complete IEEE-754 rounding/inexact policy, double FPE corpus, OS context and ABI remain open |
| COP1 precise FPE boundary system differential aggregate | `SYSTEM_DIFFERENTIAL`, opt-in | `make qemu-system-fpu-fpe-boundary-differential-gate` | `build/isa_ref/qemu_system_fpu_fpe_{inexact,invalid,overflow,underflow}_differential/completion_report.md`, `build/isa_ref/qemu_system_fpu_fpe_double_differential/completion_report.md`, `build/isa_ref/qemu_system_fpu_fpe_double_{inexact,underflow}_differential/completion_report.md` | The custom `mips32-soc-ref` machine and real RTL CPU compare the selected single-precision Inexact/Invalid/Overflow/Underflow boundaries, double-precision divide-by-zero/Invalid/Overflow boundaries, and double Inexact/Underflow boundaries through the mailbox-bounded retire stream. The aggregate runs all nine selected boundary classes and each report must be `Result: PASS`; Overflow uses the architectural `Overflow|Inexact` mask (`0x5`) on both sides. This is a bounded exception corpus, not complete IEEE-754, rounding/tininess policy, OS FPU ABI or full COP1 compliance |
| COP1 primitive invalid/underflow classification | `UNIT_VERIFIED`, opt-in | `make mips-fpu-flags-gate` | `build/unit_tb/mips_fpu_flags/compile.log`, `sim.log` | Behavioral FPU primitive verifies single `Inf-Inf`, double `Inf/Inf`, double `0*Inf` invalid flags and minimum-normal-times-half double underflow classification. This strengthens the primitive boundary only; complete IEEE-754 rounding/inexact policy, double precise FPE delivery, OS context and ABI remain open |
| COP1 FCSR rounding-mode slice | `SOC_INTEGRATED`, opt-in | `make fpu-rounding-gate mips-fpu-compare-gate` | `build/soc_test/fpu_rounding/sim.log`, `build/unit_tb/mips_fpu_compare/sim.log` | Real CPU verifies `CVT.W.S` nearest-even ties (`1.5/2.5 -> 2`), toward-zero, toward +inf and toward -inf, while fixed `ROUND.W.S` uses ties-away (`+2.5 -> 3`, `-1.5 -> -2`); primitive also checks the corresponding `ROUND.W.D` behavior. Overflow/range invalid policy, complete inexact/underflow/overflow signaling and OS FPU ABI remain open |
| COP1 double conversion and fixed-rounding slice | `SYSTEM_DIFFERENTIAL`, opt-in | `make fpu-double-gate qemu-system-fpu-double-differential-gate` | `build/soc_test/fpu_double/sim.log`, `build/isa_ref/qemu_system_fpu_double_differential/completion_report.md`, `qemu/trace_compare.log` | Real CPU and QEMU retire traces compare `CVT.S.D`, `CVT.D.S`, `CVT.D.W`, `CVT.W.D`, and `ROUND/TRUNC/CEIL/FLOOR.W.D` vectors. FCSR-configurable rounding modes, complete IEEE-754 special-value policy, precise FPE delivery, OS context and ABI remain open |
| MIPS32 CACHE/SYNC/PREF decoder contract | `SOC_INTEGRATED` | `make mips-control-cache-gate` | `build/unit_tb/mips_control_cache/compile.log`, `sim.log` | All 10 implemented CACHE operation codes, 5 reserved CACHE codes, SYNC, and PREF are checked for the expected maintenance/no-op decode and absence of unintended GPR or memory side effects; full cache ordering and OS cache ABI remain open |
| Opt-in L1 SYNC drain barrier | `SOC_INTEGRATED`, opt-in | `make l1-nonblocking-sync-gate cpu-cache-tag-gate` | `build/unit_tb/cache_concurrency/l1nb_sync/compile.log`, `sim.log`; `build/unit_tb/mips_cpu_cachetag/compile.log`, `sim.log` | SYNC uses a private maintenance encoding. The opt-in L1 accepts it only after MSHRs, response FIFO, writeback queue and line-port activity drain; the directed test holds an outstanding refill and verifies no early completion, then verifies completion after the refill. The CPU cache-tag path also observes and completes the barrier without side effects. Blocking dcache behavior remains an ordered idle completion. Full OS cache ABI and multicore memory-ordering validation remain open |
| MIPS32 unaligned merge memory slice | `SOC_INTEGRATED` | `make qemu-system-unaligned-gate qemu-system-unaligned-differential-gate` | `build/soc_test/qemu_system_unaligned/`, `build/isa_ref/qemu_system_unaligned_differential/` | Real CPU/DDR and QEMU compare little-endian LWL/LWR/SWL/SWR through mailbox retirement. Merge-load `mem_rdata` is excluded because RTL reports formatted merge data and QEMU reports the raw aligned word; GPR architectural results, addresses and access attributes remain compared. Cross-page faults, Linux policy and full ISA remain open |
| QEMU synchronous exception and integer-overflow retire differential | `SYSTEM_DIFFERENTIAL` | `python3 -m py_compile tb/isa_ref/qemu_system_state_to_jsonl.py` plus `make qemu-system-exception-differential-gate` | `build/isa_ref/qemu_system_exception_differential/completion_report.md`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl`, `qemu/trace_compare.log` | The custom QEMU machine and real RTL CPU compare signed `ADD`, `SUB`, and `ADDI` overflow as `ExcCode=12` with no faulting GPR commit, plus a non-trapping `ADDIU` wrap and the existing syscall/ERET boundary. This is selected integer exception evidence; complete MIPS32/privileged ISA compliance and arbitrary exception-priority coverage remain open |
| MIPS32 R2 DI/EI interrupt mask operations | `SOC_INTEGRATED` | `make qemu-system-di-ei-differential-gate` | `build/isa_ref/qemu_system_di_ei_differential/completion_report.md`, `qemu/trace_compare.log` | DI/EI return the pre-operation Status value in `rt` and atomically update Status.IE; RTL and QEMU system-mode retire traces compare through the mailbox boundary. WAIT, interrupt wakeup timing, debug instructions and broader privileged differential remain open |
| MIPS32 WAIT interrupt wakeup differential | `SYSTEM_DIFFERENTIAL` | `make qemu-system-wait-differential-gate` | `build/isa_ref/qemu_system_wait_differential/completion_report.md`, `qemu/trace_compare.log` | WAIT retires, a replayed software interrupt wakes the core, the handler clears Cause and returns with ERET, and execution resumes at the post-WAIT guest before the mailbox; physical interrupt timing and broader privileged ISA remain open |
| MIPS32 COP0/COP1 privileged decoder negative coverage | `SOC_INTEGRATED` | `make mips-control-cp0-gate` | `build/unit_tb/mips_control_cp0/compile.log`, `sim.log` | Thirteen supported COP0/COP1 boundary encodings and twenty-two malformed/reserved encodings are checked; invalid ERET/MFMC0/COP0 sel/transfer/RDPGPR/WRPGPR/COP1 transfer/pair fields raise RI without GPR, CP0, TLB, WAIT, memory, cache, branch, or jump side effects. Full privileged ISA remains open |
| QEMU system-mode RTL retire differential | `SOC_INTEGRATED` | `make qemu-system-retire-differential-gate qemu-system-exception-differential-gate qemu-system-bd-exception-differential-gate qemu-system-peripheral-differential-gate qemu-system-vic-differential-gate qemu-system-vic-cpu-differential-gate` | `build/isa_ref/qemu_system_*/completion_report.md`, `build/isa_ref/qemu_system_vic_cpu_differential_direct3/qemu/trace_compare.log` | Selected bare-metal corpus compares one retire record per instruction through mailbox boundaries, including CP0/ERET, BD/EPC, devices and replayed VIC timing; the VIC corpus uses direct caller MMIO loads because generic jal/load forwarding remains open; full MIPS compliance, FPU, MMU demand paging, external physical IRQ timing and Linux/OS boot remain open |
| QEMU custom GPIO input/timer model | `SOC_INTEGRATED`, opt-in | `make qemu-system-gpio-input-gate` | `build/isa_ref/qemu_system_gpio_input/completion_report.md`, `qemu_stdout.log` | `gpio-input=` drives input-only GPIO readback while `GPIO_DIR=0`; timer source-2 assertion, sticky INT status and W1C are verified in the same QEMU guest. This is a reference-machine peripheral contract, not board-level GPIO synchronization or physical timer clock signoff |
| QEMU system-mode MMU process pressure | `SYSTEM_DIFFERENTIAL`, opt-in | `make qemu-system-mmu-process-pressure-gate` | `build/isa_ref/qemu_system_mmu_process_pressure/completion_report.md`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl`, `qemu/trace_compare.log` | The current corpus is the deterministic 8x8 workload: eight software ASIDs, eight 4-KiB demand pages per ASID, 32 dynamic TLB pairs, software round-robin TLBWI refill, wired retention, dynamic shootdown and post-shootdown refill; fresh `TRACE_COMPARE_PASS` is recorded under the dedicated 2026-09-02 evidence row. TLBWR random timing and OS scheduler/page-table ownership, multicore shootdown and Linux VM remain open |
| QEMU system-mode MMU contract | `SYSTEM_DIFFERENTIAL` | `make qemu-system-mmu-contract-gate` | `build/isa_ref/qemu_system_mmu_contract/completion_report.md`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl`, `qemu/trace_compare.log` | ASID refill/reuse, wired APB mapping, sticky shootdown status, architectural ACK invalidation and post-shootdown refill pass in RTL/QEMU retire differential; bounded single-core evidence only, not OS scheduler ownership, multicore shootdown or Linux boot |
| QEMU system-mode FPU differential | `SOC_INTEGRATED` | `make qemu-system-fpu-single-differential-gate qemu-system-fpu-double-differential-gate qemu-system-fpu-cu1-exception-differential-gate` | `build/isa_ref/qemu_system_fpu_{single,double}_differential/`, `build/isa_ref/qemu_system_fpu_cu1_exception_differential/` | Opt-in single- and selected double-precision CP1 plus CU1-disabled CpU exception compare through the mailbox boundary; complete IEEE-754, OS FPU context and full ISA compliance remain open |
| FPU doubleword memory round-trip | `SYSTEM_DIFFERENTIAL`, opt-in | `make fpu-double-gate qemu-system-fpu-double-differential-gate` | `build/soc_test/fpu_double/sim.log`, `build/isa_ref/qemu_system_fpu_double_differential/completion_report.md`, `qemu/trace_compare.log` | Real CPU/SoC `SDC1` then `LDC1` verifies both words of an even FPR pair through the blocking data path; the fresh custom-QEMU/RTL retire differential passes. This is a selected two-beat memory slice, not complete COP1, IEEE-754, FPU OS ABI or full ISA closure |
| APB bit-pattern sweep isolation | `SOC_INTEGRATED` | `make phase3-regression` | `build/uvm/phase3_directed/test_logs/5_soc_apb_bit_pattern_sweep_test_seed_1.log`, `build/firmware/apb_bit_sweep_idle/firmware.hex` | The dedicated idle guest prevents CPU peripheral writes from racing the GPIO/timer directed sweep; this verifies APB register patterns, not multi-master arbitration stress |
| P1 current RTL/simulation extension bundle | `CONTRACT_CLOSED` | `make p1-current-complete` | `build/p1_complete/p1_completion_report.md` and dependent `build/unit_tb/`, `build/soc_test/`, `build/cpu_mmu_complete/` reports | Aggregate covers the current RTL/simulation slice only; full MESI/directory coherency, full ISA/FPU, Linux/OS boot and production software policy remain open |
| AXI/APB/reset/cache/TLB-resource SVA simulation gate | `SIM_ASSERT_CLOSED` | `make sva-gate` plus `SVA_ENABLE=1 make l1-nonblocking-cpu-two-error-reset-gate` | `build/sva/soc_smoke/compile.log`, `build/sva/soc_smoke/sim.log`, `build/sva/axi_sram/compile.log`, `build/sva/axi_sram/sim.log`, `build/sva/reset_sync/compile.log`, `build/sva/reset_sync/sim.log`, `build/soc_test/l1_nonblocking_cpu_two_error_reset/sim.log`, `build/sva/sva_completion_report.md` | VCS simulation assertions cover AXI payload/response stability, current single-outstanding burst termination, APB setup/wait/error behavior, reset_sync AASD, the default blocking D-cache known-state/refill bound (4096 cycles under SoC backpressure), 32-source VIC priority/IRQ/vector selection, main-TLB I/D multi-hit count consistency, the opt-in L1 maintenance idle/hold guard, and L1 response/MSHR/writeback bounds plus reset flush; no formal, CDC/RDC, lint, or product-level coverage claim |
| Verification foundation tool audit | `IMPLEMENTED` | `make verification-foundation-gate` | `build/verification_foundation/tool_inventory.tsv`, `verification_foundation_report.md` | Required SVA/formal assets and waiver scope are audited; current environment reports VCS available and formal/CDC/RDC/lint tools missing. This is infrastructure/tool-status evidence, not formal or commercial signoff |
| COP0 shadow-register-set default boundary | `UNIT_VERIFIED` | `make mips-control-cp0-gate`; `make rtl-frontend-compile RUN_ROOT=build/unit_tb/rtl_frontend_srs_boundary` | `build/unit_tb/mips_control_cp0/sim.log`, `build/unit_tb/rtl_frontend_compile/*/` | With default `SOC_SRS_ENABLE=0`, explicit RDPGPR (`rs=0x0a`) and WRPGPR (`rs=0x0e`) encodings raise RI and have no side effects; legacy behavior is preserved |
| COP0 shadow-register-set software-selected subset | `SOC_INTEGRATED`, opt-in | `make mips-control-srs-gate mips-regfile-srs-gate srs-gate` | `build/unit_tb/mips_control_srs/sim.log`, `build/unit_tb/mips_regfile_srs/sim.log`, `build/soc_test/srs/srs_completion_report.md`, `build/soc_test/srs/sim.log` | `SOC_SRS_ENABLE=1` verifies decoder fixed-field rejection, sixteen banked GPR storage, `SRSCtl.PSS` selection, `WRPGPR` writeback and `RDPGPR` readback through the real CPU/SoC and mailbox. Exception entry/ERET is covered by `srs-exception-gate`; scheduler context and Linux SRS ABI are tracked separately |
| COP0 shadow-register-set exception entry/return | `SYSTEM_DIFFERENTIAL`, opt-in | `make srs-exception-gate qemu-system-srs-exception-differential-gate` | `build/soc_test/srs_exception/srs_exception_completion_report.md`, `build/isa_ref/qemu_system_srs_exception_differential/completion_report.md`, `qemu/trace_compare.log` | `SOC_SRS_ENABLE=1` firmware selects `PSS=1/ESS=1`, verifies exception entry as `CSS=1/PSS=0`, reads the pre-exception bank through `RDPGPR`, verifies `ERET` restores `CSS/PSS=0`, and compares 42 retire records against QEMU `mips32-soc-ref`. IP-based SRSMap and nested-fault policy are covered by dedicated gates; external VEIC/EICSS mode, Linux SRS ABI and scheduler ownership remain open |
| COP0 shadow-register-set scheduler context | `SOC_INTEGRATED`, opt-in | `make srs-scheduler-context-gate` | `build/unit_tb/cpu_scheduler_srs/sim.log` | Scheduler context images preserve all 16×32 shadow GPR banks, target CSS/PSS/ESS, active GPR/FPR/FCSR, ASID and LL/SC cancellation across a task switch. Linux scheduler ABI and lazy SRS ownership remain open |

## Evidence Rules

1. Every new gate must have a reproducible Make target and write `compile.log`
   and `sim.log` below `build/`.
2. The command, expected success marker, scope, and residual risk must be
   recorded in `docs/functional_completeness_plan.md` or this registry.
3. Coverage exclusion warnings are tracked separately and are not functional
   closure evidence.
4. Current conclusions are limited to RTL frontend, unit, firmware, and SoC
   simulation. They do not imply complete OS/Linux boot, full ISA compliance,
   or production software policy.

| QEMU system MDU RTL retire differential | `SYSTEM_DIFFERENTIAL`, opt-in | `make qemu-system-mdu-differential-gate` | `build/isa_ref/qemu_system_mdu_differential/completion_report.md`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl`, `qemu/trace_compare.log`, `tb/isa_ref/test_qemu_system_state_to_jsonl.py` | Real CPU-visible `MULT/DIV`, `MADD/MADDU/MSUB/MSUBU`, `MUL`, `MFHI/MFLO` firmware is compared one retire at a time against `mips32-soc-ref`; the converter regression verifies SPECIAL2 accumulate operations have no GPR destination while `MUL/CLZ/CLO` write `rd`. Strict comparison passes through the mailbox boundary. Radix/Booth latency, low-latency division and workload performance remain open |
| COP0 SRSMap software state and mapped interrupt entry | `SYSTEM_DIFFERENTIAL`, opt-in | `make srs-map-gate qemu-system-srs-map-differential-gate` | `build/unit_tb/cp0_srs_map/sim.log`, `build/isa_ref/qemu_system_srs_map_differential/completion_report.md`, `qemu/trace_compare.log` | CP0 `(12,3)` implements all eight 4-bit Cause.IP mappings; a real VIC source on Cause.IP2 maps to CSS=3 with PSS capture on both RTL and QEMU system-mode retire traces. Synchronous ESS entry and ERET are covered separately. External VEIC/EICSS mode, Linux SRS ABI and scheduler ownership remain open |
| COP0 nested SRS exception policy | `SYSTEM_DIFFERENTIAL`, opt-in | `make srs-nested-gate qemu-system-srs-nested-differential-gate` | `build/soc_test/srs_nested/srs_nested_completion_report.md`, `build/isa_ref/qemu_system_srs_nested_differential/completion_report.md`, `qemu/trace_compare.log` | A nested synchronous `SYSCALL` while EXL is set redirects to the handler without replacing CSS/PSS or the original exception context; RTL and QEMU system-mode retire traces compare through mailbox completion. External VEIC/EICSS mode, Linux SRS ABI and scheduler ownership remain open |
| QEMU system UHI DTB handoff | `SYSTEM_INTEGRATED`, opt-in | `make qemu-system-uhi-dtb-gate` | `build/linux_boot/uhi_dtb/completion_report.md`, `firmware_build.log`, `qemu_stdout.log`, `qemu_stderr.log` | `mips32-soc-ref` loads an opaque `-dtb` blob below the guest RAM limit and resets the MIPS guest with UHI `a0=-2` and kseg0 `a1`; the guest verifies the FDT magic before writing the success mailbox. This closes the QEMU handoff contract only; Linux platform DTS, U-Boot image loading, kernel build/boot and RTL system-mode Linux differential remain open |
| Generic Linux userspace VM, fork/exec/wait pressure | `SYSTEM_INTEGRATED`, opt-in | `make linux-boot-build-gate` or `QEMU_TIMEOUT=300s make qemu-system-architecture-closure-gate` | `build/linux_boot/real/completion_report.md`, `build/isa_ref/qemu_system_architecture_closure/completion_report.md`, child logs, `build.log`, `qemu_stdout.log`, `qemu_stderr.log` | Official Linux v6.6 generic MIPS32r2 little-endian kernel and project DTS boot on `mips32-soc-ref`; initramfs `/init` touches four page-spaced user-stack locations, faults in five anonymous `mmap2` pages, changes that mapping read-only/read-write with `mprotect` while reading all five pages, verifies a child write terminates with `SIGSEGV`, maps/reads one file-backed `/bin/vm_child` page, unmaps both regions, expands and restores the process heap with `brk` while touching five heap pages, sleeps for 1 ms through `nanosleep`, explicitly yields with `sched_yield`, forks two children, execve's `/bin/vm_child` in each child, observes two new-image markers, and the parent successfully reaps both with `wait4` while checking both normal exit statuses are 0. The aggregate was freshly rerun with a 300-second timeout after the QEMU rebuild/cache validation repair. This is bounded VM/process/scheduler/timer/permission pressure, not full Linux VM, page-table ownership or demand-paging signoff. Long host timeout remains a residual risk; U-Boot, real-device boot and RTL system-mode Linux differential remain open |
| Official U-Boot MIPS32LE build prerequisite | `IMPLEMENTED`, opt-in | `make linux-uboot-build-gate` | `build/linux_boot/uboot/completion_report.md`, `config.log`, `build.log`, `artifacts.sha256` | Pinned U-Boot v2024.10 source builds with `maltael_defconfig` and the project MIPS32LE cross-toolchain. This is not a custom-machine boot: Malta board support, QSPI/DDR loading, SoC-specific port and Linux handoff remain open |
| QEMU custom-machine U-Boot compatibility probe | `INCOMPLETE`, opt-in | `make linux-uboot-custom-machine-probe` | `build/linux_boot/uboot_custom_probe/completion_report.md`, `qemu_trace.log`, `qemu_stdout.log`, `qemu_stderr.log` | A separate `CONFIG_SKIP_LOWLEVEL_INIT` Malta image reaches `board_init_f` through the kseg1 flash alias and opt-in legacy revision/UART endpoints. It still stops in Malta board initialization; no SoC U-Boot port, DDR/QSPI boot, Linux handoff or boot prompt is claimed |
| QEMU system architecture closure aggregate | `SYSTEM_INTEGRATED`, opt-in | `make qemu-system-architecture-closure-gate` | `build/isa_ref/qemu_system_architecture_closure/completion_report.md`, child logs and reports | Serial aggregate passes the current peripheral contract, selected ISA/FPU/privileged and peripheral RTL/QEMU retire differential, MMU refill/PageMask/OS-pressure, LL/SC reservation differential and generic Linux kernel-to-userspace marker. This is a bounded architecture integration gate; full ISA/IEEE-754/OS VM, full RTL Linux differential, physical device timing and formal/CDC/RDC/lint signoff remain open |
| QEMU system LL/SC RTL retire differential | `SYSTEM_DIFFERENTIAL`, opt-in | `make qemu-system-llsc-differential-gate` | `build/isa_ref/qemu_system_llsc_differential/completion_report.md`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl`, `qemu/trace_compare.log` (`TRACE_COMPARE_PASS records=293`) | Real RTL and `mips32-soc-ref` compare LLAddr virtual-address visibility, successful SC, unreserved SC, ordinary-store invalidation, exception-cleared reservation and the completion mailbox one retire at a time. QEMU's custom hook exposes the RTL virtual LLAddr contract; the converter reconstructs attempted SC address/data when QEMU fast-fails without a memory callback. Full MIPS memory ordering, arbitrary atomic interleavings, MESI/directory and Linux atomic ABI remain open |
| L1 nonblocking CPU/D-cache opt-in contract | `CONTRACT_CLOSED`, opt-in | `make l1-nonblocking-cpu-complete-gate` | `build/soc_test/l1_nonblocking_cpu_complete/l1_nonblocking_cpu_completion_report.md`; child `sim.log`/reports under `build/soc_test/l1_nonblocking_cpu_{compat,multi,stress,error,two_error,error_reset,two_error_reset}` and `build/unit_tb/cache_concurrency/l1nb_maintenance_compat` | Real CPU/D-cache path runs with two MSHRs and ROB-tagged late responses: compatibility, multi-request, three-seed reset stress, single/two-error recovery, reset-in-flight, and CACHE invalidate/writeback maintenance compatibility all pass. Uncached/peripheral traffic and unsupported tag operations remain legacy-owned; default blocking path, full nonblocking coherency and physical DDR error timing remain outside this bounded contract |
| L1 nonblocking peer snoop and store broadcast | `SOC_INTEGRATED`, opt-in | `make l1-nonblocking-gate l1-nonblocking-cpu-multi-gate dual-core-soc-gate rtl-frontend-compile` | `build/unit_tb/cache_concurrency/l1nb/sim.log`, `build/soc_test/l1_nonblocking_cpu_multi/sim.log`, `build/soc_test/dual_core/sim.log`, `build/unit_tb/rtl_frontend_compile/` | Opt-in L1 snoops invalidate clean lines, enqueue dirty lines into the four-entry writeback queue, retain snoops colliding with an active refill, and let the snoop invalidate win over the refill update. Local stores broadcast their line address through the CPU/AXI adapter. The default blocking path remains unchanged; directory ownership, arbitrary multicore ordering, full L2 coherency and physical cache signoff remain open |
| QEMU system-mode MMU refill RTL retire differential | `SYSTEM_DIFFERENTIAL`, opt-in | `make qemu-system-mmu-refill-differential-gate` | `build/isa_ref/qemu_system_mmu_refill_differential/completion_report.md`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl`, `qemu/trace_compare.log` | Fresh `TRACE_COMPARE_PASS records=3288` covers software-managed 4KB refill, permission fault, ERET retry, 64-entry TLB indexing and mailbox completion. Full privileged/MMU compliance, OS VM ownership, larger-page demand paging, multicore shootdown, Linux boot and physical signoff remain open |
| QEMU system MMU PageMask RTL retire differential | `SYSTEM_DIFFERENTIAL`, opt-in | `make qemu-system-mmu-pagemask-gate` | `build/isa_ref/qemu_system_mmu_pagemask_differential/completion_report.md`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl`, `qemu/trace_compare.log` | Fresh `TRACE_COMPARE_PASS records=276` compares the same firmware through the RTL-compatible BFC00200 boot-ROM vector, covering 4KB/16KB/64KB/256KB PageMask phases, ASIDs 4-7, even/odd halves, PFN folding and the explicit PageMask completion marker. Full privileged/MMU compliance, Linux VM ownership, multicore shootdown and physical signoff remain open |
| QEMU system MMU context/shootdown RTL retire differential | `SYSTEM_DIFFERENTIAL`, opt-in | `make qemu-system-mmu-contract-gate` | `build/isa_ref/qemu_system_mmu_contract/completion_report.md`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl`, `qemu/trace_compare.log` | Fresh `TRACE_COMPARE_PASS` compares ASID-specific refill, ASID reuse, wired APB mapping, sticky invalidate+done status, architectural shootdown and post-shootdown refill through the RTL-compatible BFC00200 vector. The custom QEMU machine invalidates matching non-global architectural TLB entries at ASID-scope ACK; OS page-table ownership, multicore IPI shootdown, Linux VM and full privileged/MMU remain open |
| QEMU system MMU OS page-table pressure differential | `SYSTEM_DIFFERENTIAL`, opt-in | `make qemu-system-mmu-os-pressure-gate` | `build/isa_ref/qemu_system_mmu_os_pressure/completion_report.md`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl`, `qemu/trace_compare.log` | Fresh RTL/QEMU retire comparison passes the three-ASID workload: independently owned root/L2 tables, same virtual pages backed by task-specific physical pages, task switching, TLB pair-state reset and explicit post-shootdown demand refill. This closes bounded single-core OS-style page-table ownership pressure, not a Linux VM, production allocator, scheduler ABI or multicore shootdown |
| MMU OS-pressure bounded aggregate | `CONTRACT_CLOSED`, opt-in | `make mmu-os-pressure-complete-gate` | `build/soc_test/mmu_os_pressure_complete/mmu_os_pressure_completion_report.md` plus child RTL/QEMU reports | Aggregate executes the software-managed refill, ASID process pressure, PageMask, shootdown protocol pressure, and RTL/QEMU system differential gates. It closes the bounded single-core OS-style page-table/shootdown evidence set; multicore IPI ownership, Linux VM ABI, arbitrary demand paging and full privileged/MMU compliance remain outside the contract |
| Dual-core shootdown target-consumption ACK | `SOC_INTEGRATED`, opt-in | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M RUN_DIR=/tmp/mips32-dual-core-ack-real2 make dual-core-soc-gate`; `make tlb-invalidate-gate` | `build/soc_test/dual_core/sim.log`, `build/soc_test/dual_core/compile.log`, `build/unit_tb/tlb_invalidate/sim.log` | The dual-core SoC path routes the mailbox ACK from the selected target CPU's synchronous TLB invalidate commit boundary: core 0 for target 0 and core 1 for target 1. TLB writes have priority and cannot be acknowledged as invalidates; the TLB unit negative test covers the same-cycle write/invalidate collision. The existing firmware passes target/reverse routing, timeout, stale ACK, busy rejection, reset control and success mailbox. Arbitrary multicore OS shootdown ownership and full Linux SMP remain open |
| L1 nonblocking DDR RTL/QEMU differential | `SYSTEM_DIFFERENTIAL`, opt-in | `make l1-nonblocking-ddr-gate qemu-system-l1-ddr-differential-gate` | `build/soc_test/l1_ddr_nonblocking/l1_ddr_nonblocking_report.md`, `build/isa_ref/qemu_system_l1_ddr_differential/completion_report.md`, `qemu/trace_compare.log` | RTL and `mips32-soc-ref` compare through mailbox retirement with two stores, same-line access, second-line refill and final hit. A pre-ROB stall/duplicate-allocation bug found by the probe was fixed and reverified. Full coherence, arbitrary multi-error, maintenance ordering, physical DDR PHY timing and Linux cache ABI remain open |
| QEMU system WDT/boot-status reset contract | `SOC_INTEGRATED`, opt-in | `make qemu-system-wdt-gate` | `build/isa_ref/qemu_system_wdt/completion_report.md`, `qemu_stdout.log`; RTL companion `make wdt-boot-failure-gate` | Reference machine models WDT CTRL/LOAD/VAL/KICK/STATUS, watchdog reset, sticky boot stage/failure/reset-cause retention and W1C recovery; the guest uses the uncached KSEG1 alias for the reference machine's upper APB half. RTL and QEMU firmware gates pass. Physical reset-domain retention, clock/power behavior and board signoff remain open |
| Hardware page-table walker backpressure/reset | `UNIT_VERIFIED` | `make page-table-walker-gate page-table-tlb-refill-gate cpu-hardware-walker-gate cpu-dside-hardware-walker-gate` | `build/unit_tb/page_table_walker/`, `build/unit_tb/page_table_tlb_refill/`, `build/unit_tb/cpu_hardware_walker/`, `build/unit_tb/cpu_dside_hardware_walker/` | The walker holds each memory request through backpressure, reports the existing permission/fault matrix, and cancels an in-flight walk cleanly on reset before accepting a new request. This closes handshake/reset behavior of the bounded two-level walker; arbitrary demand paging, OS VM ownership, Linux and full privileged/MMU compliance remain open |
| MMU IPI shootdown protocol pressure | `UNIT_VERIFIED`, opt-in | `make mmu-ipi-shootdown-pressure-gate` | `build/unit_tb/mmu_ipi_shootdown_pressure/mmu_ipi_shootdown_pressure_report.md`, `sim.log` | RTL exercises 35 shootdown requests, including 32 repeated generation/ASID/VPN invalidations plus busy/rejected, stale-ACK and timeout recovery while preserving request sequencing. This is protocol-pressure evidence only; multicore OS scheduling, page-table ownership and Linux VM integration remain open |
| Hardware page-table walker four-page-size opt-in | `SOC_INTEGRATED`, opt-in | `make page-table-walker-page-sizes-gate cpu-hardware-walker-page-sizes-gate rtl-frontend-compile cpu-hardware-walker-gate cpu-dside-hardware-walker-gate` | `build/unit_tb/page_table_walker_pages/{4K,16K,64K,256K}/`, `build/unit_tb/cpu_hardware_walker_{16K,64K,256K}/`, `build/unit_tb/rtl_frontend_compile/`, `build/unit_tb/cpu_hardware_walker/`, `build/unit_tb/cpu_dside_hardware_walker/` | `PAGE_MASK` now drives L2 index selection, leaf PFN alignment, physical offset construction, TLB PageMask and even/odd selection for 4KB/16KB/64KB/256KB; the architectural masks are `0x0000/0x0003/0x000f/0x003f`, and default `SOC_HARDWARE_WALKER_PAGE_MASK=0` remains unchanged. This is a hardware-walker page-size contract, not complete demand paging, Linux VM ownership, scheduler/shootdown policy or full privileged/MMU compliance |
| Page-table walker SVA protocol checker | `SIM_ASSERT_CLOSED` | `make sva-gate` | `build/sva/soc_smoke/compile.log`, `build/sva/soc_smoke/sim.log` | The unified SVA path checks walker state-known, request acceptance, memory-request hold under backpressure and one-cycle response handoff. Page-table contents, permissions and page-size semantics remain covered by directed walker gates; this is not formal MMU proof |
| RTL Linux progress image reuse/OOM guard | `IMPLEMENTED`, opt-in | `KERNEL=/absolute/path/to/vmlinux make rtl-linux-progress-gate` | `build/linux_boot/rtl_progress_gate/build.log`, `image.log`, `completion_report.md` | Supplying `KERNEL` now reuses the existing kernel image instead of implicitly rebuilding Linux; explicit `SKIP_LINUX_BUILD=1` remains supported. A no-coverage 17M-cycle RTL progress run passes with bounded simulator memory and no userspace success marker. This closes verification resource safety only, not Linux userspace boot or RTL/QEMU Linux differential |
| Asynchronous IRQ branch-delay ownership | `SYSTEM_DIFFERENTIAL`, default path | `make cpu-irq-delay-slot-gate cpu-cp0-gate qemu-system-vic-cpu-differential-gate qemu-system-selected-differential-gate` | `build/soc_test/cpu_irq_delay_slot/sim.log`, `build/soc_test/cpu_cp0_gate/sim.log`, `build/isa_ref/qemu_system_vic_cpu_differential_current3/qemu/trace_compare.log`, `build/isa_ref/qemu_system_selected_differential_current/` | Interrupt Cause.BD propagation now follows the oldest valid flushed pipeline event and accepts a younger ID delay slot only when the MEM->EX->ID PCs form a contiguous chain. This prevents stale delay-slot metadata from marking an older ordinary instruction while preserving a legal three-stage IRQ delay-slot case. Directed IRQ, CPU/CP0, VIC CPU differential (`TRACE_COMPARE_PASS records=736`) and the selected differential aggregate pass; full interrupt timing/OS nesting remains outside this bounded contract |
| RTL Linux return-path stack diagnosis | `BLOCK_VERIFIED`, diagnostic only | `SKIP_COVERAGE=1 ... LINUX_TARGET_TRACE_LINE=4211eb LINUX_TARGET_TRACE_CYCLE_START=18600000 LINUX_TARGET_TRACE_CYCLE_END=18720000 make rtl-linux-progress-gate` | `/tmp/rtl_linux_target_window/sim.log`, `/tmp/rtl_linux_target_window/completion_report.md` | Parameterized target-line/cycle-window traces show a real `sw` writing `0x895b0000` to the `alloc_inode` return-address slot before the later faulting load. Earlier capture proves the normal D-side/WB load path returns correct data. This narrows the open RTL Linux blocker to interrupt/exception recovery stack-frame/control-flow interaction; no userspace boot or full Linux differential claim is made |
| Retire differential resource bound | `IMPLEMENTED` | `make qemu-system-wait-differential-gate` (default `MAX_TRACE_RECORDS=1000000`, `MAX_TRACE_BYTES=268435456`) | `/tmp/mips32-trace-bound-20260829/isa_ref/qemu_system_wait_differential/rtl_gate.log`, `qemu/trace_compare.log`; `git diff --check` | The RTL JSONL sink enforces the configured record limit during simulation, before an unbounded guest can exhaust host storage; the wrapper retains post-run byte/record checks and cleans temporary QEMU capture directories on every exit path. The limit is verification resource safety, not a functional or ISA signoff |
| QEMU system retire capture source bound | `IMPLEMENTED` | `MAX_QEMU_EVENTS=128 MAX_QEMU_STATES=129 REQUIRE_SMOKE_OUTPUT=0 tb/isa_ref/run_qemu_system_retire_capture_gate.sh`; `make qemu-system-retire-capture-gate`; bounded `make qemu-system-linux-differential-gate` | `/tmp/qemu-retire-cap-128/`, `build/isa_ref/qemu_system_retire_capture/`, `build/isa_ref/qemu_linux_differential/qemu/trace_compare.log` | The QEMU plugin now receives `max-records` and stops emitting before a long-running guest can fill the filesystem; the terminal started instruction still flushes, yielding exactly N events and N+1 state boundaries. The 128-record probe converts successfully, normal system smoke capture passes, and a fresh 2M-cycle/50,000-record Linux RTL/QEMU probe compares 49,978 aligned kernel retires. This is bounded differential and resource-safety evidence, not RTL Linux userspace boot or a full-length Linux differential |
| VCS runtime/gate log separation | `IMPLEMENTED` | `SKIP_COVERAGE=1 make cpu-load-return-gate`; `SKIP_COVERAGE=1 make soc-smoke`; `SKIP_COVERAGE=1 make l1-nonblocking-cpu-two-error-reset-gate`; `make rtl-frontend-compile` | `build/soc_test/cpu_load_return/sim.log`, `build/soc_test/smoke/sim.log`, `build/soc_test/l1_nonblocking_cpu_two_error_reset/sim.log`, corresponding `sim_runtime.log` files | `tb/soc_test/run.sh` gives VCS exclusive ownership of `sim_runtime.log`, preserves its exit status, and publishes the complete runtime log to `sim.log` only after simulation exits. This also handles callers that redirect `run.sh` stdout to `sim.log`, eliminating the previous two-writer truncation/interleaving hazard; it does not change RTL behavior or coverage scope |
| RTL Linux no-coverage propagation | `IMPLEMENTED` | `SKIP_COVERAGE=1 RUN_DIR=/tmp/rtl_linux_runner_smoke KERNEL=/tmp/rtl_linux_repro/kernel/vmlinux SKIP_LINUX_BUILD=1 RTL_CYCLE_LIMIT=100000 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl_linux_runner_smoke/completion_report.md`, `/tmp/rtl_linux_runner_smoke/sim/sim.log`, `/tmp/rtl_linux_runner_smoke/sim/sim_runtime.log` | `run_rtl_linux_progress_gate.sh` now forwards the caller's `SKIP_COVERAGE` setting to `tb/soc_test/run.sh`, preventing long Linux probes from unexpectedly creating VCS coverage databases. The 100k-cycle probe passes with post-reset progress and zero userspace markers; Linux userspace boot and RTL/QEMU Linux differential remain open |
| Signed overflow false-positive fix | `SYSTEM_DIFFERENTIAL` (bounded) | `SKIP_COVERAGE=1 make qemu-system-exception-differential-gate`; `SKIP_COVERAGE=1 RUN_DIR=/tmp/rtl_linux_after_overflow_fix KERNEL=/tmp/rtl_linux_repro/kernel/vmlinux SKIP_LINUX_BUILD=1 RTL_CYCLE_LIMIT=20000000 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `build/isa_ref/qemu_system_exception_differential/`, `/tmp/rtl_linux_after_overflow_fix/sim/sim.log`, `/tmp/rtl_linux_after_overflow_fix/completion_report.md` | `mips_cpu` now gates ALU overflow exceptions to encoded `ADD`, `SUB`, and `ADDI`; link instructions such as `JAL` may write `$ra` but cannot raise arithmetic overflow from the ALU's default add path. QEMU/RTL overflow differential passes. The 20M-cycle Linux probe passes post-reset progress and advances beyond the previous `start_kernel` false exception loop, but still has no userspace marker and later reaches a separate `0x0100101c` MMU/address-fault blocker |
| RTL Linux GPR writeback diagnostic | `BLOCK_VERIFIED`, diagnostic only | `LINUX_GPR_TRACE=1 LINUX_GPR_TRACE_REG=4/16 ... tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl_linux_gpr_trace_20260829/sim/late_gpr_full.log`, `/tmp/rtl_linux_gpr_trace_20260829/sim/a0_gpr.log` | Bounded trace proves `$a0` is written as `0x01001000` by `0x889aa6f4: lw a0,4(a0)` at cycle `19808323`, then copied to `$s0` by `0x889f0b5c: or s0,a0,zero`; no context-restore write is observed. This identifies an upstream Linux kernel memory/data contract issue, but does not close RTL Linux userspace boot or justify an RTL semantic change |
| RTL Linux dirty-line ownership diagnostic | `BLOCK_VERIFIED`, diagnostic only | `LINUX_CACHE_OWNER_TRACE=1 LINUX_CACHE_OWNER_TRACE_LIMIT=256 LINUX_TARGET_TRACE_LINE=420108 ... tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl_cache_owner_20260829b/sim/sim_runtime.log`, `/tmp/rtl_cache_owner_20260829b/completion_report.md` | Handshake-only trace proves target line `0x08402100` initially refills zero, then Linux's real `0x889f12e0: sw a1,4(s2)` writes `0x01001000` to word 1; D-cache later writes back and refills the same complete line without data corruption. The Linux userspace boot blocker remains upstream runtime/control-flow or another CPU contract, not an unexplained D-cache writeback source |
| Linux system-mode retire capture plumbing | `IMPLEMENTED`, opt-in | `LINUX_RETIRE_TRACE=1 ... tb/linux_boot/run_rtl_linux_progress_gate.sh`; `REQUIRE_SMOKE_OUTPUT=0 QEMU_KERNEL=... QEMU_DTB=... QEMU_MEMORY=64M tb/isa_ref/run_qemu_system_retire_capture_gate.sh` | `/tmp/rtl_retire_probe_20260829a/sim/rtl_retire.jsonl` (20,000 records), `/tmp/qemu_linux_capture_probe_20260829b/qemu_retire.jsonl`, existing bare-metal `build/isa_ref/qemu_system_retire_capture/` | RTL progress runner now emits a bounded retire JSONL only when explicitly enabled; QEMU capture accepts Linux kernel/DTB/memory/append inputs while preserving bare-metal defaults. Both paths pass bounded capture/conversion checks. This is infrastructure evidence only; Linux userspace boot and full RTL/QEMU Linux differential remain open |
| Linux exception diagnostic trace resource bound | `IMPLEMENTED`, opt-in | `LINUX_EXCEPTION_TRACE_LIMIT=256 ... tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl_linux_exception_limit_20m/completion_report.md`, `/tmp/rtl_linux_exception_limit_20m/sim/sim.log`, `/tmp/rtl_exception_trace_limit_fix/unit_tb/rtl_frontend_compile/` | Added a default 256-record cap to the previously unbounded `LINUX_EXCEPTION_TRACE` stream and forwarded the override from the runner. Isolated RTL frontend compile passes `8/8`; a 20M-cycle no-coverage run with limit 7 produced exactly 7 exception records, a 5.4 KiB simulator log, and a 1.1 MiB VCS data structure. This prevents diagnostic log growth from becoming an OOM/disk-pressure source; it does not close the Linux exception/control-flow or userspace boot blocker |
| Page-table walker unit simulation watchdog | `IMPLEMENTED` | `SIM_TIMEOUT=30 RUN_DIR=/tmp/page-table-walker-timeout-fix-20260902 tb/unit/mmu/run_page_table_walker.sh` | `/tmp/page-table-walker-timeout-fix-20260902/sim.log`, `compile.log` | The unit entry point now bounds simulator wall time (default 30 seconds), reports timeout/runtime failure nonzero, and preserves the log. The normal walker permission/page-size/A-D matrix passes; this contains runaway test processes but is not RTL functional or formal signoff |
| Page-table TLB refill unit simulation watchdog | `IMPLEMENTED` | `SIM_TIMEOUT=30 RUN_DIR=/tmp/page-table-tlb-refill-timeout-fix-20260902 tb/unit/mmu/run_page_table_tlb_refill.sh` | `/tmp/page-table-tlb-refill-timeout-fix-20260902/sim.log`, `compile.log` | The TLB-refill unit entry point now uses the same bounded runtime and nonzero timeout/runtime failure propagation; the functional gate remains unchanged and this is resource containment evidence only |
| Linux exception frame before/after trace | `IMPLEMENTED`, opt-in | `LINUX_EXCEPTION_FRAME_TRACE=1 LINUX_EXCEPTION_FRAME_TRACE_LIMIT=64 ... tb/linux_boot/run_rtl_linux_progress_gate.sh` | `tb/soc_test/tb_mips_soc.v`, `tb/linux_boot/run_rtl_linux_progress_gate.sh`, `build/unit_tb/rtl_frontend_compile/`, `build/soc_test/cpu_irq_delay_slot/`, `build/soc_test/cpu_cp0_gate/` | Adds bounded `LINUX_EXCEPTION_FRAME_BEFORE/AFTER` records across the CP0 clock boundary, including exception/IRQ event metadata and Status/Cause/EPC/ErrorEPC/BadVAddr. Frontend compile passes `8/8`; IRQ delay-slot and CPU/CP0 gates pass with coverage disabled. This is diagnostic evidence only; it does not close RTL Linux userspace boot or full RTL/QEMU Linux differential |
| Linux exception-frame cycle-window capture | `BLOCK_VERIFIED`, diagnostic only | `LINUX_EXCEPTION_FRAME_TRACE=1 LINUX_EXCEPTION_FRAME_TRACE_CYCLE_START=13450000 LINUX_EXCEPTION_FRAME_TRACE_CYCLE_END=13550000 ... tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl_linux_exception_frame_window/completion_report.md`, `/tmp/rtl_linux_exception_frame_window/sim/sim.log` | A 14M-cycle no-coverage RTL Linux probe captures an IRQ in the delay slot at `0x88852ddc`: post-frame `EXL=1`, `EPC=0x88852dd8`, `Cause.BD=1`, followed by ERET clearing EXL. The frame is consistent with the architectural delay-slot contract; no semantic fix is justified. Userspace marker count is 0, so RTL Linux boot and full RTL/QEMU Linux differential remain open |
| Linux exception-frame invariant checker | `IMPLEMENTED`, opt-in | `make linux-exception-frame-check LOG=/path/to/sim.log` | `scripts/check_linux_exception_frame_trace.py`, `/tmp/rtl_linux_exception_frame_window2/sim/sim.log` | Pairs bounded `LINUX_EXCEPTION_FRAME_BEFORE/AFTER` records and checks post-event EXL, EPC delay-slot adjustment, and Cause.BD consistency. The fresh window log passes with `LINUX_EXCEPTION_FRAME_CHECK_PASS pairs=1`; this is an architectural diagnostic checker, not Linux boot or full differential signoff |
| QEMU MMU differential stale-firmware fix | `SYSTEM_DIFFERENTIAL` | `BUILD_DIR=/tmp/qemu_arch_refill_fixed SKIP_COVERAGE=1 make qemu-system-mmu-refill-differential-gate` | `/tmp/qemu_arch_refill_fixed/isa_ref/qemu_system_mmu_refill_differential/qemu/trace_compare.log`, `completion_report.md` | QEMU now consumes the exact run-local ELF rebuilt by the RTL child; isolated rerun passes the MMU refill RTL/QEMU retire differential. This fixes artifact selection, not full privileged/MMU or Linux differential closure |
| Generic Linux wait4 PID selection fix | `SYSTEM_INTEGRATED`, opt-in | `QEMU_TIMEOUT=60 make linux-boot-build-gate` | `build/linux_boot/real/completion_report.md`, `qemu_stdout.log`, `qemu_stderr.log` | Fresh image recheck passes the complete userspace marker set, including the intentional protected-page child `SIGSEGV`, two `execve` children, exact-PID `wait4` reaping and wait-status validation. The Linux gate now uses the architectural SC-consumes-reservation policy; the Linux-only reservation compatibility override had stranded the parent after child termination. This closes the bounded QEMU userspace/wait4 contract only; RTL Linux, Linux VM ownership and full RTL/QEMU differential remain open |
| RTL Linux 100M-cycle delay boundary | `INCOMPLETE`, diagnostic only | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M SKIP_COVERAGE=1 RTL_CYCLE_LIMIT=100000000 HOST_TIMEOUT=600s KERNEL=... SKIP_LINUX_BUILD=1 make rtl-linux-progress-gate` | `/tmp/mips32-rtl-linux-100m/linux_boot/rtl_progress_gate/sim.log`, `/tmp/mips32-rtl-linux-100m/linux_boot/rtl_progress_gate/sim/sim_runtime.log` | The fresh run reaches about 62.5M cycles before host timeout with stable simulator RSS near 158 MiB and remains in Linux `__udelay` at `0x89243530/0x89243534`; no userspace marker is present. This is stronger negative evidence against a short cycle budget, but does not yet distinguish delay calibration, CP0 Count/Compare, timer interrupt, or pipeline throughput. RTL Linux userspace boot and full RTL/QEMU Linux differential remain open |
| RTL Linux delay/CP0 correlation probe | `BLOCK_VERIFIED`, diagnostic only | `SKIP_COVERAGE=1 RTL_CYCLE_LIMIT=20000000 LINUX_DELAY_TRACE=1 LINUX_DELAY_TRACE_START=89243530 LINUX_DELAY_TRACE_END=89243534 LINUX_DELAY_TRACE_LIMIT=40 LINUX_CP0_TRACE_LIMIT=40 ... make rtl-linux-progress-gate` | `/tmp/mips32-rtl-linux-delaydiag/linux_boot/rtl_progress_gate/sim.log`, `/tmp/mips32-rtl-linux-delaydiag/linux_boot/rtl_progress_gate/sim/sim_runtime.log` | The trace shows `v0=0x00106256` decrementing normally through the `__udelay` loop, continuous CP0 Count advancement, Compare writes and expected TI/IRQ transitions. The wrapper's final no-progress status is intentional because heartbeat output was disabled. This rules out a stopped Count/missed Compare as the immediate blocker, but does not close Linux userspace boot or full RTL/QEMU Linux differential |
| Linux TLB-fault neighborhood frame check | `BLOCK_VERIFIED`, diagnostic only | `LINUX_EXCEPTION_FRAME_TRACE=1 LINUX_EXCEPTION_FRAME_TRACE_CYCLE_START=19570000 LINUX_EXCEPTION_FRAME_TRACE_CYCLE_END=19610000 ... tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl_linux_exception_frame_fault/completion_report.md`, `/tmp/rtl_linux_exception_frame_fault/sim/sim.log` | The 20M-cycle probe and checker pass for the accepted delay-slot IRQ at `0x8925dccc`: `EXL=1`, `EPC=0x8925dcc8`, `Cause.BD=1`, `BadVAddr=0xc0000020`. This further rules out immediate CP0 frame corruption but does not close the zero-marker Linux boot blocker |
| Four-task MMU OS-pressure RTL/QEMU differential | `SYSTEM_DIFFERENTIAL`, opt-in bounded slice | `make qemu-system-mmu-os-pressure-gate` | `build/isa_ref/qemu_system_mmu_os_pressure/completion_report.md`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl`, `qemu/trace_compare.log` | Four independently owned ASID/root/L2 contexts touch four demand pages, producing `refills=0x43`, `page_allocs=0x10`, `demand_faults=0x41`, four allocations per task, and post-shootdown refill. Strict QEMU/RTL retire comparison passes. Linux VM ownership, arbitrary demand paging, SMP scheduling and multicore shootdown policy remain open |
| MMU page-table root allocator atomic handoff | `UNIT_VERIFIED` | `make mmu-page-table-allocator-gate rtl-frontend-compile` | `/tmp/mips32-allocator-atomic/unit_tb/mmu_page_table_allocator/sim.log`, `/tmp/mips32-allocator-atomic/unit_tb/rtl_frontend_compile/` | The bounded root lease allocator accepts a valid release and allocation in the same cycle, reuses the released root, and returns the incremented generation token. Full-pool atomic handoff is covered without changing the default MMU-disabled path. OS page-table population, CPU demand paging, scheduler ownership, multicore shootdown and Linux VM remain open |
| MMU ASID allocator atomic handoff | `UNIT_VERIFIED` | `make tlb-asid-allocator-gate mmu-context-contract-gate` | `/tmp/mips32-asid-atomic/unit_tb/tlb_asid_allocator/sim.log`, `/tmp/mips32-asid-atomic/unit_tb/mmu_context_contract/sim.log` | The bounded ASID lease allocator rejects stale releases, accepts valid release, and atomically reuses a full-pool slot with an incremented generation token. Linux ASID lifetime, page-table population, scheduler ownership, multicore shootdown and full privileged/MMU compliance remain open |
| ISA R2 result assertion coverage | `SOC_INTEGRATED` | `make isa-r2-gate` | `build/soc_test/isa_r2_sweep/sim.log`, `build/firmware/isa_r2_sweep/firmware.objdump` | The R2 sweep explicitly checks CLZ=16, CLO=16, positive and false-condition MOVN/MOVZ destination behavior, and nonzero BAL link retirement in addition to its existing WSBH/WSBW/BITSWAP/ALIGN/ROTR/EXT/INS/RDHWR checks. Fresh run passes `REGRESSION_TEST_SUCCESS` with `CPU_CP0_SUMMARY ... ri=0`; this strengthens the implemented subset only and does not claim full ISA compliance |

| Strict RTL Linux userspace criterion | `INCOMPLETE`, opt-in | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real_lpj_fix/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=100000 HOST_TIMEOUT=180s RUN_DIR=build/linux_boot/rtl_userspace_gate_smoke make rtl-linux-userspace-gate` | `build/linux_boot/rtl_userspace_gate_smoke/completion_report.md`, `sim/sim.log` | The new top-level wrapper correctly forwards `LINUX_REQUIRE_USERSPACE=1`; the bounded RTL simulation completes, then returns nonzero solely because the `MIPS32_SOC_LINUX_BOOT_SUCCESS` marker count is 0. This closes criterion enforcement, not RTL Linux userspace boot or full RTL/QEMU system-mode differential |

| RTL Linux WAIT/CP0 timer correlation | `BLOCK_VERIFIED`, diagnostic only | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real_lpj_fix/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=15000000 LINUX_WAIT_TRACE=1 LINUX_WAIT_TRACE_LIMIT=40 LINUX_CP0_TRACE_LIMIT=128 make rtl-linux-progress-gate` | `build/linux_boot/rtl_linux_wait_edge_trace2/completion_report.md`, `sim/sim.log` | The bounded trace shows WAIT retirement at `0x8800237c`, `wait_state` entry, Count crossing Compare, TI/IP7 and interrupt acceptance, followed by ERET to `0x88002380`; Linux re-enters the idle wait loop. This rules out a stopped Count/missed Compare/WAIT wakeup in this run, but userspace boot and full RTL/QEMU Linux differential remain open |
| L1 nonblocking CPU aggregate fresh recheck | `SYSTEM_DIFFERENTIAL`, opt-in | `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M make l1-nonblocking-cpu-complete-gate` | `build/soc_test/l1_nonblocking_cpu_complete/l1_nonblocking_cpu_completion_report.md`; child `sim.log` files under `build/soc_test/l1_nonblocking_cpu_{compat,multi,stress,error,two_error,error_reset,two_error_reset,maintenance}` | Real CPU/D-cache opt-in path passes compatibility, multi-request, three-seed reset stress, single/two AXI response-error recovery, reset-in-flight and maintenance CPU gates under the cgroup memory budget. This revalidates the existing `l1_cache_nb_cpu_axi` integration; uncached/peripheral and unsupported maintenance traffic remains on legacy dcache, while full coherency, arbitrary ordering and Linux cache ABI remain open |
| L1/L2 nonblocking system differential | `SYSTEM_DIFFERENTIAL`, opt-in | `SKIP_COVERAGE=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M make qemu-system-l1-l2-nonblocking-differential-gate` | `build/isa_ref/qemu_system_l1_l2_nonblocking_differential/completion_report.md`, `rtl/rtl_retire.jsonl`, `qemu/qemu_retire.jsonl`, `qemu/trace_compare.log` | The same RTL system selects L1 nonblocking, CPU ROB, DDR window and L2 nonblocking write-back; the `qemu_system_l1_ddr` workload passes strict mailbox-boundary retire comparison against `mips32-soc-ref`. This is a bounded combined-path result; full MESI/directory coherency, arbitrary downstream AXI ordering, error/reset interleavings and Linux cache ABI remain open |
| RTL Linux `__register_sysctl_init` return boundary | `BLOCK_VERIFIED`, diagnostic only | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=14000000 LINUX_PC_TRACE=1 LINUX_PC_TRACE_RETIRE_ONLY=1 LINUX_PC_TRACE_START=88d05b30 LINUX_PC_TRACE_END=88d05d80 LINUX_PC_TRACE_CYCLE_START=10000000 LINUX_PC_TRACE_CYCLE_END=14000000 LINUX_PC_TRACE_LIMIT=512 tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl-linux-register-sysctl-20260902/completion_report.md`, `sim/sim.log` | Focused retire trace observes multiple `__register_sysctl_init` invocations and normal returns through `0x88d05b90`; the `init_security_keys_sysctls` caller returns as well. This rules out a local stuck registration function in the observed window, but does not close later initcall completion, scheduler/init-task handoff, RTL userspace or full RTL/QEMU differential |
| QEMU architecture aggregate fresh recheck | `INCOMPLETE`, opt-in | `SKIP_COVERAGE=1 QEMU_TIMEOUT=120 QEMU_BUILD_JOBS=2 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/qemu-architecture-final-20260902 make qemu-system-architecture-closure-gate` | `/tmp/qemu-architecture-final-20260902/isa_ref/qemu_system_architecture_closure/`, `/tmp/qemu-architecture-final-20260902/linux_boot/real/completion_report.md` | All bounded selected differential, MMU, FPU and LL/SC sub-gates pass. The final Linux userspace gate reaches `/init` and the boot marker, then observes the expected protected-page child SIGSEGV at `0x00400474` but no mmap marker before timeout; the aggregate therefore fails honestly. RTL Linux userspace, full RTL/QEMU Linux differential, full ISA/MMU/OS semantics and product signoff remain open |
| Linux signal/child-reap boundary recheck | `INCOMPLETE`, opt-in | `QEMU_TIMEOUT=120s RUN_DIR=/tmp/linux-anywait-20260902 tb/linux_boot/run_linux_boot_gate.sh` plus aggregate recheck | Fresh logs show strict mode stops after the protected-page SIGSEGV; `linux-guest=on` reaches mmap/mprotect, brk, sleep, yield and both exec markers but the parent remains in the second `wait4`. WNOHANG/yield polling did not resolve the boundary and all experiments were reverted. Linux signal/reap, RTL Linux userspace and full RTL/QEMU Linux differential remain open |
| QEMU Linux userspace wait4 policy runner correction | `SYSTEM_INTEGRATED`, opt-in | `QEMU_TIMEOUT=180 RUN_DIR=/tmp/mips32-linux-recheck-linuxguest-20260902 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux DTB=build/linux_boot/real/mips32_soc_ref.dtb tb/linux_boot/run_linux_boot_gate.sh` | `/tmp/mips32-linux-recheck-linuxguest-20260902/completion_report.md`, `qemu_stdout.log`, `qemu_stderr.log` | The generic Linux userspace runner selects `mips32-soc-ref,linux-guest=on`, the custom QEMU policy for Linux's wait4 LL/SC retry path. The fresh run passes boot, mmap/mprotect, protected-write SIGSEGV, brk, sleep, yield, two exec children, exact-PID wait4 reaps and wait-status validation. Bare-metal RTL/QEMU differential gates retain strict SC-consumption semantics; RTL Linux userspace and full RTL/QEMU Linux differential remain open |
| RTL Linux kernel-init handoff trace | `BLOCK_VERIFIED`, diagnostic only | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=13000000 LINUX_PC_TRACE=1 LINUX_PC_TRACE_SYMBOL=kernel_init_freeable LINUX_MODE_TRACE=1 LINUX_EXCEPTION_TRACE=1 ... tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl-linux-initfreeable-trace-20260902/sim/sim.log`, `completion_report.md` | The real RTL executes `kernel_init_freeable` initcall/setup code through the 11.2M-cycle window, then reaches idle `r4k_wait` around 11.8M. No `run_init_process`, `ret_from_kernel_thread`, kernel-to-user ERET or userspace marker is observed; timer IRQ/WAIT wakeup continues. This narrows the RTL Linux blocker to an initcall/completion wait or kernel-init handoff before exec, and does not justify an RTL change yet |
| RTL Linux `driver_init`/devtmpfs return trace | `BLOCK_VERIFIED`, diagnostic only | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=12200000 LINUX_PC_TRACE=1 LINUX_PC_TRACE_START=88d10aac LINUX_PC_TRACE_END=88d10bf4 LINUX_MODE_TRACE=1 ... tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl-linux-devtmpfs-trace-20260902/sim/sim.log`, `completion_report.md` | The trace reaches `driver_init`, enters `devtmpfs_init`, and observes `vfs_kern_mount` returning `-EINVAL`; `devtmpfs_init` propagates that error and the trace leaves the function. No RTL hang or architectural mismatch is proven at this return boundary. The next investigation must follow subsequent `do_basic_setup` initcalls/completion waits; RTL Linux userspace remains open |
| RTL Linux completion primitive return trace | `BLOCK_VERIFIED`, diagnostic only | `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=12200000 LINUX_PC_TRACE=1 LINUX_PC_TRACE_START=88a58788 LINUX_PC_TRACE_END=88a58844 LINUX_MODE_TRACE=1 ... tb/linux_boot/run_rtl_linux_progress_gate.sh` | `/tmp/rtl-linux-completion-trace-20260902/sim/sim.log`, `completion_report.md` | Multiple `wait_for_completion*` calls enter and return in the real RTL run; observed callers include `__kthread_create_on_node` and `kthread_park`. No kernel-init caller is observed stuck in the primitive, so no completion/WAIT/CP0 RTL change is justified. The remaining Linux blocker is later `do_basic_setup` initcall progress or kernel-init handoff |
| RTL Linux progress-gate configuration guard and freeable-tail probe | `IMPLEMENTED`, diagnostic | `bash -n tb/linux_boot/run_rtl_linux_progress_gate.sh`; negative `LINUX_REQUIRE_PROGRESS=1 LINUX_PROGRESS_TRACE=0 ...` (status 2); `SKIP_COVERAGE=1 SKIP_LINUX_BUILD=1 KERNEL=build/linux_boot/real/kernel/vmlinux RTL_CYCLE_LIMIT=100000 ... tb/linux_boot/run_rtl_linux_progress_gate.sh`; bounded 40M retire-only probe | `/tmp/rtl-linux-invalid-progress-config-20260902.log`, `/tmp/rtl-linux-progress-config-smoke-20260902-reuse/completion_report.md`, `/tmp/rtl-linux-freeable-tail-20260902/sim/sim.log` | The runner rejects an impossible evidence configuration before expensive image/simulator work. The reuse-kernel smoke passes with simulator status 0. The 40M-cycle probe retires through `kernel_init_freeable` after the initcall loop and calls `driver_init` without a CPU/CP0/WAIT/cache mismatch; userspace marker remains absent. RTL Linux userspace and full RTL/QEMU system differential remain OPEN |
| L2 nonblocking cross-ID out-of-order completion | `UNIT_VERIFIED`, opt-in | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/l2-ooo-20260902 make l2-nonblocking-downstream-gate` | `/tmp/l2-ooo-20260902/unit_tb/l2nb_downstream/sim.log` | The two-slot downstream responder intentionally drains the first accepted RID to `RLAST` before serving the second RID. The scoreboard verifies both complete lines and zero beats from the second RID before the first completion; the gate passes with `reads_checked=61`, `errors_checked=8`, `peak_downstream=3`, `id_switches=12`, and `wb_refill_overlap=1`. This closes bounded cross-ID completion ordering for clean refills; arbitrary AXI ordering, concurrent dirty writeback, MESI/directory coherency and full cache signoff remain open |
| QEMU selected differential aggregate current recheck | `SYSTEM_DIFFERENTIAL`, opt-in | `SKIP_COVERAGE=1 QEMU_TIMEOUT=180 QEMU_BUILD_JOBS=1 VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/qemu-selected-recheck-20260902 make qemu-system-selected-differential-gate` | `/tmp/qemu-selected-recheck-20260902/isa_ref/qemu_system_selected_differential/completion_report.md` and child logs | Fresh serial aggregate passes ISA audit/R2, MDU, branch/exception, DI/EI/WAIT, BD, unaligned, peripheral/VIC, FPU single/double/rounding/CU1, DMA SG/fault/reset, VIC full sources, MMU IPI and combined L1/CPU-ROB/DDR/L2 nonblocking retire differential. This revalidates selected bounded contracts; full ISA/MMU/IEEE-754/Linux VM, unrestricted Linux differential, complete coherency and physical signoff remain open |
| RTL Linux interrupt branch-delay EPC fix | `BLOCK_REDUCED`, diagnostic/system-integrated | `SKIP_COVERAGE=1 RUN_DIR=/tmp/rtl-linux-epc-fix-20260902 SKIP_LINUX_BUILD=1 KERNEL=/tmp/linux-singlecore-real-20260902/kernel/vmlinux RTL_CYCLE_LIMIT=7300000 LINUX_PC_TRACE_START=889a9858 LINUX_PC_TRACE_END=889a9878 LINUX_EXCEPTION_TRACE=1 tb/linux_boot/run_rtl_linux_progress_gate.sh`; follow-up 8M-cycle direct run | `/tmp/rtl-linux-epc-fix-20260902/sim.log`, `/tmp/rtl-linux-epc-fix-20260902/epc_fix_8m.log`, `/tmp/rtl-linux-epc-fix-20260902/completion_report.md`, `build`-independent `/tmp/mips32-irq-epc-fix-20260902` gate logs | When an asynchronous IRQ was accepted with a control transfer in WB and its delay slot in MEM, `except_pc` incorrectly used the branch PC and CP0 applied the BD adjustment a second time, returning to the preceding `lbu`. The fix supplies `mem_pc` for that synthetic delay-slot case. The real RTL trace now records the delay-slot PC `0x889a9874`, CP0 resumes with EPC `0x889a9870`, and the post-boundary 8M-cycle run has no `code=2`/`BadVAddr=0x65` recurrence. `cpu-irq-delay-slot-gate`, `cpu-cp0-gate`, and RTL frontend `8/8` pass. This removes one confirmed control-flow blocker; RTL Linux userspace boot and full RTL/QEMU Linux differential remain open |
| APB MMU root lease ownership | `SOC_INTEGRATED`, opt-in | `RUN_DIR=/tmp/mmu-context-root-20260902b tb/unit/tlb/run_mmu_context_status.sh` | `/tmp/mmu-context-root-20260902b/{compile.log,sim.log}` | `apb_mmu_context_status` now exposes the four-slot page-table root allocator at APB `0x28..0x38`. The directed gate passes root allocation, generation readback, stale-release rejection, valid release, sticky event/W1C behavior, and generation-incremented reuse. This closes the bounded hardware-visible root ownership slice; general OS allocator policy, unrestricted demand paging, Linux VM ownership, multicore shootdown and full privileged/MMU signoff remain OPEN |
| APB atomic MMU context lease | `SOC_INTEGRATED`, opt-in | `RUN_DIR=/tmp/mmu-context-atomic-20260902c tb/unit/tlb/run_mmu_context_status.sh` | `/tmp/mmu-context-atomic-20260902c/{compile.log,sim.log}` | Added `mmu_context_allocator` and APB `0x3c` combined allocate/release. The directed gate passes four root/ASID pairs, pool ownership, matching-token release, duplicate-release rejection, and generation-incremented reuse. This closes atomic bounded context ownership; OS allocator policy, unrestricted demand paging, Linux VM ownership, multicore shootdown and full privileged/MMU signoff remain OPEN |
| APB generation-aware shootdown ACK | `SOC_INTEGRATED`, opt-in | `RUN_DIR=/tmp/mmu-context-shootdown-gen-20260902b tb/unit/tlb/run_mmu_context_status.sh` | `/tmp/mmu-context-shootdown-gen-20260902b/{compile.log,sim.log}` | APB shootdown now snapshots the selected context generation at request time and accepts an ACK only when `pwdata[15:8]` matches; a stale ACK is rejected and sets status bit 5. The directed gate passes stale rejection, valid completion, timeout and existing context/root tests. This closes the bounded single-core ownership guard; multicore OS shootdown, scheduler policy, Linux VM and full privileged/MMU signoff remain OPEN |
| P1 current RTL/simulation aggregate fresh recheck | `SOC_INTEGRATED`, opt-in | `source /etc/profile.d/modules.sh && module load vcs && VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M SKIP_COVERAGE=1 BUILD_DIR=/tmp/p1-current-20260903 make p1-current-complete` | `/tmp/p1-current-20260903/p1_completion_report.md` and child `compile.log`/`sim.log` files | Fresh aggregate passes RTL frontend, dual-core/coherency/shootdown, MMU walker/refill/page sizes/A-D recovery, scheduler/timer/IPI, SECDED, finite VEIC, ISA R2 implemented subset, CPU/MMU and DDR4 closure gates. The memory-bounded run does not close full ISA/IEEE-754, unrestricted Linux VM/userspace RTL, arbitrary coherency, formal/CDC/RDC/lint or physical product signoff |
| RTL Linux spurious-IRQ delay diagnosis | `BLOCK_VERIFIED`, diagnostic only | `LINUX_PROFILE=rtl-minimal RTL_CYCLE_LIMIT=60000000 LINUX_PROGRESS_TRACE=1 ... tb/linux_boot/run_rtl_linux_progress_gate.sh` with exact relocated kernel symbolization | `/tmp/rtl-linux-userspace-60m-900s-20260902/sim.log`, `/tmp/linux-singlecore-real-20260902/kernel/vmlinux`, `addr2line`/`objdump` output | The repeated `0x889b1c3c..0x889b1c44` window is `__udelay`, reached through Linux `try_misrouted_irq`/spurious-IRQ handling; the prior `0x0100101c` address fault was not reproduced. This narrows the open RTL Linux issue to SoC IRQ wiring/driver contract, without justifying a speculative CPU/CP0/timer workaround. RTL userspace boot and full RTL/QEMU Linux differential remain open |
| QEMU/RTL UART interrupt wiring contract | `SOC_INTEGRATED`, opt-in | `make irq-contract-audit`; `RUN_DIR=/tmp/qemu-vic-cpu-build-fixed-20260903 make qemu-system-vic-cpu-differential-gate`; `RUN_DIR=/tmp/linux-irq-contract-20260903-fixed KERNEL=build/linux_boot/real/kernel/vmlinux DTB=build/linux_boot/real/mips32_soc_ref.dtb SKIP_LINUX_BUILD=1 QEMU_TIMEOUT=30s tb/linux_boot/run_linux_boot_gate.sh`; `source /etc/profile.d/modules.sh && module load vcs && VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/rtl-frontend-irq-contract-20260903 make rtl-frontend-compile` | `build/verification_foundation/irq_contract_audit.log`, `/tmp/qemu-vic-cpu-build-fixed-20260903/isa_ref/qemu_system_vic_cpu_differential/`, `/tmp/linux-irq-contract-20260903-fixed/completion_report.md`, `/tmp/rtl-frontend-irq-contract-20260903/unit_tb/rtl_frontend_compile/` | QEMU `env->irq[n]` is `Cause.IP[n+2]`. Both custom-SoC and Linux guest paths model UART as VIC source 1 aggregated to IP2, matching RTL; the `linux-guest` property remains for other Linux compatibility behavior such as LL/SC reservation policy. The audit and 735-record VIC CPU differential pass, Linux userspace gate remains green, and RTL frontend compile passes. RTL Linux userspace, full RTL/QEMU Linux differential and unrestricted VM/shootdown remain open |
| L1/L2 nonblocking cache concurrency aggregate | `SYSTEM_DIFFERENTIAL`, opt-in | `VCS_JOBS=1 EDA_MEMORY_MAX=1500M EDA_SWAP_MAX=512M BUILD_DIR=/tmp/l1-l2-nonblocking-complete-20260903 make l1-l2-nonblocking-complete-gate` | `/tmp/l1-l2-nonblocking-complete-20260903/cache_concurrency_complete/cache_concurrency_completion_report.md` and child gate logs | The aggregate requires and combines the standalone L1/L2 MSHR and downstream RID/OOO checks, real CPU/D-cache nonblocking integration, DDR window path, and QEMU system-mode retire differential. It is opt-in and leaves the default blocking path unchanged; full MESI/directory coherency, arbitrary dirty-writeback ordering, Linux cache ABI and physical DDR PHY timing remain open |
